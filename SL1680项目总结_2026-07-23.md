# SL1680 语音终端 · 项目总结

**日期：2026-07-24**　　版本：v2.6　　设备：SL1680 (Synaptics Astra "Dolphin" RDK)

> 这份是**项目级总结**——项目是什么、现在能干什么、怎么构建、什么还没解决。
> 逐日调试细节见 `SL1680调试总结_2026-07-23_完整版.md`。

### 结论可信度标注

| 标记 | 含义 |
|---|---|
| ✅ **已验证** | 直接测到的现象，或改完复测确认生效 |
| ⚠️ **高概率推断** | 现象为真，但因果链是推的 |
| ❓ **待确认** | 需要厂商/更多数据才能定论 |

---

## 一 · 项目是什么

在 Synaptics SL1680（4×Cortex-A73 + NPU）上做一台**本地语音 AI 终端**，配 DisplayLink DL7400 扩展多屏显示。

**当前形态：不需要 PC。** 语音识别和合成全在板上跑，只有对话大脑走云端 DeepSeek。

### 硬件构成

| 部件 | 型号 | 接法 |
|---|---|---|
| 主板 | SL1680 Dolphin RDK | — |
| 麦克风 | Logitech C920（USB 音频接口） | USB `1-1.3` |
| 摄像头 | 同一个 C920（USB 视频接口） | 同上，两个接口可同时用 |
| 功放/喇叭 | HT517 | I2S，`dolphinasoc` |
| 多屏扩展 | DisplayLink DL7400（固件 12.3.26） | USB3 `1-1.1.1`，SuperSpeed |
| 显示器 | 3 块：2× 4K@60 + 1× 2880×1800@60 | DL7400 输出 |

> ⚠️ 板载麦克风 ZTS6672 有驱动层问题（i2s-mic1 时钟未使能），**现用 C920 替代**，需 Synaptics FAE 支持。

---

## 二 · 系统架构

```
[视觉] C920 摄像头 ──► NPU (mobilenet 人体检测, 25ms/帧)
                          └─► 人走近 ──► SIGUSR1 ──► astra_voice 播欢迎语

[语音] C920 麦克风 ──► silero VAD 断句 ──► SenseVoice ASR (CPU, int8)
                                              │
                                              ▼
                                    astra_llm.py 四层路由
                                    ① 本地音量指令  amixer 真的执行
                                    ② 本地设备指令  时间/温度/内存/切屏
                                    ③ 联网搜索      智谱 web_search + 接地
                                    ④ 云端对话      DeepSeek
                                              │
                                              ▼
                                    matcha TTS (CPU) ──► ALSA astraout ──► HT517

[显示] astra_voice ──► /tmp/astra_status.txt (中文)
       astra_translate ──► /tmp/astra_status_en.txt (英文)
                              │
                              ▼
                        dl_face ──► GStreamer ──► DL7400 ──► 3 块屏
                        屏0 = 表情脸 + 中英双语字幕
                        屏1 = 可语音切成 C920 实况画面
```

### 各环节用什么模型

| 环节 | 模型 | 跑在哪 |
|---|---|---|
| 人体检测 | `mobilenet224_full1` | **NPU** |
| VAD | `silero_vad.onnx` | CPU |
| 语音识别 | **SenseVoice int8** | CPU |
| **对话主模型** | **`deepseek-chat`** | 云端 |
| 联网搜索 | 智谱独立 Web Search API（`search_std`，约 ¥0.01/次） | 云端 |
| 断网兜底 | `qwen2.5-0.5b-instruct-q8_0.gguf` | CPU |
| 中英翻译 | 同 `deepseek-chat` | 云端 |
| 语音合成 | **matcha** + vocos 声码器 | CPU |

> ✅ NPU 只对视觉有用。语音链路全在 CPU —— Synaptics 自己的语音示例也全跑 CPU。

---

## 三 · systemd 服务

| 服务 | 作用 | 开机自启 |
|---|---|---|
| `astra-voice` | 语音助手主程序（VAD+ASR+四层回复+TTS，一进程常驻） | ✅ |
| `astra-translate` | 中英字幕翻译守护进程 | ✅ |
| `dl-face` | DL7400 对话界面 | ✅ |
| `vision-wake` | 视觉唤醒 | ✅ |
| `astra-cleanup.timer` | 每天 4:00 清语音段 | ✅ |
| `astra-mode` | 双模看门狗 | ❌ **已停用** |
| `astra-xiaozhi` | 云端 xiaozhi 客户端 | ❌ 停用 |

> **为什么停掉 `astra-mode`**：它每 8 秒探测 PC 的 8000 端口，**探到就会 `stop astra-voice` 切云端模式**。
> 板子回到家庭网络后和 PC 同网段，这个风险变高了。别再 enable。

---

## 四 · 功能清单与验证状态

全部在 2026-07-23 重启后复验：

| 功能 | 状态 | 实测 |
|---|---|---|
| 三块屏开机自动点亮 | ✅ | 2×3840×2160@60 + 1×2880×1800@60，`NRestarts=0` |
| 语音对话（云端 DeepSeek） | ✅ | 「你是谁」→「我是Astra语音助手」 |
| 联网实时数据 | ✅ | 「今天广州天气」→「多云到晴，26℃到35℃」真实数据 |
| 本地设备指令 | ✅ | 「现在几点」→「14点29分」（真实系统时钟） |
| 语音控制音量 | ✅ | 「音量调到20%」→ amixer 实测变 `[20%]` |
| 语音切摄像头 | ✅ | 「打开摄像头」→ 屏1 切 camera + vision-wake 自动停 |
| 语音切回表情脸 | ✅ | 「关闭监控」→ 复位 + vision-wake 自动恢复 |
| 视觉唤醒 | ✅ | 人走近播欢迎语 |
| 中英双语字幕 | ✅ | 屏上「你说/Astra」两行中英对照 |
| 断电重启全恢复 | ✅ | 软件 reboot 验证；另有一次非计划重启也全恢复 |
| 麦克风掉线不空转 | ✅ | 见下节 |

### 本版新增：麦克风等待守护

**问题**（2026-07-23 实测）：C920 挂的 `1-1.3` 总线会抖，实测掉线 **86 秒**。
期间 `astra_voice` 每次启动都报 `打不开 plughw:CARD=C920,DEV=0 (capture): No such device` 退出，
systemd 每 3 秒重拉一次，**每次都白白加载 14–17 秒模型再死掉**，连崩 7 次。

**解法**：`ExecStartPre=/usr/bin/astra_wait_mic.sh C920 120` 排在最前。

两个设计要点：
1. **按卡名判断而不是卡号** —— `/proc/asound/C920` 是按名字建的，卡号每次开机都会变（实测 C920 时而 card0 时而 card2）
2. **要等到采集节点而不只是卡目录** —— 实测卡目录先出现，`/dev/snd/pcmC<N>D0c` 要等 udev 再建一会儿，中间那个窗口去开会报 `No such file or directory`

**✅ 对抗性验证**（整设备 unbind/bind 模拟真拔插）：

| | 旧行为 | 新行为 |
|---|---|---|
| 麦克风不在时 | 崩 3 次 + 每次白加载模型 | `activating`，**加载模型 0 次、报错 0 次、NRestarts 0** |
| 麦克风插回来 | — | `[wait-mic] C920 在第 17 秒就绪` → 加载一次模型 → 开始监听，**NRestarts 全程 0** |

---

## 五 · 关键技术决策（都是踩出来的）

### 音频：`astraout` 是必需不是优化 ✅

Synaptics 驱动 `berlin_outdai_hw_params` 在**非 48000 采样率**（如 matcha 的 22050）下空指针崩溃，
把播放子设备卡成占用、owner 是已死 PID，**用户态无法释放，只能重启**。

解法：`/etc/asound.conf` 定义 `pcm.astraout` = `plug → softvol → plug(强制48000/2ch/S16_LE) → hw`，
硬件永远见不到 22050。**所有 ALSA 寻址必须用 `CARD=名字`**，索引 `hw:0` 会因声卡号重排而错。

### 显示：绝不能盲信 `preferred_mode` ✅

小米显示器 EDID 报 3840×2160**@160Hz** 为首选模式，DL7400 喂不动，
`dlsdk_display_show` **永久阻塞**（确定性卡在第 7 帧）。钳到 60Hz 立刻就好。

> 这个故障极像"显示器坏了/线坏了"，因为 **EDID 是显示器的属性，跟着屏走**。换线"没用"也是同一原因。

### 联网：智谱 chat 的 `web_search` 工具不工作 ✅

实测 `prompt_tokens` 只有 21、`search_result` 0 条，而模型**照样编出一个气温**
（编 15~25℃/18~28℃，真实 28~36℃，两个模型还互相矛盾）。

正确做法是调**独立搜索 API**，拿到真实网页内容后配 `GROUND_PROMPT`（"只根据这些资料回答，
绝对不许编造数字"）交给 DeepSeek。搜不到时，**天气/股价这类硬实时问题直接说查不到，不回落**。

### 误触发：纯能量门槛无解 ✅

实测办公室底噪 **RMS 575~713**，用户说话 **RMS 470~1170**——**两者重叠**。
`--min-rms` 提到 1150 的代价是必须靠近麦（30cm 内）。
**远场+嘈杂环境的根治方案是 KWS 唤醒词，还没做。**

---

## 六 · 已知未解决

| # | 问题 | 状态 |
|---|---|---|
| 1 | **误触发严重** —— 办公室人声被当指令，每次都真打云端 API | 需 KWS。板上 `/home/voice/kws/` 素材齐全但未接入 |
| 2 | **DL7400 屏断电不自动重亮** | SDK 三个缺陷，无安全在线检测手段。已提报告给 DisplayLink |
| 3 | **AEC 回声消除没做** | 现靠播报时丢采集帧 |
| 4 | **板载麦 ZTS6672 驱动问题** | 需 Synaptics FAE |
| 5 | **TTS 授权** | espeak-ng GPLv3 穿透整条链路，产品化前必解 |
| 6 | ~~`astra_voice` 二进制无法复现~~ | ✅ **v2.5 已解决** —— recipe 现在能完整构建，见第七节 |
| 7 | **模型约 2GB 不在镜像里** | 烧完要单独灌 `/home/voice/`，见 `README_models.md` |
| 8 | **镜像没在真板子上实烧验证过** | 板子 SSH 自 2026-07-23 晚不可达 |

### DL7400 断电不自愈的三个 SDK 缺陷

1. `dlsdk_register_hotplug_callback()` **恒返回 `DLSDK_UNSUCCESSFUL(2)`**，事件从未送达
   - ✅ 实测排除了平台原因（`LIBUSB_CAP_HAS_HOTPLUG=1`）
   - ⚠️ 但**没有**证明故障在 libdlsdk 内部——也可能是前置条件没满足或该配置本就不支持
2. EDID / preferred_mode / display 数量**全是缓存**，屏断电了照样返回 SUCCESS
3. **★致命**：对已断开的 sink 调 `dpaux_read`，在有并发推帧时会让该 device 上**所有输出一起黑屏**

> 危险版本留档在 `dl_face.c.DANGEROUS_dpaux_DO_NOT_BUILD`，**永远不要编进生产版**。
> 现行方案 = 语音说「重新检测屏幕」重启 dl-face 重新枚举。

---

## 七 · 仓库与构建

### 目录

```
meta-dlsdk/
  VERSION                                  ← 版本记录，每次同步追加
  recipes-ai/
    astra-voice/astra-voice_1.0.bb         ← 语音助手（2026-07-23 新建）
              /files/  astra_voice.c, astra_llm.py, astra_translate.py,
                       astra_wait_mic.sh, astra_setvol.sh, vision_wake.sh,
                       asound.conf, llm.conf.sample, silent.wav, *.service
    sherpa-onnx/sherpa-onnx_1.13.4.bb
  recipes-graphics/
    dl-face/dl-face_1.0.bb                 ← 显示界面（2026-07-23 新建）
    dl-clock/ dl-edid/ dl-player/ dlsdk/
tools/                                     ← 远程操作脚本
backups/                                   ← 备份包 + CHANGELOG + 部署清单
```

### 构建

```bash
cd /home/astra/sdk && source poky/oe-init-build-env build-sl1680
bitbake astra-media          # 完整镜像(含语音终端)
```

单独构建某个包：`bitbake astra-voice` / `bitbake dl-face` / `bitbake sherpa-onnx`。

**语音终端怎么进镜像**：`packagegroup-astra-terminal` 定义"这台机器是干什么的"，
由 `recipes-core/images/astra-media.bbappend` 挂到 Synaptics 官方镜像上 ——
沿用原来的构建命令和烧录流程，镜像名和产物布局都不变。
不想装：`local.conf` 里写 `ASTRA_TERMINAL_PACKAGES = ""`。

产出的 SYNAIMG 烧录包在 `tmp/deploy/images/sl1680/SYNAIMG/`，约 2.5 GB。

**⚠️ 模型不在 recipe 里**（约 2GB），装完镜像要单独灌到 `/home/voice/`，
见 `files/README_models.md` 和 `MODEL_MANIFEST.txt`。

### 构建期抓到的真 bug

**编译层**

1. **Yocto 硬关了 CMake 的 FetchContent** —— `cmake.bbclass` 传
   `-DFETCHCONTENT_FULLY_DISCONNECTED=ON`，sherpa 的 8 个第三方包全下不下来，
   configure 直接失败。**症状酷似"网络不通"，但日志里根本没有 curl/HTTP/超时记录**。
   解法：14 个包（含 4 层嵌套）走 `SRC_URI` 预取 → 拷进 `${S}` 命中 `possible_file_locations`
   → 再关掉那个开关。⚠️ `openfst`/`eigen` 各有两个版本被不同子项目索要，都得备着
2. **`-Werror=format-security`** —— espeak-ng 重设 C 标志丢了 `-Wformat`，
   剩下的 `-Wformat-security` 被 `-Werror` 变成致命错误
3. **字体包名** —— 是 `ttf-wqy-zenhei` 不是 `wqy-zenhei`，写错 `bitbake` 直接 `Nothing RPROVIDES`

**打包层（都是"构建全绿、烧板才炸"那一类）**

4. **无 SOVERSION 的 `.so` 被丢进 `-dev` 包** —— sherpa 的 CMake 从不设 SOVERSION，
   Yocto 默认把无版本号 `.so` 当开发用软链。运行时镜像里根本没有这个库。
   `SOLIBS=".so"` / `FILES_SOLIBSDEV=""` 修复，**已端到端验证**（deb 里 4 个 `.so` 都在）
5. **预编译 `libonnxruntime.so` 没被登记成符号版本提供者** —— `RPROVIDES` 显式声明
6. **第三方装错位置** —— `sherpa-onnx.pc` 装到 `${prefix}` 根下、`cargs.h` 装进 `${libdir}`

**镜像层（v2.6 验收时抓到，只看"构建成功"一个都发现不了）**

7. **`/etc/asound.conf` 与 `alsa-state` 撞车** → `do_rootfs` 直接失败。
   删掉 alsa-state 那份 **32 字节的占位注释**
8. **`dl-clock` 和 `dl-face` 都自启** → DLSDK **单进程独占**，两者抢 DL7400，
   表现为"屏幕黑着但服务 active"。dl-clock 改 `SYSTEMD_AUTO_ENABLE = "disable"`
9. **`vision-wake` 漏了自启** → 烧完的板子"人走近打招呼"不工作。
   ⚠️ 这是我判断错了：以为它独占 C920，实际视频/音频是两个独立接口可以并存。
   **应以板子实测状态为准，别按推测排除**
10. **`synap_cli_od` 依赖没声明** → 碰巧 `astra-media` 已装，换个 image 就静默失效

> **`SYSTEMD_AUTO_ENABLE:pn-<recipe>` 写在别的 recipe 的 bbappend 里不生效** ——
> 这种 `:pn-` 覆盖只在 conf 文件（local.conf/layer.conf）里有效。必须改到该 recipe 自己里。

### 仓库 vs 板子

> **板子是真相，仓库历史上多次落后。** 曾出现：`astra-voice.service` 停留在旧硬件配置、
> `astra_llm.py` 四层能力一个都没有、5 个文件仓库里根本不存在、
> 以及最危险的一次——板端回滚了会让两块屏全黑的代码但**仓库源码没还原**。

改完板子当场跑 `tools/export_live.sh` 导出回灌，用 `backups/2026-07-23/DEPLOY_MANIFEST.txt`
的 sha256 对照表验证一致。**判断某个回退文件安不安全必须 diff 内容，不能看文件名。**

---

## 八 · 远程操作

板子在家庭网络 `192.168.5.126`（root/1234），和 PC 同网段直连。

| 脚本 | 用途 |
|---|---|
| `tools/brun.ps1` / `bsh.ps1` / `bput.ps1` | **直连**跑命令 / 跑脚本 / 传文件 |
| `tools/wgrun.ps1` / `wgsh.ps1` / `wgput.ps1` / `wgget.ps1` | 板子在外面时经 WireGuard 跳板 |
| `tools/wgjump.ps1` | 只登跳板机，板子失联时区分"隧道断了"还是"板子没了" |
| `tools/export_live.sh` | 导出板端实际运行文件 + sha256 清单 |

### 六个反复踩的坑

1. **上传会被注入 3 字节 UTF-8 BOM** —— 文本没事，**二进制直接废**（`Exec format error`）。判据：板上大小 = 本地 +3
2. **`.cmd` 里绝不能写中文注释** —— cmd.exe 用 ANSI 代码页，中文变乱码并被当命令执行
3. **`powershell -File` 会把参数按空格重切** —— 外层引号保不住，`-w` / `$(...)` 被 PowerShell 抢去解析。**把远端命令写成脚本文件**
4. **busybox 缺项** —— 没有 `install`、`timeout`、`base64`、`ldd`；`head` 不认 `-c`/`-12`；`wget -T` 管不住 connect 挂起（用 `curl -m`）
5. **askpass 的子串匹配** —— `findstr /C:"192.168.5.1"` 会匹配上 `192.168.5.126`，把跳板密码喂给板子，只报 `Permission denied` 毫无线索
6. **板上找 PID 别用 `pgrep -f`** —— 会匹配到外层的 sh 自己

---

## 九 · 版本记录

| 版本 | 日期 | 内容 |
|---|---|---|
| **v2.6** | 2026-07-24 | **出了第一个「烧完即用」的完整镜像**；验收抓到 4 个「构建成功但功能不全」的问题 |
| **v2.5** | 2026-07-23 | **sherpa-onnx 首次在 Yocto 里构建成功**；根因是 Yocto 硬关了 CMake FetchContent |
| **v2.4** | 2026-07-23 | 麦克风等待守护（`astra_wait_mic.sh`），消除 USB 抖动时的空转重载 |
| **v2.3** | 2026-07-23 | Codex 审阅整改：移除危险 AUX 代码、板端配置全量回灌、补两个从来不存在的 recipe、修 sherpa `.so` 打包 |
| **v2.2** | 2026-07-23 | 语音「重新检测屏幕」；三个 DLSDK 诊断工具；DisplayLink 问题报告 |
| **v2.1** | 2026-07-23 | 双屏分角色 + 语音切监控；CPU 优化；本地设备工具 |
| **v2.0** | 2026-07-23 | 中英双语字幕；智谱联网搜索接地 |

完整改动见 `backups/CHANGELOG.md`，layer 版本见 `meta-dlsdk/VERSION`。

---

## 十 · 下一步建议

1. **治误触发（最高优先）** —— 接 KWS 唤醒词。板上 `/home/voice/kws/` 的 encoder/decoder/joiner int8、
   tokens、关键词文件都在，`sherpa-onnx-keyword-spotter-alsa` 也在，**只差接进链路**。
   顺带能解决"语音暂停对话"——现在做不到是因为回复链最后一层规则兜底必定出声，
   真静音只能停采音，而停了之后要用语音唤醒就必须有 KWS
2. **~~闭合构建验证~~** —— ✅ **已完成**（v2.5/v2.6）。三个 recipe 全绿，
   完整镜像已产出。**但没在真板子上实烧验证过**，这是现在最该补的一步
3. **产品化** —— 智能看护/安防终端（demo 已通，差多摄像头 CSI×2 + 录像 + 告警推送）
