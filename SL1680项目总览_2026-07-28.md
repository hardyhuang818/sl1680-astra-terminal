# SL1680 边缘 AI 终端 · 项目总览

**日期：2026-07-28**　版本：v2.7　设备：Synaptics SL1680（Astra "Dolphin" RDK）
**定位：本文是项目唯一的顶层总览**——三大成果域、关键知识、资产清单、未解决项、文档导航。此前各专题文档全部从第 9 节索引进入，不再需要逐份翻找。

---

## 1. 项目一句话

在 Synaptics SL1680（4×Cortex-A73 + NPU）上构建**边缘 AI 人机交互终端**：本地语音 AI（识别/合成全在板上）+ 两条显示链路（DisplayLink DL7400 USB 多屏 + TC358775 直驱 LVDS 触摸屏）+ 视觉唤醒，全部断电自愈。

**测试条件**：GitHub 开源 Astra SDK（Yocto scarthgap）+ 借用 SL1680 RDK ×1 与 DL7400 EVK ×1 + 自购外设（C920 摄像头、显示器 ×4 自有、TM10.5-TD7800 触摸屏、LAMTL 转接板、功放喇叭等）。

## 2. 系统总架构

```
                     ┌───────────────────────── SL1680 (4×A73 + NPU) ─────────────────────────┐
                     │                                                                         │
[C920 摄像头] ──USB──►│ NPU: mobilenet 人体检测 25ms/帧 ──► 人走近 ──► 视觉唤醒(欢迎语)          │
[C920 麦克风] ──USB──►│ CPU: silero VAD ──► SenseVoice ASR(int8) ──► 四层路由                   │
                     │      ①本地音量 ②本地设备 ③联网搜索(智谱search_std+接地) ④云端DeepSeek     │
                     │      ──► matcha TTS ──► ALSA astraout ──► [HT517 功放+喇叭] (I2S)        │
                     │                                                                         │
                     │ 显示链A: dl_face ──► GStreamer ──► [DL7400] ──USB3──► 最多4屏             │
                     │          屏0=表情脸+中英字幕  屏1=可语音切摄像头实况                        │
                     │ 显示链B: weston ──► DSI 4-lane ──► [LAMTL/TC358775] ──LVDS──►             │
                     │          [TM10.5-TD7800 1280×720 触摸屏] (触摸 I2C+INT 回板, ~120Hz)      │
                     └─────────────────────────────────────────────────────────────────────────┘
   云端：DeepSeek(对话/翻译) + 智谱独立搜索 API　｜　断网兜底：qwen2.5-0.5b 本地
```

## 3. 三大成果域

### 3A. 本地语音 AI 终端（v2.0 → v2.6）

| 能力 | 要点 | 实测 |
|---|---|---|
| 全本地语音链 | SenseVoice ASR(int8) + matcha TTS，不依赖 PC | ASR RTF 0.197（4 线程）；TTS 0.37 |
| 四层回复路由 | 音量/设备指令本地真执行；天气等实时题走真搜索+接地；闲聊走 DeepSeek | 「音量调到20%」→ amixer 实测 [20%] |
| 视觉唤醒 | NPU 人体检测 → SIGUSR1 → 欢迎语 | 25ms/帧 |
| 中英双语字幕 | astra_translate 守护，屏上两行对照 | ✓ |
| 麦克风守护 | 按卡名+采集节点双条件等待，USB 抖动 86 秒不空转 | 对抗验证：0 次模型重载 |
| 稳定性 | 断电重启全恢复；每天 4:00 自动清理 | 多次重启复验 ✓ |

### 3B. DL7400 USB 多屏（成果汇报域）

| 结论 | 数据 |
|---|---|
| 4 屏同时点亮可行 | 并发帧率 8.7 / 5.8 / 15.0 / 5.7 fps |
| USB3 总带宽恒定，屏越多每屏越慢 | 3→4 屏时 4K 屏 7.2→5.8 fps（−20%） |
| 视频管线瓶颈在 DL 推流 | 111→31.9(硬缩放)→7.4 fps 逐级实测 |
| 3 个 SDK 缺陷已报 DisplayLink | 热插拔回调恒失败 / EDID 缓存 / dpaux 全黑雷 |
| ⚠️ 屏断电不自愈 | 现行方案：语音「重新检测屏幕」重启 dl-face |

### 3C. TD7800 LVDS 触摸屏直驱（v2.7，2026-07-27/28 新增）

六层根因逐层排除后点亮，显示+触摸双通，断电自愈 9/9 项实测：

| # | 根因 | 修复 |
|---|---|---|
| 1 | base 设备树是树莓派屏残留配置 | dsi_panel 节点整体替换为 TM10.5 时序 |
| 2 | TC358775 上电三态无人初始化 | 29 条 DSI 通用长写序列烤入 DTB |
| 3 | 面板 RESX 悬空（整颗 TDDI 复位） | 硬件拉高 3.3V |
| 4 | U-Boot 把 SPI 数据脚占成 UART2 流控 | dts pinmux 夺回（顺带获得 SPI 调试通道） |
| 5 | DSI→LVDS 零带宽余量 → 全屏活雪花 | Byte_clk 65300 + LVCFG PCLKDIV=4（+33% 余量） |
| 6 | 触摸 INT 的 GPIO bank 归属不明 | 实验法定位 porta10（ATTN 锁低→I2C 排空→释放者即是） |

配套产出：TD7800 SPI 调试工具（读器件码/BIST/读 NVM）、触摸 120Hz 多点报点、180° 显示翻转 + 触摸校准、上海时区。

## 4. 关键知识沉淀（踩坑换来的，跨项目可复用）

1. **DSI→LVDS 桥必须留带宽余量**：LVDS 像素钟 = DSI 时钟线频率 ÷ PCLKDIV；DSI 侧开销（包头/EoTp/LP）会吃掉零余量配置 → 行缓冲欠载 = 活雪花
2. **TDDI 复位纪律**：TP_RST/RESX 是整颗芯片的命门——悬空全哑，交给 SoC 控会杀显示，一律硬件拉高
3. **电平域纪律**：1.8V 域引脚喂 3.3V 会经 ESD 二极管倒灌抬轨（实测 2.38V）
4. **EDID preferred_mode 不可盲信**：显示器报 160Hz 首选模式让 DL7400 永久阻塞；故障跟屏走，酷似硬件坏
5. **ALSA 非 48k 采样率会内核崩溃**：astraout 强制重采样链是必需品；寻址必须用 CARD=名字
6. **智谱 chat 的 web_search 是假的**：必须调独立搜索 API + 接地提示词；搜不到宁可说查不到
7. **Yocto 四类暗坑**：FetchContent 被硬关（症状像断网）；无 SOVERSION 的 .so 进错包；`:pn-` 覆盖只在 conf 生效；构建全绿≠功能完整（必须 debugfs 级验收）
8. **板子是真相**：仓库多次落后于板端；改完必回灌 + sha256 对账
9. **远程操作三律**：复杂命令一律脚本文件（PowerShell 重切参数）；busybox 缺项先查（head -c/timeout/install/base64）；二进制传输防 BOM

## 5. 软件资产

| 资产 | 位置 | 说明 |
|---|---|---|
| Yocto layer | `meta-dlsdk/`（VERSION v2.7） | astra-voice / dl-face / sherpa-onnx / dlsdk / **linux-syna.bbappend（TD7800 补丁）** |
| 完整镜像 | `flash_image_20260723163055/` | SYNAIMG 2.5GB，烧完即用（模型另灌） |
| TD7800 boot 包 | `SL1680_td7800_boot/` | 三代镜像 + 烧录/验证脚本 + 复盘 README |
| 模型备份 | `backups/models_2026-07-24/` | 1.15GB 唯一副本（⚠️ etc_astra.tar.gz 含明文密钥，勿外传） |
| 调试代码备份 | `TD7800_bringup_backup_2026-07-28/` | 79 文件+SHA256：dts/补丁/板侧43脚本/WSL27脚本/证据帧 |
| 板端 rootfs 三件套 | 板上 /etc | 180°翻转 / 触摸校准 / 时区（待进 Yocto） |

**构建**：`source poky/oe-init-build-env build-sl1680 && bitbake astra-media`；内核单独重建 75 秒（`bitbake linux-syna -C compile` + `-c deploy -f`）；boot 分区 dd 双槽 + python 回读校验（全程 ~4 分钟）。

## 6. 硬件资产与接线

| 器件 | 接口 | 接线文档 |
|---|---|---|
| SL1680 RDK | J208（DSI 22P）/ J32（40P 排针，占用表见接线总表） | 《硬件接线总表》§1/§3 |
| LAMTL 转接板 | J1（DSI 入）/ J3（LVDS 出）/ J2、J4 未用 | 同上 §1/§2 |
| TM10.5-TD7800 | U4（LVDS+触摸）/ U5（电源/SPI/复位）/ U3（背光 LED） | 同上 §2/§3 |
| DL7400 EVK | USB3 上行 + 4× DP/HDMI | 即插即用 |
| 音频套件 | HT517 功放（I2S，J32）+ 喇叭；麦用 C920 | 《小智套件接线指南》 |
| 外部电源 | 12V 背光（经外接驱动板→LED）/ 3.3V 面板+复位 / 1.8V LAMTL | 接线总表 §4（含电平域警示） |

## 7. 当前运行状态（板端实测，2026-07-28）

| 项 | 状态 |
|---|---|
| 自启服务 | astra-voice / astra-translate / dl-face / vision-wake / cleanup.timer ✅；astra-mode、astra-xiaozhi、dl-clock 停用 |
| 显示 | DL7400 多屏 ✅ + TM10.5 LVDS 桌面 ✅（180° 翻转、CST 时钟） |
| 触摸 | 0x2C 驱动接管、IRQ porta10、evtest 报点、方向校准 ✅ |
| boot | `boot_td7800_touch.subimg`（sha 0aa90172…）双槽；回退备份在板上 /home |
| 断电自愈 | 9/9 项自检通过（含显示、触摸、校准、时区、SPI 通道） |

## 8. 已知未解决（合并全项目）

| # | 问题 | 优先级 |
|---|---|---|
| 1 | **KWS 唤醒词未接入**——误触发烧云端 API；素材全在 `/home/voice/kws/` 只差接线 | ★ 最高 |
| 2 | AEC 回声消除缺失（现靠播报时丢采集帧） | 高 |
| 3 | DL7400 屏断电不自愈（3 个 SDK 缺陷，已报 DisplayLink） | 中（等厂商） |
| 4 | 板载麦 ZTS6672 驱动问题（需 Synaptics FAE） | 中 |
| 5 | TTS 授权：espeak-ng GPLv3 穿透链路（产品化前必解） | 中 |
| 6 | 新镜像未在真板实烧验证 | 中 |
| 7 | LVDS 杜邦线换双绞（现能跑，属边缘状态） | 低 |
| 8 | TD7800 补丁 cleanall 干净构建验证 | 低 |
| 9 | rootfs 三件套（tzdata/翻转/触摸校准）进 Yocto | 低 |

## 9. 文档导航（治"凌乱"的入口——只记这一节即可）

**顶层**（先看）：
- 本文《SL1680项目总览_2026-07-28》（中/英，md+html）——唯一顶层入口
- 《SL1680项目总结_2026-07-23》（中/英）——v2.6 语音终端详版（构建 bug 清单最全）

**TD7800 屏专题**（v2.7 新增域）：
- 《TM10.5_TD7800屏点亮_总结_2026-07-28》（中/英，md+html）——六层根因+修改路径
- 《SL1680_TD7800_LAMTL_硬件接线总表_2026-07-28》（中/英，md+html）——逐脚接线基准
- 《TC358775_配置与调试记录_2026-07-28》（md+html）——29 条初始化逐条注释+报错原文
- 《TD7800_SPI_读ID与BIST_源代码_2026-07-28》（md+html）——SPI 工具完整代码
- `TD7800_bringup_backup_2026-07-28/README_备份说明.md`——79 文件备份清单

**汇报与对比**：
- 《SL1680_DL7400_成果汇报_2026-07-24》（中/英）——四屏+帧率给管理层的版本
- 《SL1680_vs_ESP32_对比_2026-07-24》——平台选型
- 《SL1680_机器人手指驱动方案调研》《SL1680_电容触觉手指方案调研》《iCub指尖触觉传感器论文整理》——机器人方向储备

**运维**：
- 《SL1680备份与恢复手册_2026-07-24》——五套备份怎么用
- `SL1680_td7800_boot/README_TD7800屏点亮.md`——烧录与回退
- `meta-dlsdk/VERSION`——版本流水账；`backups/CHANGELOG.md`——逐日改动

## 10. 下一步路线

1. **KWS 接入**（一举治误触发 + 解锁"语音暂停"）
2. 新镜像真板实烧验证 + rootfs 三件套进 Yocto → 交付同事"一次烧录全功能"
3. 机器人方向：SL1680 躯干算力 + TM10.5 脸 + DL7400 遥操作台 + 手指执行器（调研已备）——与 Synaptics 官方"高速接口×机器人"叙事对齐
