# Astra SL1680 构建环境与命令手册

**日期**：2026-08-01　**适用**：Synaptics Astra SL1680（Dolphin RDK）
**SDK 版本**：`scarthgap_6.12_v2.3.0`（Yocto 5.0 scarthgap / kernel 6.12.62）
**受众**：需要搭建同款构建环境的同事。本文所有数据来自实际在跑的构建机，不是抄官方文档。

---

## 0. 一图看懂整体结构

```
┌─ Windows 11 ────────────────────────────────────────────────┐
│  D:\...\sl1680-astra-terminal\        ← git 仓库(文档+自有层源)  │
│                                                              │
│  ┌─ WSL2: Ubuntu 22.04 ────────────────────────────────┐    │
│  │  /home/astra/sdk/                ← 官方 SDK(钉版本)    │    │
│  │    ├─ poky/                        Yocto 5.0.9        │    │
│  │    ├─ meta-synaptics/              官方 BSP 层         │    │
│  │    ├─ meta-openembedded/ 等        官方自带            │    │
│  │    ├─ meta-dlsdk/            ★ 我们的层(从仓库接入)     │    │
│  │    ├─ meta-tcm2-touch/       ★ 我们的层(触摸驱动)       │    │
│  │    └─ build-sl1680/                构建目录             │    │
│  │         └─ tmp/deploy/images/sl1680/   产物            │    │
│  └──────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────┘
                    │ scp / dd
                    ▼
            SL1680 板子 (192.168.5.126)
```

**核心原则：不 fork 官方 SDK。** 上游钉死在 tag，我们的全部修改装在两个自有 Yocto 层里
（内核改动以 `bbappend` + patch 形式打在官方 `linux-syna` 上）。官方升级时只需换 tag 重新验证。

---

## 1. 主机要求（实测配置）

| 项 | 本机实际 | 最低建议 |
|---|---|---|
| 系统 | **Ubuntu 22.04.5 LTS**（WSL2） | Ubuntu 20.04/22.04，物理机或 WSL2 都行 |
| CPU | 24 核 | ≥8 核（核越多全量构建越快） |
| 内存 | 47 GB | ≥16 GB（链接 chromium/qt 时吃内存） |
| 磁盘 | 1 TB（已用 357 GB，其中 `tmp/` 占 **159 GB**） | **≥500 GB 空闲**，SSD |
| Python | 3.10.12 | ≥3.8 |
| git | 2.34.1 | ≥2.28 |

> WSL2 用户注意：把 SDK 放在 **WSL 原生文件系统**（`/home/...`），不要放 `/mnt/c` ——
> 跨文件系统 IO 会把构建拖慢一个数量级。本机 WSL 虚拟盘单独挂了 1TB（`/dev/sdd`）。

### 1.1 依赖包（Yocto scarthgap 标准 + 少量补充）

```bash
sudo apt update
sudo apt install -y gawk wget git diffstat unzip texinfo gcc build-essential \
  chrpath socat cpio python3 python3-pip python3-pexpect xz-utils debianutils \
  iputils-ping python3-git python3-jinja2 python3-subunit zstd liblz4-tool \
  file locales libacl1 bmap-tools
sudo locale-gen en_US.UTF-8
```

---

## 2. 获取源码（四步，幂等）

```bash
# 1) 官方 SDK —— 版本必须钉死，勿用 master
git clone --branch scarthgap_6.12_v2.3.0 https://github.com/synaptics-astra/sdk ~/sdk
cd ~/sdk
# SDK 自带 poky/meta-synaptics 等子层；若为 submodule 形式:
git submodule update --init --recursive 2>/dev/null || true

# 2) 我们的仓库（含 meta-dlsdk / meta-tcm2-touch 两个层 + 全部文档）
git clone git@github.com:hardyhuang818/sl1680-astra-terminal.git ~/sl1680-astra-terminal

# 3) 接层（脚本幂等，重复跑无害；做两件事：symlink 层目录 + 写 bblayers.conf）
~/sl1680-astra-terminal/tools/setup_sdk.sh ~/sdk

# 4) 验证层都接上了
cd ~/sdk && source poky/oe-init-build-env build-sl1680
bitbake-layers show-layers | grep -E "dlsdk|tcm2"
```

### 2.1 版本锚点（对账用）

| 组件 | 版本 |
|---|---|
| Astra SDK | tag `scarthgap_6.12_v2.3.0` |
| poky | yocto-5.0.9（SDK 自带） |
| meta-synaptics | `d552818`（SDK 自带，勿单独升级） |
| kernel（linux-syna） | 6.12.62 |
| 我们的层 | `meta-dlsdk`（版本流水见其中 `VERSION` 文件，当前 v2.9）+ `meta-tcm2-touch` |

### 2.2 bblayers.conf 最终形态（供核对）

官方自带层之外，末尾追加两行即可：

```
  /home/astra/sdk/meta-tcm2-touch \
  /home/astra/sdk/meta-dlsdk \
```

### 2.3 local.conf 关键项（SDK 模板默认已带，列出便于核对）

```
MACHINE ??= "sl1680"
PACKAGE_CLASSES ?= "package_deb"
INIT_MANAGER = "systemd"
LICENSE_FLAGS_ACCEPTED = "commercial Synaptics-EULA ..."
IMAGE_FSTYPES += " synaimg ext4 ext4.gz"
INITRAMFS_IMAGE_BUNDLE = "1"
```

我们唯一会动的开关：`ASTRA_TERMINAL_PACKAGES = ""`（置空 = 跳过语音/多屏应用，退回原厂镜像）。

---

## 3. 构建命令全集

**每个新终端都要先进环境**（bitbake 只在 source 之后可用）：

```bash
cd ~/sdk && source poky/oe-init-build-env build-sl1680
```

### 3.1 全量镜像（首次约 3~6 小时，视机器）

```bash
bitbake astra-media
```

产物在 `tmp/deploy/images/sl1680/`：

| 文件 | 用途 |
|---|---|
| `SYNAIMG/` | **完整烧录包**（约 2.5GB），配官方 USB 烧录工具 |
| `astra-media-sl1680.rootfs.ext4` | rootfs 单独镜像 |
| `linux_bootimgs.subimg` | **boot 包**（kernel + DTB + initramfs），可单独 dd |
| `Image-sl1680.bin` / `*.dtb` | 裸 kernel / 设备树 |

### 3.2 内核快速迭代（日常最常用，全程 **约 75 秒**）

改了 `dolphin-rdk.dts` 或内核源码之后：

```bash
bitbake linux-syna -C compile        # 大写 C：强制从 compile 重跑（含 dts 重编）
bitbake linux-syna -c deploy -f      # 强制重新部署产物
```

> ⚠️ 直接改的是 `tmp/work-shared/sl1680/kernel-source/` 下的源码 ——
> **这个目录是易失的**，`cleanall` 就没。改完必须回灌成 patch 放进
> `meta-dlsdk/recipes-kernel/linux/files/`（怎么正确生成 patch 见 §5.6）。

改完后**必须字节级验证 DTB**（构建成功 ≠ 改动生效）：

```bash
python3 - <<'EOF'
b = open("/home/astra/sdk/build-sl1680/tmp/deploy/images/sl1680/dolphin-rdk.dtb","rb").read()
print("我的节点在吗:", b"synaptics,tcm-i2c" in b)
EOF
```

### 3.3 单包构建（改了某个 recipe 之后）

```bash
bitbake astra-voice                  # 语音主程序
bitbake sherpa-onnx                  # 本地 ASR/TTS 运行时
bitbake dl-face                      # DL7400 对话界面
bitbake synaptics-tcm2               # 触摸驱动内核模块(meta-tcm2-touch)
```

### 3.4 清理（由轻到重）

```bash
bitbake <recipe> -c clean            # 清 work 目录，保留 sstate
bitbake <recipe> -c cleansstate      # 连 sstate 一起清（强制真重编）
bitbake <recipe> -c cleanall         # 再连 downloads 一起清（会重新下载！慎用）
```

> 验证"补丁能否从零应用"用 `cleansstate` + 重编即可，**不要**动不动 `cleanall`。

### 3.5 排查与检查

```bash
bitbake <recipe> -e | grep ^SRC_URI=          # 看变量展开后的实际值
bitbake <recipe> -c devshell                  # 进 recipe 的构建环境 shell
bitbake-layers show-recipes "*tcm*"           # recipe 在哪个层
bitbake-layers show-appends linux-syna        # 谁 append 了它
oe-pkgdata-util find-path "*/astra_voice"     # 文件属于哪个包
```

构建日志位置：`tmp/work/<arch>/<recipe>/<ver>/temp/log.do_<task>`

---

## 4. 烧录

### 4.1 全量烧录（USB 工具，第一次/大版本用）

Windows 侧用官方 `usb-tool-astra-update`（板子进 USB 烧录模式），
把 `SYNAIMG/` 整目录喂给它。约 2.5GB，10 分钟内。

### 4.2 只刷 boot（内核/DTS 迭代用，30 秒）

板子跑着的时候直接 dd（A/B 双槽都刷）+ **sha256 回读校验**：

```bash
scp tmp/deploy/images/sl1680/linux_bootimgs.subimg root@192.168.5.126:/tmp/
ssh root@192.168.5.126 'sh -s' <<'EOF'
IMG=/tmp/linux_bootimgs.subimg
SZ=$(wc -c < $IMG)
dd if=$IMG of=/dev/mmcblk0p8 bs=1M 2>/dev/null
dd if=$IMG of=/dev/mmcblk0p9 bs=1M 2>/dev/null
sync
python3 -c "
import hashlib
exp = hashlib.sha256(open('$IMG','rb').read()).hexdigest()
for d in ('/dev/mmcblk0p8','/dev/mmcblk0p9'):
    got = hashlib.sha256(open(d,'rb').read($SZ)).hexdigest()
    print(d, 'OK' if got==exp else 'MISMATCH!')
"
EOF
```

然后**断电重上**（见 §5.1，这块板软复位不可靠）。

### 4.3 烧完镜像后还差两样

| 缺什么 | 怎么补 |
|---|---|
| 约 2GB 语音模型（不进镜像） | 单独灌到板子 `/home/voice/`，清单见 `meta-dlsdk/recipes-ai/astra-voice/files/README_models.md` |
| DLSDK 二进制（DisplayLink NDA） | 构建前放入 `meta-dlsdk/recipes-graphics/dlsdk/files/`，清单+sha256 见同目录 `BINARIES_MANIFEST.txt`；不用 DL7400 就在 local.conf 置 `ASTRA_TERMINAL_PACKAGES = ""` |

---

## 5. 踩过的坑（每条都真踩过，按血泪值排序）

### 5.1 这块板软复位不可靠
`reboot -f`、`systemctl reboot -ff`、sysrq 全部无效。**每次刷完 boot 都按"需要人工断电重上"来安排**，
改动尽量攒成一次重启。

### 5.2 bbappend 文件名的版本通配规则（两边坑正好相反）
- `linux-syna` 配方**带版本号**（6.12.62）→ append 必须写 **`linux-syna_%.bbappend`**（带 `_%`）
- `tzdata` 配方**不带版本号** → append 必须写 **`tzdata.bbappend`**（不带 `_%`）
写反的症状都是 `No recipes available for ...`，bitbake 不会告诉你是文件名的事。

### 5.3 cmake FetchContent 被 Yocto 硬关（症状像断网）
`cmake.bbclass` 默认 `FETCHCONTENT_FULLY_DISCONNECTED=ON`，第三方包在 do_configure 阶段
下载失败，报错看起来像网络问题——其实根本没联网。解法：全部第三方包写进 `SRC_URI`
（带 sha256）+ `do_configure:prepend` 拷到源码树命中 `possible_file_locations`。
参照 `meta-dlsdk/recipes-ai/sherpa-onnx/sherpa-onnx_1.13.4.bb`（14 个包的完整做法）。

### 5.4 无 SOVERSION 的 .so 会被打进 -dev 包
构建全绿、包也出了，烧到板子上就是没有那个 .so ——它被分进了 `-dev` 包。
症状离构建极远，极难倒查。解法见 sherpa-onnx recipe 里的 `SOLIBS`/`FILES` 处理。

### 5.5 镜像集成四坑（构建成功 ≠ 功能完整）
① 两个包装同一个文件 → do_rootfs 冲突；② 两个服务抢同一设备（DLSDK 单进程独占）；
③ 包装进去了但服务没 enable（`SYSTEMD_AUTO_ENABLE` 只在 recipe 里生效，
`SYSTEMD_AUTO_ENABLE:pn-X` 写在 conf 里**不生效**）；④ 验收必须 debugfs 直读产物 ext4，
别信构建清单。

### 5.6 内核补丁不能用 `git diff` 生成
Yocto 的 `do_patch` 会把旧补丁 commit 进 kernel-source 的 git HEAD，
`git diff` 只会输出**增量**（实测 70 行 vs 应有 248 行）。正确做法：
对**原始基线 dts** 做完整 `diff -u`，然后干跑重放验证：

```bash
# 基线 + 补丁 == 当前源码，字节级一致才算数
patch -p1 --dry-run < 新补丁 && patch -p1 -s < 新补丁 && cmp 打完的文件 当前文件
```

### 5.7 debugfs 不跟符号链接
weston.ini 真身是 `/etc/xdg/weston/weston.ini.weston-init`（update-alternatives 管理）。
用 debugfs 验收镜像时查真身文件，否则误报"配置缺失"。

### 5.8 WSL 特有
- 从 Windows 调 WSL 跑复杂命令，**用脚本文件**别用内联命令（引号会被 PowerShell/bash 两层转义打碎）
- Git Bash 会把 `/home/...` 路径重写成 Windows 路径，调 wsl 要从 PowerShell 走

---

## 6. 与板端的关系（团队约定）

**板子是真相。** 在板上直接调通的东西，必须回灌仓库并跑 sha256 对账：

```bash
tools/export_live.sh        # 拉板端文件与仓库比对
```

- 版本流水：`meta-dlsdk/VERSION`（每个里程碑一条，含板端 sha256）
- 每里程碑：git commit + tag（v2.7、v2.8、v2.9…）
- 逐日改动：`backups/CHANGELOG.md`

## 7. 参考文档索引

| 文档 | 内容 |
|---|---|
| `SETUP_SDK.md` | 四步复现的最简版 |
| `SL1680项目总览_2026-07-28.md` | 项目顶层文档（三大成果域 + 全部专题索引） |
| `meta-dlsdk/VERSION` | 版本流水与每版改动明细 |
| `SL1680_故障排查实录_2026-07-28.md` | 运行时问题排查实录 |
| `TD7800触摸根因与视觉唤醒修复_2026-07-28.md` | 硬件电平域问题的完整方法论 |

---

*本文数据取自实际构建机（Ubuntu 22.04.5 / 24 核 / 47GB / tmp 占 159GB），2026-08-01 核对。*
*DLSDK 二进制与面板厂商资料受 NDA 约束，请勿随文档外发。*
