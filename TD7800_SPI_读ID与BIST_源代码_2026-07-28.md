# TD7800 显示 SPI：读 0xBF 器件码 & BIST 模式 —— 完整源代码

日期：2026-07-28 ｜ 实测平台：SL1680 (dolphin RDK) → TM10.5-TD7800 ｜ 状态：**全部代码已实测通过**
实测读数：`BFh = 02 3C 68 0A`（与 datasheet 标准值逐字节一致）；BIST 图案点亮验证过。

---

## 1. 前置条件

**硬件**（4 线 SPI + 复位，接线详见《硬件接线总表》）：

| 面板 U5 | 信号 | SL1680 J32 |
|---|---|---|
| 1 | SCK | J32.23（SPI2_CLK） |
| 2 | SDA（MOSI，板→屏） | J32.19（SPI2_SDO） |
| 3 | SDO（MISO，屏→板） | J32.21（SPI2_SDI） |
| 4 | SS（片选） | J32.24（SPI2_SS0n） |
| 9 | RST TFT（RESX） | **必须拉高 3.3V**（悬空=芯片复位，SPI 无响应） |

**软件**：`/dev/spidev0.0` 由三处 DTS 配置提供（已固化在 boot 里）——`&spi0` 使能 + `spidev@0` 子节点 + `spi2_data_pmux`（把被 U-Boot 占成 UART2 流控的 SM_SPI2_SDO/SDI 夺回 spi2 功能）。使用前 `modprobe spidev`。

**为什么用 ctypes 不用 fcntl**：板上是裁剪版 Python 3.12，`import fcntl` 报 `ModuleNotFoundError`——所以 ioctl 走 `ctypes.CDLL(None).ioctl` 直调 libc。

## 2. 协议要点（TD7800B datasheet §22.1）

- **9-bit 帧**：首位 DCX（0=命令，1=参数）+ 8 位数据，MSB 在前；SPI mode 0（芯片在 SCL 上升沿采样写入、下降沿输出读数）
- **写**：每个寄存器 = 1 个命令帧 + N 个参数帧（CS 同一窗口内连发）
- **读**：`38h`（进读模式）→ 发读命令帧并继续给哑元时钟，SDO 吐数据 → `39h`（退读模式）。第一个返回字节 = 命令回显
- **SDO 有 1-bit 相位偏移**（下降沿驱动的半拍差）——解析时把返回字流拼成位流、按"找到命令回显"锚定再切字节（代码里 `read_reg` 就是干这个）
- 本文用到的寄存器：

| 寄存器 | 名称 | 用法 |
|---|---|---|
| B0h | Manufacturer Command Access Protect | 写 `00` 解锁厂商命令（写 `03` 重新上锁） |
| D6h | Sequencer Test Control | BIST 前写 `00` |
| DEh | Test Image Generator | 参数1 bit0=TIGON：`01`=开测试图案，`00`=关（视频源回 LVDS） |
| BFh | Read Device Code（只读，12 参数） | 前 4 字节标准值 **02 3C 68 0A**（Synaptics Supplier ID + 器件码）——读通 = SPI 链路 + 芯片全活 |
| B1h / C0h | LVDS IF Setting / Display Timing | 读面板 NVM 实际配置（对时序答案用） |
| 29h / 11h | Display On / Sleep Out | 厂商 initial code 的点亮收尾（纯命令帧，无参数） |

## 3. 完整整合版源代码（单文件，5 种操作）

板上保存为 `/tmp/td7800_spi_tool.sh`，用法：`sh td7800_spi_tool.sh id|bist_on|bist_off|lighton|cfg`

```sh
#!/bin/sh
# TD7800 显示 SPI 工具（SL1680 侧）
#   id       读 0xBF 器件码（期望 02 3C 68 0A）
#   bist_on  开测试图案（B0<-00, D6<-00, DE<-01）
#   bist_off 关测试图案（DE<-00，视频源回 LVDS）
#   lighton  发 29h Display-On + 11h Sleep-Out（厂商 initial code 收尾序）
#   cfg      读面板 NVM 配置（B1h LVDS 设置 / C0h 显示时序）
modprobe spidev 2>/dev/null
python3 - "$1" <<'EOF'
import struct, ctypes, os, sys, time

# ---- ioctl 底层（板上 python 无 fcntl，ctypes 直调 libc）----
libc = ctypes.CDLL(None, use_errno=True)
def ioctl(fd, req, buf):
    r = libc.ioctl(fd, ctypes.c_ulong(req), buf)
    if r < 0:
        raise OSError(ctypes.get_errno(), "ioctl 0x%x" % req)

DEV = "/dev/spidev0.0"
SPI_IOC_WR_MODE          = 0x40016b01   # u8
SPI_IOC_WR_BITS_PER_WORD = 0x40016b03   # u8
SPI_IOC_WR_MAX_SPEED_HZ  = 0x40046b04   # u32
SPI_IOC_MESSAGE_1        = 0x40206b00   # struct spi_ioc_transfer ×1

fd = os.open(DEV, os.O_RDWR)
ioctl(fd, SPI_IOC_WR_MODE,          ctypes.byref(ctypes.c_uint8(0)))    # mode 0
ioctl(fd, SPI_IOC_WR_BITS_PER_WORD, ctypes.byref(ctypes.c_uint8(9)))    # 9-bit DCX 帧
ioctl(fd, SPI_IOC_WR_MAX_SPEED_HZ,  ctypes.byref(ctypes.c_uint32(500000)))  # 500kHz（上限10MHz）

def xfer(words):
    """全双工传一串 9-bit 字；CS 在整个 transfer 内保持有效。
    每个字在缓冲区占 2 字节（小端 u16），dw-apb-ssi 原生支持 9bpw。"""
    n = len(words) * 2
    tx = ctypes.create_string_buffer(b"".join(struct.pack("<H", w) for w in words), n)
    rx = ctypes.create_string_buffer(n)
    # struct spi_ioc_transfer: u64 tx_buf, u64 rx_buf, u32 len, u32 speed_hz,
    #   u16 delay_usecs, u8 bits_per_word, u8 cs_change, u8 tx_nbits, u8 rx_nbits, u16 pad
    t = struct.pack("QQIIHBBBBH", ctypes.addressof(tx), ctypes.addressof(rx),
                    n, 500000, 0, 9, 0, 0, 0, 0)
    ioctl(fd, SPI_IOC_MESSAGE_1, ctypes.create_string_buffer(t, len(t)))
    return [struct.unpack_from("<H", rx.raw, i * 2)[0] & 0x1FF for i in range(len(words))]

# ---- TD7800 9-bit 帧语义 ----
def cmd(c):   xfer([c])            # DCX=0：命令帧（bit8=0）
def param(p): xfer([0x100 | p])    # DCX=1：参数帧（bit8=1）

def write_reg(c, params=()):
    """一个 CS 窗口内：命令帧 + 全部参数帧"""
    xfer([c] + [0x100 | p for p in params])

def read_reg(c, nparams):
    """38h 进读模式 -> 发读命令并继续给哑元时钟采样 SDO -> 39h 退出。
    SDO 输出有 1-bit 相位偏移：把返回字拼成位流，锚定命令回显后按 8-bit 切参数。"""
    xfer([0x038])                                   # enter_read_mode
    rx = xfer([c] + [0x000] * (nparams + 3))        # 读命令 + 哑元，全双工采样
    xfer([0x039])                                   # exit_read_mode
    bs = "".join(format(w, "09b") for w in rx)
    for off in range(0, 18):                        # 容错扫描位偏移
        if off + 8 <= len(bs) and int(bs[off:off + 8], 2) == c:   # 找到回显
            out, k = [], off + 8
            while k + 8 <= len(bs) and len(out) < nparams:
                out.append(int(bs[k:k + 8], 2)); k += 8
            return out
    return None

# ---- 操作分发 ----
op = sys.argv[1] if len(sys.argv) > 1 else "id"

if op == "id":
    p = read_reg(0xBF, 5)
    ok = p and p[:4] == [0x02, 0x3C, 0x68, 0x0A]
    print("BFh =", " ".join("%02X" % v for v in p) if p else "读取失败",
          "<- 通过, IC 活着" if ok else "")

elif op == "bist_on":
    write_reg(0xB0, [0x00])     # 解锁厂商命令
    write_reg(0xD6, [0x00])     # 序列器测试控制
    write_reg(0xDE, [0x01])     # TIGON=1 测试图案开
    print("BIST ON —— 看屏出图案")

elif op == "bist_off":
    write_reg(0xB0, [0x00])
    write_reg(0xDE, [0x00])     # TIGON=0，视频源切回 LVDS 输入
    print("BIST OFF —— 视频源回 LVDS")

elif op == "lighton":           # 厂商 initial code 尾序（文件名"Light on with 11h29h"）
    cmd(0x29); time.sleep(0.2)  # Display On
    cmd(0x11); time.sleep(0.15) # Sleep Out
    print("29h + 11h 已发")

elif op == "cfg":
    b1 = read_reg(0xB1, 7)      # LVDS IF Setting
    c0 = read_reg(0xC0, 7)      # Display Timing
    print("B1h =", " ".join("%02X" % v for v in b1) if b1 else "失败")
    if b1:
        print("  LVFMT=%d(0=VESA) LVCOLMOD=%d(1=24bit4lane) LVSYNM=%d(2=DE+SYNC)"
              % ((b1[0] >> 3) & 1, (b1[0] >> 2) & 1, b1[0] & 3))
        print("  LVHBP=%d LVHFP=%d LVVBP=%d"
              % (((b1[1] & 0xF) << 8) | b1[2], ((b1[3] & 0xF) << 8) | b1[4],
                 ((b1[5] & 0xF) << 8) | b1[6]))
    print("C0h =", " ".join("%02X" % v for v in c0) if c0 else "失败")
    if c0:
        print("  RTN(1H)=%d RCLK  VBP=%d  NL(行数)=%d  VFP=%d"
              % (((c0[0] & 0xF) << 8) | c0[1], c0[2],
                 ((c0[3] & 0xF) << 8) | c0[4], (c0[5] << 8) | c0[6]))

os.close(fd)
EOF
```

## 4. 实测输出样例（2026-07-27/28 板上原始记录）

```
# id —— RESX 拉高后首次命中
RX = 1FF 17E 008 1E3 080 140 000 …
★ 命中器件码 02 3C 68 0A (stride=8, off=1)   ← 1-bit 相位偏移的实际体现

# cfg —— 面板 NVM 与厂商 FW 表逐字节一致
B1h = 06 00 5A 00 61 00 10   → VESA / 24bit 4lane / DE+SYNC，porch 90/97/16
C0h = 01 0F 0A 02 D0 00 0A   → RTN=271 RCLK，NL=720（原生行数实锤），VFP=10

# bist_on → 屏出测试图案（面板整条显示通路无罪的判据）
# bist_off + weston restart → LVDS 桌面
```

## 5. 踩坑备忘（这套代码为什么长这样）

1. **`fcntl` 不存在** → ctypes 直调 libc ioctl（`use_errno=True` 拿错误码）
2. **9 bpw 的缓冲区排布**：每字 2 字节小端；dw-apb-ssi 支持 4–16 bpw，9 没问题
3. **SDO 的 1-bit 偏移**：不做位流锚定会把 `02 3C 68 0A` 读成移位乱码——`read_reg` 的偏移扫描就是解药
4. **读之前芯片必须出复位**：RESX（U5.9）悬空时一切读写全静默（全 0 或全 1FF）
5. **pinmux 前提**：SM_SPI2_SDO/SDI 默认被 U-Boot 设成 UART2 流控（收发全 0 的元凶），已在 DTB 固化夺回；若在别的镜像上复用本代码，先 `devmem 0xf7fe2c14` 确认值为 `0x00005A01`
6. 速率放 500kHz 是调试保守值，datasheet 写周期 ≥100ns（≈10MHz 上限）

## 6. 原始分散脚本对照（备份目录 `TD7800_bringup_backup_2026-07-28/board-scripts/`）

| 原始脚本 | 内容 | 本文对应 |
|---|---|---|
| `td_spidev_test2.sh` | 首个 spidev 版：ID 读（双 mode 自动试）+ BIST | `id` + `bist_on` |
| `full_retest.sh` | RESX 修复后全套重测（含触摸探针） | `id` + `bist_on` |
| `read_panel_cfg.sh` | 读 BFh/B1h/C0h 并解码 | `cfg` |
| `bist_off_lvds.sh` | 关 BIST + weston restart 切 LVDS | `bist_off` |
| `dispon_sleepout.sh` | 29h/11h 点亮尾序 | `lighton` |
| `spi_loopback.sh` | J32.19↔21 短接回环自证 | （诊断用，未并入） |
| `td_spi_test.sh` | 早期 GPIO 位敲版（后被硬 SPI 取代） | （历史存档） |
