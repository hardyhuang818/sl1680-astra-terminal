# TM10.5-TD7800 LVDS 屏点亮总结（显示 + 触摸）

日期：2026-07-28 ｜ 平台：Synaptics SL1680 (dolphin RDK) ｜ 结果：**显示 + 触摸双链路全通，断电重启自动恢复（9/9 自检项通过）**

---

## 1. 最终链路

```
SL1680 J208 (MIPI-DSI 4-lane, 522.4Mbps/lane)
   → FPC → LAMTL 转接板 (Toshiba TC358775, DSI→LVDS)
   → LVDS 单链 5 对差分 (OD口/第一链, 457Mbps/对)
   → TM10.5 面板 U4 的 A 口 → TD7800 TDDI → 1280×720@60 玻璃

触摸: TD7800 → TP I2C 0x2C (TW0, J32.3/5) + ATTN 中断 (J32.12 = porta10)
      → synaptics_tcm2 驱动 → /dev/input/event2 (双指多点, ~120Hz)
SPI 调试: TD7800 显示寄存器口 (U5.1/2/3/4 ↔ J32.23/19/21/24) → /dev/spidev0.0
```

## 2. 重启持久性自检（2026-07-28 实测，开机 0 分钟时采集）

| # | 项 | 结果 |
|---|---|---|
| 1 | DSI-1 连接器 | connected / enabled / 1280x720 ✓ |
| 2 | 触摸驱动 | i2c 0x2C = UU（驱动接管）✓ |
| 3 | 触摸中断 | IRQ48 (porta10) 正常计数 ✓ |
| 4 | 触摸校准 | LIBINPUT_CALIBRATION_MATRIX=-1 0 1 0 1 0 ✓ |
| 5 | weston | active，transform=rotate-180 ✓ |
| 6 | 时区 | CST (Asia/Shanghai) ✓ |
| 7 | SPI 调试通道 | /dev/spidev0.0 ✓（需 `modprobe spidev`） |
| 8 | SPI pinmux | 0x00005A01（DTB 固化，无需 devmem）✓ |
| 9 | 桌面 | 正向显示 + 触摸交互 ✓ |

## 3. 六层根因（按发现顺序）

| # | 根因 | 症状 | 修复 |
|---|---|---|---|
| 1 | base 设备树的 dsi_panel 是**树莓派 7 寸屏配置**（800×480/1-lane，power-supply 指向物理不存在的 i2c 0x45 芯片） | DSI-1 永远 disconnected，SoC 不发流 | 替换为 TM10.5 时序节点，删除 RPi 引用 |
| 2 | **TC358775 上电 LVDS 输出三态**，无人写寄存器 | 就算有 DSI 流也无 LVDS 输出 | 29 条初始化命令（DSI 通用长写）烤进 DTB |
| 3 | **面板 RESX（U5.9 RST TFT）悬空** | 整颗 TD7800 趴在复位：触摸 0x2C 消失 + 屏黑 + SPI 无响应（三症同源） | 拉高 3.3V（硬件） |
| 4 | **U-Boot 把 SM_SPI2_SDO/SDI 复用成 UART2 流控** | SPI 收发全 0，调试通道假死 | dts 加 pinmux 节点夺回 spi2 功能 |
| 5 | **DSI→LVDS 零带宽余量** | 全屏随机彩色雪花（活的），光栅稳定 | Byte_clk 48975→65300（+33%），LVCFG PCLKDIV 3→4 |
| 6 | 触摸 INT 脚 bank 归属不明（GPIO10 在三个 gpiochip 里都有 line10） | 无法写中断节点 | 实验法定位：ATTN 锁低 → I2C 排空报文 → 释放变高的 gpio586 = porta10 |

**第 5 层专述**（最隐蔽的一层）：DSI 链路有包头/EoTp/LP 切换开销，把输入带宽配成和 LVDS 输出严格相等时，有效像素速率必然低于面板消耗率 → 桥片行缓冲每行欠载 → 输出随机数据。对照实验：RK3568 原生 LVDS 直出（无 DSI 转换级）用同样杜邦线正常，证明不是线材。公式：**LVDS 像素钟 = DSI 时钟线频率(byteclk×4) ÷ PCLKDIV；DSI 带宽必须留显著余量**（厂商例 4.4%，本项目 33%）。

## 4. 主要修改与路径

### 4.1 内核设备树（核心修改，全部烤进 boot 镜像）

**文件**：`/home/astra/sdk/build-sl1680/tmp/work-shared/sl1680/kernel-source/arch/arm64/boot/dts/synaptics/dolphin-rdk.dts`（WSL Ubuntu-22.04）
**原始备份**：同目录 `dolphin-rdk.dts.pre_td7800`
**⚠️ work-shared 是易失目录**（`bitbake -c cleanall` 即清空），正式载体是 4.2 的仓库补丁。

四块改动：

1. **`&drm` 下的 `dsi_panel` 节点整体替换**：
   - 时序：1280×720@60，pclk 65300kHz，H 前廊97/同步30/后廊60，V 前廊7/同步2/后廊14（HTOTAL 1467 / VTOTAL 743）
   - `Lanes=4`，`Byte_clk=65300`（⚠️ 不能回调 48975，会全屏雪花）
   - 删除 `power-supply`/`backlight`（原指向不存在的 RPi 芯片）
   - `command` = TC358775 初始化序列，格式 `0x29 <长度> <寄存器LE16> <数据LE32>`，延时 `0xFF <微秒LE32>`；关键值：LANEENABLE=0x1F（4 lane）、VPCTRL=0x03F00100、HTIM1=0x003C001E、HTIM2=0x00610500、VTIM1=0x000E0002、VTIM2=0x000702D0、LVMX 七条=VESA 映射、**LVCFG=0x41**（LVEN+PCLKDIV=4，单链）
   - 命令由 VPP 显示固件在每次 modeset 时经 DSI 链路 LP 模式自动下发（无需 I2C）
2. **`&i2c0` 下新增 `synaptics_tcm@2c`**（触摸）：compatible `synaptics,tcm-i2c`，INT=`<&porta 10 0x2008>`（=J32.12，pad I2S2_DI1）；**故意不配 reset-gpio**（TDDI 复位会连显示一起杀）
3. **`&spi0` 使能 + spidev 子节点**（SM SPI2 → J32.19/21/23/24，TD7800 显示 SPI 调试通道）
4. **`&sm_pinctrl` 下新增 `spi2_data_pmux`**：把 SM_SPI2_SDO/SDI 从 U-Boot 设置的 uart2 功能夺回 spi2

### 4.2 仓库回灌（正式载体）

- `D:\Claude code\Case6_Astra\meta-dlsdk\recipes-kernel\linux\linux-syna.bbappend`
- `D:\Claude code\Case6_Astra\meta-dlsdk\recipes-kernel\linux\files\0001-dolphin-rdk-td7800-tm10p5-lvds-panel.patch`（186 行，含上述全部四块）
- `D:\Claude code\Case6_Astra\meta-dlsdk\VERSION` → v2.7 条目
- ⚠️ 补丁尚未跑过 cleanall→do_patch 干净构建验证（板子当前配置由 work-shared 直接编译产生，内容一致）

### 4.3 板端 boot 分区（已烧录生效）

- 分区：`/dev/mmcblk0p8`（boot_a）+ `/dev/mmcblk0p9`（boot_b）双槽同刷
- 最终镜像：`boot_td7800_touch.subimg`，sha256 `0aa901725795467ba867e5a11982fadc6a606b7003f89393d327a0fd263dac75`
- 备份（板上 /home/）：`boot_a_backup_td7800.img` / `boot_b_backup_td7800.img`（点屏工程之前的原始 boot）
- 回退：`dd if=/home/boot_a_backup_td7800.img of=/dev/mmcblk0p8 bs=1M; dd if=/home/boot_b_backup_td7800.img of=/dev/mmcblk0p9 bs=1M; sync; reboot`

### 4.4 板端 rootfs 三件套（重烧 rootfs 会丢，待进 Yocto）

| 文件 | 内容 | 作用 |
|---|---|---|
| `/etc/xdg/weston/weston.ini` | 追加 `[output]` `name=DSI-1` `transform=rotate-180` | 画面 180°（面板物理倒装），触摸坐标自动跟转 |
| `/etc/udev/rules.d/99-td7800-touch-cal.rules` | `LIBINPUT_CALIBRATION_MATRIX="-1 0 1 0 1 0"` | 触摸 X 轴镜像校准 |
| `/usr/share/zoneinfo/Asia/Shanghai` + `/etc/localtime` | TZif（从 WSL 拷入，镜像无 tzdata） | CST 时区 |

### 4.5 烧录包目录

`D:\Claude code\Case6_Astra\SL1680_td7800_boot\`：三代镜像（首版/带宽修正版/触摸版）、烧录脚本、验证脚本、README（含完整复盘）。

## 5. 硬件接线定案

**电源/控制（LAMTL 侧）**：
- VCC1V8_LCM（J1-8）：外供 **1.8V**；⚠️ TC358_RST/TC358_PWR_EN 必须用 **1.8V 电平**——接 3.3V 会经 RESX 的 ESD 二极管倒灌把 1.8V 轨抬到 2.38V（实测踩坑）
- 12V 背光外供；3.3V 从 J208.1（PWR_3V3_CTL）；使能 J208.6（PWR_ON_DSI）→ J1-13
- **面板 U5.9（RST TFT/RESX）拉高 3.3V**——悬空 = 整颗 TD7800 复位（头号坑）

**LVDS 数据（LAMTL J3 OD 口 → 面板 U4 A 口）**：

| LAMTL J3 | 信号 | 面板 U4 | 信号 |
|---|---|---|---|
| 7 / 8 | OD0− / OD0+ | 18 / 19 | LVD0A− / LVD0A+ |
| 9 / 10 | OD1− / OD1+ | 21 / 22 | LVD1A− / LVD1A+ |
| 11 / 12 | OD2− / OD2+ | 24 / 25 | LVD2A− / LVD2A+ |
| 15 / 16 | ODCK− / ODCK+（时钟） | 27 / 28 | LVDCLKA− / LVDCLKA+ |
| 17 / 18 | OD3− / OD3+ | 30 / 31 | LVD3A− / LVD3A+ |

（单链走第一链=OD 组，datasheet Fig 5-10 实锤；ED 组/面板 B 口全不接；GND 多根共地）

**触摸 + SPI（J32 排针）**：TP SCL→J32.5、SDA→J32.3、INT→J32.12、GND→J32.9；SPI SCK→J32.23、MOSI→J32.19、MISO→J32.21、CS→J32.24 ↔ 面板 U5 的 1/2/3/4。

## 6. 调试工具箱（保留可复用）

- **SPI 直控面板**（`/dev/spidev0.0`，9-bit DCX 帧，板上 python3 用 ctypes 调 ioctl）：
  BIST 开：`B0←00, D6←00, DE←01`；关：`DE←00`；读器件码 `0xBF` 应回 `02 3C 68 0A`；可读 B1h/C0h 等面板 NVM 配置
- **触摸报点**：`evtest /dev/input/event2`（坐标 0-1279×0-719、双指、~120Hz）；可视化画板 `weston-simple-touch`
- **展示横幅**：gst-launch textoverlay+clockoverlay（标题+走秒时钟；⚠️ text 属性里不能放换行，多行用多个 textoverlay）
- **重建烧录流程**（~4 分钟全程）：改 dts → `bitbake linux-syna -C compile` + `-c deploy -f`（75 秒）→ 字节级验证 DTB → scp → 板上 dd 双槽 + python 回读校验 → reboot

## 7. 遗留事项

1. LVDS 杜邦线换双绞（现在能跑，属边缘工作状态；457Mbps/对）
2. meta-dlsdk 补丁做一次 cleanall 干净构建验证
3. rootfs 三件套（tzdata/weston.ini/udev 规则）进 Yocto recipe
4. 面板 FSEL（U4-40，极性翻转周期）当前悬空默认，未见异常，暂不处理
