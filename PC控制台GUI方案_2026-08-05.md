# SL1680 PC 控制台 GUI 方案

**日期**：2026-08-05　**结论先行**：**可以做，而且不用装任何新东西。**
板子上 python3 + systemd + 现成脚本已经覆盖了所有控制点，缺的只是一层"远程可点"的壳。

---

## 1. 三条技术路线对比

| 路线 | 做法 | 优点 | 缺点 | 判断 |
|---|---|---|---|---|
| A. PC 原生程序包 SSH | Windows 程序（如 PyQt）把按钮翻译成 ssh 命令 | 板子零改动 | 每台 PC 要装环境；每次点击开一条 ssh，慢（实测 1~2s/次）；密码管理麻烦 | 备选 |
| **B. 板端 Web 控制台** ★ | 板子跑一个 **python3 标准库 HTTP 服务**（约 300 行，无任何依赖），PC/手机浏览器打开 `http://192.168.5.126:8080` | PC **零安装**；手机也能用；一次开发到处用；实时状态推送容易 | 板子加一个 service（无依赖，风险极低） | **推荐** |
| C. 完整前后端框架 | FastAPI/Vue 等 | 好看 | 板上没 pip，装依赖是大工程；杀鸡用牛刀 | 否 |

**推荐 B 的关键依据**（都是已验证的事实）：
- 板上 `python3 3.12.9` 自带 `http.server`/`json`/`subprocess`，**零依赖**就能起 API 服务
- 所有控制动作板上已有现成命令（见 §2 清单），API 层只是薄薄一层转发
- 板子和 PC 在同一局域网（`192.168.5.126`），在外可走已有的 WireGuard
- 前端单文件 HTML（我们已有同款样式体系），浏览器打开即用

---

## 2. 可控点清单（全部基于板上已有能力，无需新驱动）

### 2.1 功能开关（systemctl，全部现成）

| 开关 | 背后命令 | 说明 |
|---|---|---|
| 语音助手 | `systemctl start/stop astra-voice` | |
| 视觉唤醒 | `systemctl start/stop vision-wake` | 与"摄像头实况"互斥，UI 要做联动 |
| 多屏表情界面 | `systemctl start/stop dl-face` | DLSDK 单进程独占，与 dl-clock 互斥 |
| 定时提醒守护 | `systemctl start/stop astra-timer` | |
| 开机自启 | `systemctl enable/disable <svc>` | 与"立即启停"分开两列 |

### 2.2 语音调节（现成脚本/ALSA）

| 控制 | 背后命令 | UI 形态 |
|---|---|---|
| 播放音量 | `astra_setvol.sh <0-100>`（带回读核对） | 滑块 + 当前值 |
| 麦克风增益 | `amixer -c <N> sset Mic <0-100>%` | 滑块（默认 50%，超过会削顶——UI 标红区） |
| 采音门槛 RMS | astra_voice 配置（当前 1150） | 滑块 + "太轻被跳过"计数实时显示 |
| TTS 试说 | 写 `/tmp/astra_say.txt` + `SIGUSR2`（记忆系统已用此通道） | 文本框 + "说"按钮 |
| 唤醒测试 | `kill -USR1 <pid>`（视觉唤醒同款信号） | 按钮 |

### 2.3 视觉唤醒参数

| 控制 | 现状 | UI 形态 |
|---|---|---|
| 触发框高阈值 | `vision_wake.sh 45 2` 的第 1 参数 | 滑块 30~80%，改后自动重启服务 |
| 防抖帧数 | 第 2 参数 | 数字 1~5 |
| 实时框高 | journal 心跳日志已有 `框高=NN%` | 实时曲线/数字，**调阈值时的标定神器** |
| 摄像头快照 | 复用 `/tmp/vw_cur.jpg`（检测管道现成产物） | 缩略图 2s 刷新 |

### 2.4 舵机（pca9685.py 已实测）

| 控制 | 背后命令 | UI 形态 |
|---|---|---|
| 单通道角度 | `pca9685.py angle <ch> <0-180>` | **16 路滑块**（默认折叠只展开已用通道） |
| 演示摆动 | `pca9685.py loop <ch> <sec>` / `stop` | 按钮对 |
| 全部松力 | `pca9685.py alloff` | **红色急停钮**（常驻右上角） |

### 2.5 提醒与记忆（记忆系统 v2.9 的文件直读直写）

| 控制 | 数据源 | UI 形态 |
|---|---|---|
| 倒计时/每日提醒 | `timers.json` / `daily.json` | 列表 + 删除 + 新增表单 |
| 长期记忆 | `memory.md`（本来就设计为人类可编辑） | 文本编辑器 + 保存 |

### 2.6 状态仪表盘（只读，全部现成数据源）

| 指标 | 数据源 |
|---|---|
| 电源轨电压/电流 | **板载 INA3221**（`/sys/class/hwmon`，PWR_3V3/VDDM_1V8 等 6 轨+电流）——舵机接入后看电源健康特别有用 |
| 服务状态灯 | `systemctl is-active` ×5 |
| CPU/内存/磁盘/uptime | `/proc` |
| 触摸活着吗 | `/proc/interrupts` 中断计数增量 |
| 摄像头/麦克风在不在 | `lsusb` + `/dev/v4l/by-id` |
| I2C 设备图 | `i2cdetect`（触摸 0x2c / INA 0x40-41 / PCA9685 0x70） |
| 最近对话 | `journalctl -u astra-voice` 尾部（`[你]`/`[机器]` 行） |

### 2.7 显示链路（应急按钮）

| 控制 | 背后命令 | 场景 |
|---|---|---|
| 重启 weston | `systemctl restart weston` | 桥片重新初始化，屏黑救急 |
| 面板 Display-On | SPI 发 `29h/11h`（td7800_spi_tool.sh 现成） | 复位后屏不亮 |
| BIST 测试图 | 同工具 `bist_on/off` | 排查"屏还是链路"问题 |

### 2.8 明确不放进 UI 的

- **重启板子** —— 软复位不可靠（需人工断电），做成按钮只会造成"点了没反应"的误会；只放"需要断电重启"的提示
- 烧录/内核操作 —— 风险高频次低，留在命令行
- 板子密码修改等系统级设置

---

## 3. 架构与界面布局

```
┌─ PC / 手机浏览器 ────────────────────────────────────────┐
│  http://192.168.5.126:8080  （单文件 HTML+JS，无需安装）    │
└──────────────┬──────────────────────────────────────────┘
               │ HTTP GET/POST (JSON) · 2s 轮询状态
┌──────────────▼──────────────────────────────────────────┐
│  板端 astra-webctl.service (python3 http.server, ~300行) │
│    GET  /api/status      ← 一次返回全部仪表盘数据           │
│    POST /api/service     ← {name, action}                │
│    POST /api/volume      ← {value}                       │
│    POST /api/servo       ← {ch, angle} / {action:stop}   │
│    POST /api/say         ← {text}                        │
│    GET  /api/snapshot    ← 摄像头快照 jpg                  │
│    ...                                                   │
└──────────────────────────────────────────────────────────┘
```

界面五个区（原型见 `SL1680控制台_UI原型_2026-08-05.html`）：

```
┌────────────────────────────────────────────────┬─────────┐
│ ① 状态条: 服务灯×5 · 电源轨 · CPU/内存 · [急停]  │         │
├──────────────┬─────────────────┬───────────────┤ ⑤ 实时  │
│ ② 功能开关    │ ③ 语音调节        │ ④ 视觉唤醒     │   日志  │
│  五个 toggle  │  音量/麦/门槛滑块  │  阈值+实时框高  │  对话流 │
│  +自启开关    │  TTS试说          │  摄像头快照     │         │
├──────────────┴─────────────────┴───────────────┤         │
│ ⑥ 舵机 16 路滑块(折叠)  ⑦ 提醒列表  ⑧ 记忆编辑    │         │
└────────────────────────────────────────────────┴─────────┘
```

## 4. 工作量估算

| 阶段 | 内容 | 估时 |
|---|---|---|
| M1 | 板端 API（状态+开关+音量+舵机+说话）+ 前端骨架 | 1 天 |
| M2 | 视觉唤醒调参+快照、提醒/记忆编辑、日志流 | 1 天 |
| M3 | 显示应急按钮、互斥联动、进 Yocto recipe 自启 | 半天 |

**安全边界**：只监听局域网；板子是测试镜像默认凭据（用户已拍板保留），控制台不再引入新的认证面，但 API 只暴露白名单动作（不开任意命令执行）。

---

## 5. 与 Robot PDF 的衔接（为什么这个 GUI 值得做）

刚学的《Synaptics Tactile Tech for Robot》里，Synaptics 自家的灵巧手 PoC 就配了
**"Software & GUI：rawdata 报告 / 力值输出 / 滑移检测 / 标定 + Visualization toolkit"**——
控制台+可视化是机器人演示的标配形态。

我们的板子已经是他们图里 "Astra Edge Compute + Actuator Ctrl + Camera/Mic 多传感器" 那一块
（SL1680 + PCA9685 舵机 + C920 + 麦克风）。如果后续拿到 SN6012T/CTS 触觉模块（SPI/I2C，3.3V，
正好 J32 还有空闲的 SPI2_SS1n/SS3n），这个 Web 控制台就是现成的**触觉数据可视化载体**——
加一个 `/api/tactile` 端点画 5×12 力值热力图即可，和他们 Coralboard 上的 GUI 对标。

---

*相关：`SL1680功能一页纸_2026-07-31.html`（能力总览）· `PCA9685_MG90S_接入SL1680_2026-07-31.md`（舵机）*
