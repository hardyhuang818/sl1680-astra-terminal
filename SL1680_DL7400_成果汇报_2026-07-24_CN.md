# SL1680 + DL7400 项目成果汇报

**日期**：2026-07-24　　**平台**：Synaptics SL1680 (Dolphin RDK) + DisplayLink DL7400 EVK
**所有数据均来自真机实测**，非纸面推导。

---

## 一 · 执行摘要

在 Synaptics SL1680 上做出一台**本地语音 AI 终端 + 多屏显示系统**，当前状态：

| 能力 | 结论 |
|---|---|
| **多屏扩展** | ✅ **4 屏并发实测跑通**，混合分辨率，无需内核驱动 |
| **语音交互** | ✅ 板上 ASR + TTS 全本地，云端大脑，端到端 5~6 秒 |
| **视觉唤醒** | ✅ NPU 人体检测，人走近自动打招呼 |
| **摄像头上屏** | ✅ C920 实况画面可语音切换到指定屏 |
| **产品化就绪度** | ⚠️ 可做**数字标牌 / 看板 / 交互终端**；❌ 不能做视频墙 |

**一句话定位**：这是一台**多屏内容展示 + 语音交互终端**，不是视频播放设备。

---

## 二 · 测试条件（软硬件基础）

**这套系统是在极低资源投入下做出来的**：一块借来的开发板、一块 EVK、几个自购配件，
软件基于 Synaptics 在 GitHub 的开源 SDK。

### 2.1 硬件清单

| 项 | 型号 / 规格 | 数量 | 来源 |
|---|---|---|---|
| **主控开发板** | Synaptics SL1680 Astra（Dolphin RDK）<br>4×Cortex-A73 + NPU，4 GB LPDDR4 | 1 | **借用**（原 Yealink 测试板，已保留出厂镜像可还原） |
| **多屏扩展** | DisplayLink DL7400 EVK（Redwood-8Gb Video Dock）<br>USB `17e9:7000`，固件 12.3.26 | 1 | **借用** |
| 摄像头 + 麦克风 | Logitech C920（一个 USB 设备提供音频 + 视频两个接口） | 1 | **自购** |
| 功放 | HT517（I2S 数字功放） | 1 | 自购 |
| 喇叭 | 配套扬声器 | 1 | 自购 |
| **显示器 ×4** | Sculptor 便携屏 2880×1800 @60（HDMI）<br>Redmi 27 NU 3840×2160 @60<br>Mi Monitor 3840×2160 @60<br>三星 S24D390 1920×1080 @60 | 4 | **自有** |

> **未使用**：GPU、外接 PC、专用 AI 加速卡。语音 AI 全部跑在 SL1680 本身的 CPU 和 NPU 上。

### 2.2 软件基础

| 组件 | 来源 | 授权 | 说明 |
|---|---|---|---|
| **Astra Yocto SDK** | **GitHub `synaptics-astra`（开源）** | 开源 | 分支 `scarthgap_6.12_v2.3.0`，提交 `a0344af`（2026-04-14） |
| Yocto / Poky | scarthgap 5.0.9 | 开源 | 内核 6.12.62 |
| **DLSDK** | ⚠️ **不在开源 SDK 里** | **闭源**（`LICENSE = CLOSED`） | Synaptics/DisplayLink 单独提供的预编译 `libdlsdk.so` + 固件包 |
| sherpa-onnx | GitHub k2-fsa | Apache-2.0 | 语音识别/合成推理框架 |
| SenseVoice / matcha / silero | 开源预训练模型 | 各自协议 | 见 §3.2 |
| 云端对话 | DeepSeek API | 商业 API | 仅对话大脑走云端 |

> ⚠️ **一个必须说清的边界**：Synaptics 在 GitHub 开源的是 **Astra Yocto BSP**（内核、板级支持、
> 多媒体、NPU 运行时）。**DisplayLink 的 DLSDK 不在其中**，是单独提供的闭源二进制，
> 目前需 NDA。据官方应用笔记，**DLSDK（官方名 DisplayLink Direct）计划 2026-08-01 公开发布**，
> 届时这条产品线的开源/产品化门槛会显著降低。

### 2.3 我们自己做的部分

在上述基础上，本项目新增：

| 产出 | 说明 |
|---|---|
| **`meta-dlsdk` Yocto layer** | 把 DLSDK 打包进 Yocto，含 udev 规则、固件自动升级、依赖收敛 |
| **4 个显示应用** | `dl_face` / `dl_player` / `dl_clock` / `dl_edid`（全部自研 C 程序） |
| **语音终端** | `astra_voice`（C）+ 四层路由（Python）+ 双语字幕守护进程 |
| **完整可烧录镜像** | `bitbake astra-media` 一条命令产出，沿用原厂烧录流程 |

### 2.4 测试环境

| 项 | 说明 |
|---|---|
| 构建环境 | WSL2 Ubuntu 22.04，Yocto scarthgap |
| 交叉编译 | aarch64（`gcc-cross` 13.3.0） |
| 板子连接 | 局域网 SSH 直连；外出时经 WireGuard 隧道远程调试 |
| 测试周期 | 2026-07-13 ~ 2026-07-24（约 10 天） |
| 数据采集 | 帧率取程序内置计数器每 60 s 输出；CPU 用 `/proc/stat` 前后差值（非 `top` 单次采样） |

---

## 三 · 多屏能力（重点）

### 3.1 四屏并发实测结果

DL7400 的 **4 个输出口全部占满**，四路帧数持续增长，无一卡死：

| 屏 | 型号 | 厂商 ID | 分辨率 | 端口 | 实测帧率 |
|---|---|---|---|---|---|
| 0 | Sculptor 便携屏（HDMI） | YCT 0xAC00 | 2880×1800 @60 | `^0` | **8.7 fps** |
| 1 | Redmi 27 NU | XMI 0xB005 | 3840×2160 @60 | `^1` | **5.8 fps** |
| 2 | S24D390（三星） | SAM 0x0B65 | 1920×1080 @60 | `^2` | **15.0 fps**（跑满） |
| 3 | Mi Monitor | XMI 0x27B4 | 3840×2160 @60 | `^3` | **5.7 fps** |

### 3.2 带宽分摊规律（三屏 → 四屏对比）

| 配置 | 4K 屏帧率 | 1080p 屏帧率 |
|---|---|---|
| 三屏（4K + 1080p + 4K） | **7.2 fps** | 15.0 fps |
| 四屏（2880×1800 + 4K + 1080p + 4K） | **5.8 fps** | 15.0 fps |
| 变化 | **↓ 约 20%** | 不变（已跑满设定上限） |

**结论**：USB 3.0（5 Gbps）总吞吐基本恒定，**新增屏只是摊薄每路份额**，不是线性劣化。
1080p 屏因像素量小，始终能跑满程序设定的 15 fps 上限。

### 3.3 视频播放能力（瓶颈定位）

4K VP9 → 1080p，150 帧实测：

| 管线阶段 | 耗时 | 帧率 | 说明 |
|---|---|---|---|
| 纯硬件解码 4K VP9 | 1.35 s | **111 fps** | 解码毫无压力 |
| ＋软件 `videoconvert/videoscale` | 17.55 s | 8.5 fps | ❌ 浪费 94% 时间 |
| ＋硬件 `synavideoconvertscale` | 5.63 s | **31.9 fps** | ✅ 快 3.7 倍，管线上限 |
| ＋`appsink sync=true`（按时间戳） | — | 17.6 fps | |
| ＋推送到 DL7400 | — | **7.4 fps** | ★ **真正的瓶颈在 DL 推流** |

**摄像头实况**：C920 MJPEG 720p30 → 推 4K 屏，实测 **7.0 fps**。

### 3.4 ⚠️ 一个必须澄清的误导性数据

| 内容类型 | 分辨率 | 帧率 |
|---|---|---|
| **真实视频**（4K VP9 → 1080p） | 1920×1080 | **7.4 fps** |
| 合成测试图（仅一根竖条在动） | 1920×1080 | 60.3 fps ⚠️ **不可用于规划** |

DisplayLink 的 DL3+ 压缩**只传变化区域**。合成测试图几乎不传数据 → 假高帧率。
真实视频每帧全变 → 只有 1/8。**早期基于合成图的 60fps 结论已作废。**

### 3.5 性能定位

| 用途 | 可行性 |
|---|---|
| 数字标牌 / 看板 / 仪表盘 / 时钟（低变化内容） | ✅ **很合适**，DL3+ 压缩优势正好发挥 |
| 交互界面 / 字幕 / 状态显示 | ✅ 很合适 |
| 摄像头监控画面 | ⚠️ 7 fps，可用但不流畅 |
| 全屏流畅视频 / 视频墙 | ❌ **7.4 fps，不可用** |

---

## 四 · 语音交互能力

### 4.1 系统架构

```
[视觉] C920 摄像头 → NPU 人体检测(25ms/帧) → 人走近 → 自动打招呼

[语音] C920 麦克风 → silero VAD 断句 → SenseVoice ASR(CPU)
                                            ↓
                                    四层路由决策
                                    ① 本地音量指令   → amixer 真的执行
                                    ② 本地设备指令   → 时间/温度/内存/切屏
                                    ③ 联网实时搜索   → 搜索 API + 事实接地
                                    ④ 云端对话       → DeepSeek
                                            ↓
                                    matcha TTS(CPU) → 功放播放

[显示] 表情脸 + 中英双语实时字幕 → DL7400 多屏
       可语音切换某块屏显示摄像头实况
```

### 4.2 端侧模型性能实测

| 环节 | 模型 | 实测指标 | 位置 |
|---|---|---|---|
| 人体检测 | mobilenet224_full1 | **25 ms/帧** | **NPU** |
| 语音断句 | silero VAD | 实时 | CPU |
| 语音识别 | SenseVoice **int8** | **RTF 0.197**（4 线程）= **5 倍实时余量** | CPU |
| 语音合成 | matcha + vocos | **RTF 0.37**（热合成） | CPU |
| 本地兜底 LLM | Qwen2.5-0.5B Q8_0 | 19.6 tok/s | CPU |
| 云端对话 | DeepSeek | — | 云端 |

**ASR 线程扩展性**：1 线程 RTF 0.626 → 2 线程 0.338 → **4 线程 0.197**（接近线性）。

**量化收益（SenseVoice fp32 → int8）**：速度 **快 1.45×**，内存 **省 3.3×**（1287 MB → 393 MB）。

**TTS 选型迭代**：melo RTF 2.05（不可用）→ matcha RTF 0.37，**提速 5.5 倍**。

### 4.3 端到端响应时间

| 场景 | 实测 |
|---|---|
| 本地指令（时间/音量/切屏） | **即时**（零云端往返） |
| 云端对话一轮 | **5 ~ 12 秒**（视回答长度） |
| 联网实时问题（如天气） | **约 5 秒**（搜索 2.3 s ＋ LLM） |
| TTS 首句发声 | 流式播报，**首句约 1 秒**（不等整段生成） |

### 4.4 已实现功能清单

| 功能 | 状态 | 实测示例 |
|---|---|---|
| 中英双语语音识别 | ✅ | SenseVoice 中英混合 |
| 云端智能对话 | ✅ | 「你是谁」→「我是 Astra 语音助手」 |
| **联网实时数据** | ✅ | 「今天广州天气」→「多云到晴，26℃ 到 35℃」**真实数据** |
| 本地设备查询 | ✅ | 「现在几点」→ 真实系统时钟 |
| 语音控制音量 | ✅ | 「音量调到 20%」→ 实际生效 |
| 语音切换屏幕内容 | ✅ | 「打开摄像头」→ 指定屏切实况画面 |
| 视觉唤醒 | ✅ | 人走近自动播欢迎语 |
| **中英双语实时字幕** | ✅ | 屏上「你说 / Astra」两行中英对照 |
| 断网兜底 | ✅ | 自动回落本地 0.5B 模型 |
| 断电重启全恢复 | ✅ | 含一次非计划断电验证 |

### 4.5 幻觉抑制（技术亮点）

实测发现：某国产大模型的"联网搜索"工具**并不真正工作** ——
返回搜索结果 0 条，但模型**照样编造出一个气温**（编 15~25℃ / 18~28℃，真实 28~36℃，
且两个模型互相矛盾）。

**我们的做法**：改调独立搜索 API 拿到真实网页内容，再配"只根据这些资料回答，
绝对不许编造数字"的接地提示词。搜不到时，天气/股价这类**硬实时问题直接说查不到，不回落**。

**效果**：实时数据问答**零编造**。

---

## 五 · 资源占用

| 场景 | CPU（4 核合计，100% = 单核满载） |
|---|---|
| 优化前（3 屏 + 语音待机） | 52.5% |
| **优化后**（同配置） | **21.7%** |
| 当前（2×4K + 1×2880×1800 三屏） | dl_face 167% + astra_voice 110% |

**优化手段**：显示刷新率 15 fps → 5 fps（像素搬运 311 → 104 MB/s）；
视觉检测循环加节流（背靠背连跑 → 0.5 s 间隔）。

> 三块屏含两块 4K 时像素搬运约 436 MB/s，CPU 占用相应升高 —— **屏数和分辨率是主要成本**。

---

## 六 · 工程交付

| 交付物 | 内容 |
|---|---|
| **Yocto layer `meta-dlsdk`** | 一层搞定：DLSDK + 依赖 + 应用 + 服务 + 字体 |
| **完整可烧录镜像** | 2.5 GB，烧完开机即用（另需灌模型 + 填 API key） |
| 显示应用 | `dl_face`（表情+字幕）/ `dl_player`（视频）/ `dl_clock`（时钟）/ `dl_edid`（诊断） |
| 语音应用 | `astra_voice`（主程序）+ `astra_llm.py`（四层路由）+ `astra_translate.py`（双语字幕） |
| 文档 | 项目总结、烧录说明、备份恢复手册、DisplayLink 问题报告（中英双语） |

**构建方式**：`bitbake astra-media` 即产出含全部功能的镜像，沿用原厂烧录流程。

---

## 七 · 关键技术结论（供内部复用）

### 7.1 DL7400 集成

1. **DLSDK 是纯用户态 libusb**，无需内核模块，`meta-dlsdk` 直接编译即可
2. **必须 USB 3.0** —— USB 2.0 下 SDK 直接拒绝驱动
3. **绝不能盲信 `preferred_mode`** —— 某显示器 EDID 报 4K@160Hz 为首选，
   DL7400 喂不动，推屏**永久阻塞**。必须钳制 ≤60 Hz
4. **DLSDK 单进程独占** —— 一个进程握着设备，第二个进程枚举到 0 个
5. **不能把桌面扩展到这些屏** —— DLSDK 是"推像素"API，没有 DRM/KMS 节点，
   必须自己写程序渲染。对内容墙场景反而更可控

### 7.2 判断"屏到底通没通"要看帧数，不能靠肉眼

| 现象 | 含义 | 解法 |
|---|---|---|
| 帧数**冻结** | 链路卡死 | 降刷新率 / USB 深度复位 |
| 帧数**在涨但屏黑** | 链路正常，显示器输入源没切对 | 按屏上 OSD 切 Input Source |

**两者原因和解法完全不同，混为一谈会浪费大量时间。**

### 7.3 语音侧

1. **NPU 只对视觉有用** —— 语音链路全在 CPU（Synaptics 自己的语音示例也是）
2. **量化收益因模型而异**，必须逐个实测（SenseVoice int8 快 1.45×，但 VITS int8 反而慢 1.9×）
3. **纯能量门槛无法区分人声和环境噪音** —— 实测办公室底噪 RMS 575~713，
   人声 470~1170，**两者重叠**。远场场景根治方案是唤醒词

---

## 八 · 已知限制与后续计划

| # | 问题 | 影响 | 计划 |
|---|---|---|---|
| 1 | **环境噪音误触发** | 嘈杂环境下需靠近麦（30 cm 内） | 接入 KWS 唤醒词（素材已就绪） |
| 2 | **显示器断电后不自动重亮** | 需语音指令或重启服务恢复 | 已提问题报告给 DisplayLink |
| 3 | **无回声消除（AEC）** | 播报时靠丢采集帧规避 | 待评估 |
| 4 | **板载麦克风驱动问题** | 现用 USB 麦替代 | 需 Synaptics FAE 支持 |
| 5 | **TTS 授权** | espeak-ng 为 GPLv3，穿透整条链路 | **产品化前必须解决** |
| 6 | 新镜像未整板实烧验证 | — | 待执行 |

### DL7400 断电不自愈问题（已提报厂商）

实测发现 SDK 三个层面的问题，已写中英双语报告：

1. 热插拔回调注册**恒失败**，显示器接入/移除事件从未送达
2. EDID / 首选模式 / 显示器数量**全是缓存**，屏断电了照样返回成功
3. 唯一能反映现实的 AUX 读取接口，**在并发推帧时会让该设备所有输出一起黑屏**

**结论**：SDK 层面目前没有安全的在线检测手段。

---

## 九 · 商业化定位建议

**适合**：
- 多屏数字标牌 / 商场导购屏 / 展厅内容墙
- 语音交互终端（前台接待、导览、智能看护）
- 低变化内容的多屏仪表盘 / 监控看板

**不适合**：
- 视频墙 / 多路视频监控回放（帧率不够）
- 需要桌面扩展的办公场景（DLSDK 不提供 DRM/KMS）

**成本优势**：一颗 SL1680 + 一个 DL7400 即可驱动 4 屏，
且语音 AI 全部端侧运行（仅对话大脑走云端），无需 GPU 或额外 PC。
