# SL1680 故障排查实录 · 2026-07-28

**一天之内定位并修复 9 个真 bug**，全部有实测证据、已回灌仓库（v2.8，tag 已推 GitHub）。
本文按"现象 → 排查过程 → 真根因 → 修复 → 教训"记录，供以后遇到同类症状时直接查。

---

## 速查表

| # | 现象 | 真根因 | 一句话修法 |
|---|---|---|---|
| 1 | Astra 完全听不见 | C920 USB 音频端点 EPROTO(-71)，软件复位救不回 | **物理拔插** USB |
| 2 | 语音识别准确率骤降 | 采到的音频**削顶**（增益 +38dB，峰值打满 32767） | 麦增益降到 +29dB |
| 3 | "打开摄像头"不响应 | ASR 丢字成"开摄像头"，规则表无此说法 | 补同义词 + 裸动词 |
| 4 | 语音调音量无效，却回"已调到最大" | **中文数字**"百分之百"没解析 → 落云端 → **云端假装执行** | 加中文数字解析 |
| 5 | 音量总是自动变小 | 服务 ExecStartPre 每次启动强制设 30% | 默认值改 90% |
| 6 | 新 4K 屏不亮 | 首选模式 3840x2160@**160Hz** + SDK EDID 缓存 | 重启 dl-face 重新枚举 |
| 7 | 天气查不到（第一层） | **DNS 被路由 Fake-IP 劫持**（198.18.0.0/15 不可路由） | 固定公共 DNS |
| 8 | 天气仍查不到（真根因） | **astra_voice.c 内置规则截胡**，astra_llm.py 根本没被调用 | 删除过时兜底规则 |
| 9 | 天气答"我市"含糊 | 搜索查询**没带城市** | 加城市定位 + Open-Meteo |

---

## 1 · 麦克风完全失聪

**现象**：语音助手不响应，systemd 每 3 秒重拉一次，每次白烧 12.5 秒加载模型。

**排查**：`arecord` 直接录音也失败 → 不是应用问题。dmesg 实锤：

```
usb 1-1.4: 3:1: usb_set_interface failed (-71)   ← 从开机 53 秒起反复出现
usb 1-1.4: can't set config #1, error -71        ← 最终整个设备配置失败
```

`-71` = EPROTO，USB 协议级错误。视频接口还活着（/dev/video* 在），只有音频接口死了。

**尝试过且无效**：`unbind/bind`、`authorized` 强制重配置、端口级断电（hub 不支持）。

**根因**：USB 链路层状态错乱。**软件复位一律救不回，只有物理重新上电**。

> **教训**：USB EPROTO 级故障，kernel 的所有软复位手段都无效。判据是 dmesg 里的 `-71`；见到就直接拔插，别浪费时间试软件手段。
> 另：C920 的音频是**等时（isochronous）端点**，总线紧张时最先饿死——它从开机 53 秒就在报错，我的 unbind 只是把它推下了悬崖。

---

## 2 · 语音识别准确率骤降

**现象**："打开摄像头"被听成"呃摄像头"、"听国"，音量指令也识别不稳。

**排查**：分析板上存档的语音段（`/home/voice/segs/*.wav`）：

```
seg_004.wav  RMS=16932  peak=32767  削顶 2717 个采样点  ★
seg_005.wav  RMS=14390  peak=32767  削顶 2115 个采样点  ★
seg_006.wav  RMS=15546  peak=32767  削顶 3048 个采样点  ★
```

**根因**：麦克风增益 48/60 = **+38dB**，近距离说话直接打爆 16bit 满量程。削顶波形破坏高频信息，对声学模型是毁灭性的——典型表现就是**吞字**（"打开"→"开"）。

**修复**：增益降到 30/60（+29dB）；服务文件 `sset Mic 80%` 改 `50%`（否则重启复原）。

> **教训**：ASR 变差先看**波形**不要先怀疑模型。存档语音段这个设计救了命——`--save-dir` 值得一直开着。

---

## 3 · 摄像头语音指令不响应

**根因**：ASR 输出"开摄像头"（丢了"打"），规则表只认"打开摄像头"，落到云端 LLM 后回答"我没有摄像头功能"。

**修复**：短指令表补 `开摄像头/开监控/开启摄像头/打开摄像机/看摄像头`；动词表加裸 `开`/`关`（必须与"摄像头/监控"名词同现才生效，误触风险可控）。

---

## 4 · 音量指令无效但回复"已调到最大"⭐

**现象**：说"音量调到百分之百"，回答"好的，音量已经调到最大了"，**实际纹丝不动**。

**排查**：读 `try_volume()` 代码：

```python
m = re.search(r"(\d+)", text)     # ← 只认阿拉伯数字
```

ASR 输出的是「音量调到**百分之百**」——**全是汉字，没有一个阿拉伯数字**。匹配失败 → 返回 None → 静默落到云端 LLM → **LLM 张口就来"已经调到最大了"，什么都没做**。

**修复**：加中文数字解析（`百分之百`/`百分之五十`/`调到八十`/`最大`/`最小`）。

**实测验证**：
```
[你 ] 音量调到百分之百。
[机器] 好的，音量调到100%。      ← 真执行了
```

> **教训（最阴险的一类）**：本地规则没命中时**静默回落云端**，而云端 LLM 会**一本正经地假装执行**。系统日志一切正常，用户却发现没生效。这和"echo 已装其实没装""脚本无条件 exit 0"是同一种病：**报告与事实不符**。

---

## 5 · 音量总是自动变小

**根因**：服务文件第 18 行

```
ExecStartPre=-/usr/bin/astra_setvol.sh 30%    ← 每次服务启动强制降到 30%
```

我为了改配置重启了几次服务，每次都把音量拽回 30%——用户体验就是"音量又自己变小了"。

**修复**：默认值改 90%；`astra_setvol.sh` 同时诚实化（回读核对，失败非零退出；旧版无条件 `exit 0`，失败也报成功）。

---

## 6 · 新换的 4K 屏不亮

**现象**：换了一块 4K 显示器接 DL7400，不点亮。

**排查**：`dl_edid` 读 EDID（DLSDK 单进程独占，须先停 dl-face）：

```
显示器名: Mi Monitor
首选模式: 3840x2160 @160Hz     ← 教科书级复现我们记录过的陷阱
支持模式: 26 个（含 @60Hz）
```

但 dl_face 日志显示它**正确钳到了 60Hz**。真正的问题是：

```
[dl_face] 热插拔回调注册: 失败 (status=2, handle=NULL)
```

**根因**：DLSDK 三缺陷之一——**热插拔回调恒失败 + EDID/显示器列表全是缓存**。dl_face 在 12:06 枚举了一次就再没更新，你之后换的屏它根本不知道。

**修复**：重启 dl-face 重新枚举，两块屏立刻按 @60Hz 点亮。语音"重新检测屏幕"是同样效果。

---

## 7 · 天气查不到（第一层：DNS）

**排查**：

```
nslookup open.bigmodel.cn  →  198.18.26.109
```

`198.18.0.0/15` 是 **RFC 2544 保留的基准测试网段**，不是真实公网 IP——这是代理软件（Surge/Clash 类）的 "Fake IP" 特征。板子的 DNS 是 `198.18.0.2`（家庭路由下发），但板子本身不走代理，于是**解析到不可路由的假地址，所有云端 API 超时**。

**修复**：`/etc/systemd/resolved.conf.d/99-astra-dns.conf` 固定公共 DNS（223.5.5.5 / 119.29.29.29），已做成 recipe 进镜像。

> **教训**：板子换网络环境后**先 `nslookup`**。看到 198.18.x.x 就是这个坑。这类故障症状是"超时"，极易误判成 API key 失效或服务挂了。

---

## 8 · 天气仍查不到——真根因 ⭐⭐⭐

DNS 修好后，**手动跑脚本成功（1-2 秒返回真实天气），服务里依然回"我现在没有联网"**。

### 排查绕了六轮，因为每一层都在说谎

| 轮次 | 假设 | 结果 |
|---|---|---|
| 1 | 网络不通 | ping 正常 |
| 2 | DNS 假 IP | **真问题，修了，但不是主因** |
| 3 | 搜索 API 失败 | 我的测试**没带环境变量**——测试本身有缺陷 |
| 4 | astra_voice 子进程超时 | 猜的，实测搜索只要 3 秒，不成立 |
| 5 | `strings` 搜二进制找不到那句话 | **strings 默认只认 ASCII，中文根本搜不出来** |
| 6 | **加调用日志** | **日志文件为空 = astra_llm.py 根本没被调用** |

### 破局的两个动作

**① 给 astra_llm.py 加调用日志**（因为 astra_voice 调它时写死了 `2>/dev/null`，把所有诊断信息扔掉了）：

```
13:11:44  IN      --stream 今天天气怎么样？
13:11:45  OUT-WX  广州现在雷阵雨，28度，体感34度，湿度88%……
```

日志为空就是铁证——不是调用失败，是**压根没调用**。

**② 用 `grep -a` 而不是 `strings` 搜二进制**：

```
$ grep -aoE "我现在没有联网[^\"]{0,30}" /usr/bin/astra_voice
我现在没有联网，查不到实时天气，抱歉。      ← ★ 内置规则，实锤
```

### 真根因

`astra_voice.c` 的 `factual_reply()` 里：

```c
// 天气 —— 无数据源，诚实说不知道，绝不编（这正是之前"晴朗气温适宜"幻觉的根源）
if (strstr(heard, "天气") || strstr(heard, "气温") || ...) {
    snprintf(out, cap, "我现在没有联网，查不到实时天气，抱歉。");
    return 1;      // ← 直接返回，cloud_reply 永远轮不到
}
```

而调用顺序是：

```c
if (factual_reply(...)) return;    // ① 天气在这里就被截了
if (cloud_reply(...)) return;      // ② 联网能力在这里，永远执行不到
```

> ### ⭐ 最贵的教训
>
> **这条规则写的时候是完全正确的**——当时确实没有天气数据源，写它就是为了防止模型编造"晴朗气温适宜"这种幻觉。
>
> 但**能力补齐之后没人回来删它**。于是当年的诚实变成了今天的谎言，而且遮蔽得毫无痕迹：astra_llm.py 连调用都不会被调到，日志、错误码、返回值全都正常。
>
> **规矩**：实现"没有能力时的诚实兜底"时，就要同时在代码里留下"能力补齐后必须删除此处"的标记。

**修复**：删掉该规则（源码里留了长注释解释来龙去脉），重建 astra_voice 部署上板。

### 顺带发现的部署坑

板上原 `astra_voice` 是 **34MB 静态链接**的老产物，而 recipe 现在产出 **67KB 动态链接**版——板子 rootfs 里**根本没有 sherpa 的 .so**（那是旧镜像，早于 sherpa recipe）。

处理：把 sherpa 库装到 `/usr/lib/sherpa` + `ld.so.conf.d` 注册 + service 加 `LD_LIBRARY_PATH` 三重保险。**新镜像自带 sherpa-onnx 包，无此问题。**

---

## 9 · 天气回答含糊 → 接入 Open-Meteo

**现象**：即使查到了，回答是"今天我市大部分地区晴到多云"——**"我市"是哪个市？**

**根因**：`web_search(text)` 把用户原话直接当搜索词，"今天天气怎么样"里**没有城市**，抓回来的是随机某地新闻稿。

**修复（两步）**：

**① 城市定位**：内置 30 个常用城市，用户说了城市就用他说的，没说就用 `ASTRA_CITY` 默认值。

**② 换掉整条数据链路——用 Open-Meteo 气象 API 直答**：

| | 旧：搜索 + LLM 总结 | 新：Open-Meteo |
|---|---|---|
| 耗时 | 5-7 秒（**超时被判定云端失败**） | **1-2 秒** |
| 数据 | 网页新闻稿，可能是几天前的 | 官方气象，实时 |
| 编造风险 | 模型从行文里抠数字 | **零**——结构化 JSON 套模板 |
| 成本 | 搜索 ¥0.01 + LLM token | **免费，不需要 API key** |
| 能力 | 只能"今天怎么样" | 今天/明天、体感、湿度、降雨概率、自动提醒带伞 |

实现要点：内置城市经纬度表（零网络查表）→ 查不到再走 Open-Meteo 地理编码 → WMO 天气代码映射中文 → 套模板生成口语句子，**全程不经过大模型**。

**实测**：
```
今天天气怎么样  → 广州现在雷阵雨，28度，体感34度，湿度88%，今天25到30度，降雨概率100%，记得带伞。 [1秒]
深圳今天天气    → 深圳现在雷阵雨，28度，体感34度，湿度81%，今天25到30度，降雨概率86%，记得带伞。
明天天气如何    → 广州明天雷阵雨伴冰雹，气温25到30度，降雨概率100%，记得带伞。
北京冷不冷      → 北京现在小雨，34度，体感39度，湿度52%，今天26到35度，降雨概率41%，记得带伞。
```

---

## 贯穿全天的三条元教训

### ① 系统会说谎，而且是"正常地"说谎

今天有 **4 个 bug** 属于"报告与事实不符"：

- 云端 LLM 假装执行了音量指令（#4）
- `astra_setvol.sh` 无条件 `exit 0`，失败也报成功（#5）
- 内置规则说"没有联网"，其实网络好好的（#8）
- 我自己的测试没带环境变量却当成"API 失败"的证据（#8 第 3 轮）

**这类 bug 最贵**，因为日志、返回码、监控全是绿的。**唯一的破法是拿到执行现场**——加调用日志、回读核对、字节级校验。

### ② 诊断信息不能扔

`astra_voice` 调 LLM 时写死 `2>/dev/null`，把 stderr 全丢了。查天气问题时 `[search] failed: xxx` 这种关键线索完全看不见，我只能一层层手工复现，多花了至少一小时。

**改进**：已给 astra_llm.py 加常驻调用日志（`/tmp/astra_llm_calls.log`）。

### ③ 工具本身也会骗你

`strings` 默认只提取 ASCII 字符串，**中文一个都搜不到**。我因此在第 5 轮得出"二进制里没有这句话，所以是模型生成的"这个**错误结论**，直接导致后面几轮方向跑偏。

**正确姿势**：搜二进制里的中文用 `grep -a`，不要用 `strings`。

---

## 本次改动清单（已全部回灌 v2.8）

| 文件 | 改动 |
|---|---|
| `astra_voice.c` | 删除天气拦截规则（附长注释说明来龙去脉） |
| `astra_llm.py` | Open-Meteo 天气直答 / 中文数字 / 摄像头同义词 / 城市定位 / 提示词 / 调用日志 |
| `astra-voice.service` | 麦增益 80%→50%、音量默认 30%→90%、加 `LD_LIBRARY_PATH` |
| `astra_setvol.sh` | 诚实化：回读核对，失败非零退出 |
| `99-astra-dns.conf` | 固定公共 DNS（做成 systemd-conf.bbappend 进镜像） |
| `tzdata.bbappend` 等 | rootfs 三件套进 Yocto（详见 VERSION v2.8） |

GitHub：`hardyhuang818/sl1680-astra-terminal` @ tag **v2.8**
