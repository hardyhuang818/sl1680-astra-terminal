# SL1680 + DL7400 多屏扩展 · 完整总结

> 2026-07-17 · 全部结论均来自 **DL7400 EVK 真机实测**（非纸面推导）
> 硬件：SL1680 (Dolphin RDK) + DL7400 EVK (Redwood-8Gb) + 4 台显示器

---

## 0. 一句话结论

**✅ 4 个屏实测跑通**（2880x1800 + 4K + 1080p + 4K 混合分辨率同时点亮），**不需要写任何内核驱动**。
**❌ 但别人拿默认 SL1680 镜像 + DL7400 EVK 插上去，什么都不会发生** —— 见 §2。

---

## 1. 实测结果（四屏并发）

DL7400 的 **4 个输出口全部占满**，四路帧数持续增长，无一卡死：

| 屏 | 型号 | 厂商 | 分辨率 | 端口 | 帧率 |
|---|---|---|---|---|---|
| 0 | Sculptor（便携屏，HDMI） | YCT 0xAC00 | 2880×1800 @60 | `^0` | **8.7 fps** |
| 1 | Redmi 27 NU | XMI 0xB005 | 3840×2160 @60 | `^1` | **5.8 fps** |
| 2 | S24D390（三星） | SAM 0x0B65 | 1920×1080 @60 | `^2` | **15.0 fps** 跑满 |
| 3 | Mi Monitor | XMI 0x27B4 | 3840×2160 @60 | `^3` | **5.7 fps** |

**带宽分摊观察**：三屏→四屏后，两个 4K 从 7.2 → 5.8 fps（掉 ~20%），1080p 始终跑满 15fps。
USB3(5Gbps) 总吞吐基本恒定，新增屏摊薄每路份额。

### 性能定位：多屏内容展示，不是视频墙

| 用途 | 可行性 |
|---|---|
| 数字标牌 / 看板 / 时钟 / 仪表盘（低变化内容） | ✅ **很好**，DL3+ 只压变化区域 |
| 全屏流畅视频 | ❌ **7.4 fps**（4K VP9→1080p 实测），不可用 |

> ⚠️ 早期用"合成测试图"测出的 60fps 是**误导性数据** —— 那图只有一根竖条在动，
> DisplayLink 几乎不用传数据。真实视频每帧全变，只有 1/8。**别用它做规划。**

---

## 2. ★别人能直接用吗？—— 不能，缺三样★

**默认 astra-media 镜像 + DL7400 EVK 插上去 = 什么都不会发生。**
系统不会多出 4 个显示器，`weston` 桌面也扩展不过去。

| 缺什么 | 说明 |
|---|---|
| **`libusb`** | 默认镜像**没有**（实测 `find / -name "libusb*"` 为空）。这是 DLSDK 唯一的外部依赖 |
| **DLSDK** | 不在 Yocto 里，是 Synaptics 单独发布的 zip（需 NDA）。要自己打包进镜像 |
| **应用程序** | **DLSDK 是"推像素"API，不是显卡驱动** —— 没有 DRM/KMS 节点。必须自己写程序渲染并调 `display_show()`，否则屏永远黑 |

### 关键架构认知

```c
dlsdk_display_show(display, DLSDK_PIXEL_FORMAT_XRGB, pixels, stride);
                                                      ↑
                                          你自己准备好的像素 buffer
```
- ❌ **不能**把 Weston/Wayland 桌面扩展到这些屏（那需要 evdi 内核模块，且只支持 2 屏）
- ✅ 每个屏画什么，**由你的应用完全掌控** —— 对多屏内容墙反而正合适
- **程序退出 = 释放显示 = 屏幕变黑**（设计如此）。生产环境必须做成常驻服务

---

## 3. ★本 Case 的价值：趟平了 6 个会卡住好几天的坑★

价值**不在"证明能接"**（datasheet 早写了 4 口），而在**这些只有真机才会暴露、且极难自行定位的问题**：

### 坑① 绝不能盲信 `dlsdk_display_preferred_mode()` ← 最坑的一个

Mi Monitor 的 EDID 把 **3840x2160@160Hz** 报成"首选模式"。DL7400 + USB3(5Gbps) **根本喂不动** → `wait_on_show` **永久阻塞**（确定性死卡：7 帧 / 18 帧，每次一模一样）。

**为什么极难排查**：EDID 是**显示器的属性**，所以故障会**"跟着屏走"** —— 换线、换端口、换到另一台机器，问题都跟着那台屏跑。**这会让人 100% 误判成"这台屏坏了"或"这根线不行"。**

> 📌 **我自己就误判了**：一度断言"Mi Monitor 的 HDMI 有问题"，还换过 4K HDMI 线（没用）、对调过端口。
> 直到限制 60Hz 后瞬间正常 —— 同一台屏、同一根线、同一个口。**HDMI 从来就没问题。**

**修复**（`pick_mode()`）：从 `dlsdk_display_modes()` 里挑**「与 preferred 同分辨率、刷新率 ≤ 60Hz 的最高刷新率」**，找不到才退回 preferred。

### 坑② 进程被硬杀 → 设备卡死，且只有一种复位有效

没干净 `teardown` 就被杀 → `dlsdk_get_devices()` 一直返回 **0 个设备**。

| 复位方式 | 效果 |
|---|---|
| `echo 0/1 > authorized` | ❌ **救不回来** |
| **`usb` 驱动 unbind/bind** | ✅ **能救回来** |

```bash
echo -n "2-1.4.1" > /sys/bus/usb/drivers/usb/unbind; sleep 3
echo -n "2-1.4.1" > /sys/bus/usb/drivers/usb/bind
```
> 不知道这条，会以为 EVK 坏了。设备路径会变（实测 2-1.4.1 → 2-1.2.1），所以复位脚本要**按 VID 17e9 动态查找**。

### 坑③ 同一台屏别同时接 DP + HDMI

会被枚举成**两个独立 display（EDID 完全相同）**，其中一个还报出**虚假的 160Hz**，同时驱动互相打架。**一台屏只接一根线。**

### 坑④ `auto-resize` 默认开启 → 字体被放大 3 倍

overlay 以 640×480 为基准自动缩放：1920 宽 → **×3**。写 216pt 实际渲染成 648pt，**直接撑爆屏幕**。
→ 必须 `auto-resize=false`，此后 `px = pt × 96/72`。

### 坑⑤ 镜像里没有任何 CJK 字体

`fc-list :lang=zh` 为空，只有 Liberation。中文被 Pango 渲染成 **Unicode 码点方框**（591A=多、5C4F=屏…），**看起来就是乱码**。
→ 加 `ttf-wqy-zenhei`（~14MB，meta-oe 现成）。

### 坑⑥ 软件缩放白吃 94% 的时间

4K→1080p 实测（150 帧）：

| 管线 | 耗时 | 帧率 |
|---|---|---|
| 纯硬解 4K VP9 | 1.35s | **111 fps** ← 解码毫无压力 |
| + 软件 `videoconvert/videoscale` | **17.55s** | **8.5 fps** ← ❌ 浪费 94% |
| + 硬件 `synavideoconvertscale` | 5.63s | **31.9 fps** ← ✅ 快 3.7 倍 |

> 注意：`synavideoconvertscale` 只输出 RGB/BGRA/NV12/I420/YUY2 —— **没有 BGRx**，用 BGRA（32bpp 布局与 `DLSDK_PIXEL_FORMAT_XRGB` 一致）。

---

## 4. ★方法论：判断"屏到底通没通"要看帧数，不能靠肉眼★

这是本 case 最有价值的一条经验。`dl_clock` 每 60s 打印各屏帧数：

| 现象 | 含义 | 解法 |
|---|---|---|
| 帧数**冻结** | **链路卡死** | 降 Hz（坑①）/ USB 深度复位（坑②） |
| 帧数**在涨但屏黑** | 链路正常，**显示器输入源没切对** | 按屏上 OSD 切 Input Source |

**两者原因和解法完全不同，混为一谈会浪费大量时间。**

> 另一个坑：看日志要**认准 PID** —— 卡死的旧进程会留下误导性统计（我被坑过一次）。
> 还有：**屏数和模式号会变**（实测 2→3→4），动线后必须重新 `dl_edid`，否则模式号对不上。

---

## 5. 交付物（可直接复用）

| 产出 | 位置 | 作用 |
|---|---|---|
| **`meta-dlsdk`** Yocto 层 | `Case6_Astra/meta-dlsdk/` | 一层搞定：SDK + 依赖 + 服务 + 字体 |
| ├ `dlsdk_1.5.0.bb` | | 打包 aarch64 `libdlsdk.so` + 头文件 + 固件 spkg + udev 规则(VID 17e9) |
| ├ `dl-clock` | | **时钟+跑马灯，开机自启，多屏，每屏显示自己的 EDID 身份** |
| ├ `dl-player` | | GStreamer 硬解 → 硬件缩放 → 推屏（视频/图片/测试图） |
| ├ `dl-edid` | | 读各口 EDID + 全部模式（排查"无信号"必备） |
| └ `dl-multiscreen-demo` | | 枚举/点亮/实测 fps |
| 镜像集成 | `astra-media.bbappend` | `IMAGE_INSTALL` 含 dlsdk/播放器/时钟/**libusb1**/**ttf-wqy-zenhei** |

**烧完即用**：重编镜像后开机自动点亮所有接上的屏。

### 快速上手

```bash
# 所有屏显示时钟(默认限 60Hz，每屏显示自己的 EDID 身份)
dl_clock --wait --fps 15 --speed 15 --max-hz 60

# 播放视频(硬解+硬件缩放)
dl_player video.mp4

# 排查"无信号"：读 EDID 和支持的模式
dl_edid

# 设备卡死时复位(按 VID 动态查找，不怕换 USB 口)
/usr/bin/dl-clock-reset.sh
```

---

## 6. 硬件前提（缺一不可）

| 项 | 要求 | 实测 |
|---|---|---|
| **USB 3.0** | **必需**。USB2 下 SDK 直接不驱动 | EVK 实测 `speed=5000` SuperSpeed ✅ |
| 固件 | 12.3.26（SDK 配套） | **SDK 自动升级**：`cfg.firmwarePath` 指向 spkg 目录即可，无需手动刷 |
| glibc | ≥ 2.30 | 板子 2.39 ✅ |
| DRAM | 8Gb 支持三屏(5K2K+4K+4K)、四屏(4×4K) | Datasheet Table 3 |

> ⚠️ 早期用**零售 Anker 坞**测试时，经 USB-C→USB-A 线只协商到 **USB 2.0**（多数 C-to-A 线只有 USB2），
> SDK 拒绝驱动 → 白折腾很久。**EVK 是原生 USB3 上行，没这问题。**

---

## 7. 扩展性

- **一颗 DL7400 = 4 屏**（已全部验证）
- **多颗可同时挂**：`dlsdk_get_devices()` 支持枚举多颗（C++ 封装自动扩容），无设备数硬上限（唯一上限 255 surface）
- → 更多屏 = **N 颗 × 4 屏**，天花板是 **USB3 带宽 + A73 CPU 跑 DL3+ 压缩**，不是 SDK
