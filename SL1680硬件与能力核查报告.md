# SL1680 硬件与能力核查报告 —— 代码 + 原理图全面盘点

> 日期：2026-07-19　板子：Synaptics Astra SL1680 @ 192.168.5.126
>
> **本次新读的一手材料**：RDK 载板原理图 SC950-000798-01 RevF 全 14 页｜MIPI-DSI LCM 连接指南 RevD 全 10 页｜DL7400 原理图 NR-152973 全 21 页｜Kbridge2/TD7800 图纸｜LAMTL(TC358775) 转接板图纸+datasheet｜`astra_voice.c`/`dlsdk.h`/meta-dlsdk/delivery_sl1680 源码与交付物
>
> **证据分级**：🟢**实测**＝板上跑出来的｜🔵**已核实**＝一手原理图/源码｜🟡**理论**＝datasheet 推得｜🔴**受阻**＝有硬伤未解
>
> 回答一个问题：**SL1680 到底能做什么？**

---

## 一、原理图告诉我们的硬件底子 🔵

### 1.1 关键架构：载板 + 核心板

RDK 是"载板 + 核心板"两层：**SoC / DRAM / eMMC 全在核心板上**，经 260-pin（DDR4-SODIMM 形态）金手指 J29 插到载板。载板兼容 SL1680/1640/1620 三款核心板，部分引脚因型号而异。载板暴露什么接口，SL1680 就有什么能力边界。

### 1.2 载板接口全清单（原理图页码可查）

| 接口 | 规格 | 页 | 意味着 |
|---|---|---|---|
| **HDMI 2.1** J12 | Type-A 母座，含 eARC/CEC/HPD | p5 | 主交互屏；4K60 + HDCP2.2 |
| **MIPI-DSI 4-lane** J208 | 22pin 0.5mm FPC，树莓派 DSI 兼容 | p7 | 第二块独立屏（与 HDMI 不能镜像）|
| **MIPI-CSI0 4-lane** J206 | 22pin FPC | p6 | 主摄像头 |
| **MIPI-CSI1 2-lane** J207 | 15pin，树莓派摄像头脚位 | p6 | **第二路摄像头**（看护双机位）|
| **GbE RJ45** J2 | SoC 内置 PHY + 带磁 RJ45 | p8 | 千兆有线 |
| **PoE 排针** J34 | 无源，磁中心抽头引出 | p8 | 加外置 PD 模块→一根网线供电+联网 |
| **USB3 ×4 Type-A** | GL3523 hub 分出（共享一路 SoC USB3）| p2/p10 | DL7400/摄像头/声卡都挂这 |
| **USB2 Type-C** J215 | TUSB320 DRP，host/device 双角色 | p11 | 烧录/调试口 |
| **M.2 Key-E** J17 | PCIe ×2 lane + SDIO + UART + I2S3 | p9 | WiFi/BT（默认 AP12676）；**板上唯一 PCIe 出口** |
| **40-pin 树莓派排针** J32 | I2C/UART0/PWM1/SPI2/I2S1/PDM×2 + GPIO | p4 | 传感器/继电器/扩展板生态；1.8V→3.3V 已转 |
| 风扇口 J13 | PWM + TACH | p12 | 产品化散热闭环 |
| JTAG J22 / QSPI J1 / 复位 SW6 / USB-Boot SW7 | | p12 | 调试与启动方式齐全 |
| **电源** | USB-C PD sink（5V/15V 跳线）→ 5V/6A + 3.3V/1.8V buck | p14 | 无单颗 PMIC，分立电源树 |
| **功耗测量** | INA220 ×2（0x40/0x41）在 I2C 上测 3.3V/1.8V 电流 | p14 | 可软件读实时功耗 |

### 1.3 两个此前没写进任何报告的硬结论 🔵

1. **载板没有音频 codec、没有麦克风阵列、没有功放、没有耳机孔**——只有数字 I2S×3 + PDM 排针。现在语音方案用 USB 声卡（C920 麦 + USB 耳机）不是将就，**是这块板唯一免焊接路径**；产品化要么加 I2S codec 小板，要么走 HDMI 音频。
2. **载板没有 SD 卡槽、没有 SoC 用的板载 flash**（唯一一颗 SPI-NOR 是 USB hub 的配置芯片）；系统只能在核心板 eMMC 上，外扩存储走 USB。

### 1.4 显示扩展硬件的真实形态 🔵

**J208 直连 DSI 屏**：22pin 里只有 3.3V，**无 5V、无背光 PWM 引脚**——5V 要从 40pin 排针 pin4/6 飞线（官方指南就是这么教的）；触摸 I2C 在 pin2/3，两根控制线来自 I2C GPIO 扩展器（不是 SoC 直出）。官方验证过的直连屏只有 Waveshare 7" 800×480。

**LVDS 屏要过桥**：SL1680 无原生 LVDS PHY。LAMTL 转接板用 Toshiba TC358775：DSI 4-lane@1Gbps 进，单链 LVDS 最高 1600×1200、双链最高 1920×1200@60（模块标称 1080p60）。⚠️ 模块输入是 30pin FPC，和 J208 的 22pin 不同，**需要换线序转接**——官方指南明确说"第二种接法不在本文范围"。

**DL7400（USB 多屏）**：DisplayLink "Navarro" 四头 USB 图形芯片，自带 LPDDR4x 私有帧缓存（Micron 16Gbit）。**每个口都是 DP(4-lane)+HDMI 二合一**，靠 DPx_CONFIG1 上拉选模式。上行是 USB3 Gen1 5Gbps 单 lane。**纯输出芯片，图纸上没有任何视频输入**——采集是 Kbridge2 的事。芯片还引出了 10G-Ethernet/RGMII/I2S（本卡未用）。手头这张图是 PCIe 卡形态（ASM3142 桥），对 SL1680 无关，走的是原生 USB3。

**Kbridge2/TD7800 子板**：TI SN65DSI85 的 MIPI-DSI→双链 LVDS 桥 + 触摸 + 背光——это是**单屏点亮/触摸固件烧录的工程台架**，不是多屏扩展器，别和 DL7400 混。

---

## 二、代码已实证的能力 🟢

### 2.1 全本地语音 AI（`astra_voice.c`，642 行常驻 C 程序）

```
ALSA采集 → Silero VAD → SenseVoice ASR(int8) → 四层回落 → matcha TTS → ALSA播放
                                                  │
                              事实接地 → 云Kimi → 本地Qwen0.5B → 规则
```

| 指标 | 实测值 |
|---|---|
| 整轮延迟 | ~5-6 秒 |
| SenseVoice int8 | RTF 0.197（4 线程，5 倍实时余量）|
| matcha TTS | RTF ~0.37（常驻热合成；比 melo 快 7 倍）|
| 本地 Qwen2.5-0.5B Q8 | 19.6 tok/s（断网兜底）|
| 模型加载 | 只在启动加载一次（~10s），干掉每轮 11.1s 冷启动 |

设计要点：SIGUSR1=视觉唤醒播欢迎语；SIGUSR2+`/tmp/astra_say.txt`=播报任意告警（看护复用）；状态文件原子写（tmp+rename）给字幕 UI 读；设备寻址用稳定名（CARD=C920 等），躲开 USB 重启换号。

### 2.2 NPU 视觉

- 人体检测 **24ms/帧**（置信度 0.94），常驻 GStreamer synapinfer 管线
- `vision_wake.sh`：人走近（框高≥45% 持续 N 帧）→ 欢迎语——**已部署为服务**
- `vision_care.sh`：滞留 / 区域无人 / 疑似跌倒（框宽>高）→ 语音告警——**demo 已跑通**
- 预装 YOLOv8 检测/姿态、posenet、mobilenet 等 12 个视觉模型

### 2.3 显示三路并存

| 路 | 实测状态 | 定位 |
|---|---|---|
| **原生 HDMI** | Weston 硬件合成 1080p 点亮；gst-ai 摄像头→NPU→HDMI 跑通；规格 4K60 | 主交互屏（丝滑满帧）|
| **DL7400 4 屏** | 2880×1800+4K+1080p+4K 混分辨率同亮；全动态视频仅 ~7fps | 低刷信息墙/看板 |
| **DSI/LVDS** | dtbo 已交付但开机不生效（见 §四）| 待打通 |

`dlsdk.h` 全 API 已核实：设备/屏枚举、EDID、模式设定、`dlsdk_display_show` 推像素、热插拔回调、DP AUX 透传——**纯用户态 libusb，无内核驱动**，scp 上板即用。`dl_face` 对话脸（颜文字+中文实时字幕，wqy-zenhei 字体）与 astra_voice 状态文件联动。

### 2.4 系统工程

- Yocto scarthgap / kernel 6.12.62，全量构建 9624 任务全过（~1h52m）
- eMMC A/B 双槽 GPT（rootfs_a/b 各 5120MB + home 4332MB），支持 .swu OTA
- 4 个 systemd 服务开机自启，上电 ~15s 就绪；凌晨 4 点自动清理语音留存

---

## 三、组合成产品方向（按就绪度排序）

| # | 方向 | 就绪度 | 差什么 |
|---|---|---|---|
| 🥇 | **智能看护/安防终端** | demo 已通 | 双 CSI 摄像头（硬件支持！）、事件录像、告警推送 |
| 🥈 | **多模态对话设备**（视觉唤醒+语音+脸+字幕）| 四件套全通 | 对话脸迁原生 HDMI、产品化封装 |
| 🥉 | **多屏信息墙/数字看板** | 4 屏已通 | 内容管理层；只适合低刷内容 |
| 4 | 边缘家居中枢 | astra_voice 就绪 | **板上无容器**，HA 要原生移植 |
| 5 | 客流/行为分析 | 模型预装 | 规则层（supervision 纯 Python 可直接用）|

---

## 四、产品化前必须面对的硬边界 🔴

| # | 边界 | 细节 |
|---|---|---|
| 1 | **NPU 只做视觉** | 语音/LLM 全跑 A73 CPU；SyNAP 固定 shape、无 KV cache；Synaptics 自家语音示例也全是 CPU |
| 2 | **A73 无 dotprod** | int8 收益逐模型实测：SenseVoice 快 1.45×，VITS 反而慢 1.9×，别照搬 |
| 3 | **TTS 授权全有雷** | espeak-ng GPLv3 静态穿透、matcha 模型无授权、melo 上游 AGPL；商用前必解 |
| 4 | **DL7400 带宽天花板** | USB3 5Gbps 共享 4 屏；全动态视频 ~7fps；USB2 下 SDK 直接拒开；`preferred_mode` 是陷阱（4K@160 EDID 永久卡死链路，必须钳 ≤60Hz）|
| 5 | **触摸/LVDS 集成缺口** | 两个 dtbo 编译进 /boot 但 **开机不应用**（FIT image 无 overlay 清单），触摸 probe -22；触摸 INT 还是占位符；MIPI→LVDS 桥型号未定（TC358775 需 I2C 初始化，现在 dtso 假设免配置桥）|
| 6 | **板上无 docker/podman** | Frigate（唯一原生吃 SyNAP 的成熟方案）、HomeAssistant 进不来，要加容器运行时或原生移植 |
| 7 | **无真唤醒词、无 AEC** | 现在"说话就应"+视觉唤醒；回声消除是土办法（播放时不采集）|
| 8 | 人脸识别整线 | 模型全非商用授权 + 无 SyNAP 后端，产品化避开 |
| 9 | 内存预算 | 4GB 物理 → 可用 ~2.9GB；语音栈 ~415MB，多屏 UI + 常驻 LLM 要精打细算 |

---

## 五、一句话总结

**SL1680 = 视觉 NPU 很强 + 4×A73 够跑本地语音 + 三路显示（HDMI 4K60 / DSI / USB 多屏）+ 双摄输入 + 完整 Yocto 工程链的边缘 AI 盒子。**

它的本命是**多模态对话终端**和**智能看护**；它做不了的是视频墙、云端级大模型、以及任何绕不开授权雷的 TTS/人脸方案。硬件上音频必须外挂（USB/I2S/HDMI），存储只有核心板 eMMC，PCIe 只留给了 WiFi。

*🟢 数字均为板上实测；🔵 结论均可回溯到图纸页码/代码行号（详见各分报告与 `wf_results.json`）。*
