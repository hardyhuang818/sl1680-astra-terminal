# TC358775 (DSI→LVDS 桥) 配置与调试记录

日期：2026-07-28 ｜ 载体：LAMTL 转接板（Neardi，芯片 Toshiba TC358775XBG）｜ 用途：SL1680 DSI → TM10.5-TD7800 LVDS 屏
状态：**已点亮并固化**。本文收录：完整初始化配置（逐寄存器注释）+ 调试全程的具体报错/读数原文 + 排错决策树。

---

## 1. 控制通道：怎么"够得着"这颗芯片

- **走 DSI 链路本身**（通用长写包，类型 0x29），**不用 I2C**——LAMTL 的 I2C 脚（J1.9/10，1.8V 域）全程未接
- 命令烤在 `dolphin-rdk.dts` 的 `dsi_panel/command` 属性里，**VPP 显示固件在每次 modeset 时以 LP 模式自动下发**（`dpi_lp_cmd=1`）
- 字节格式：`0x29 <payload长度> <寄存器地址LE16> <数据LE32>`；延时：`0xFF <微秒LE32>`
- 机制出处：BSP 自带的 `dolphin-ws-panel-overlay.dtbo`（树莓派屏内置 TC358762，同一手法），datasheet 亦确认"支持通用长包访问寄存器组"
- 上电默认状态（datasheet 原文）：**"Following power-on, the port is defaulted in sleep mode: All of the LVDS outputs are tri-stated"** ——不初始化必黑屏，LAMTL 板上无 MCU/EEPROM 代劳

## 2. 最终初始化序列（29 条，逐条注释）

数值来源：LAMTL 附带 `LAMTL-TC358775XBG-DSI-LVDS-timing.xls` "Code" 表（I2C 0x1E 版脚本）+ TC358775 功能规格书；时序寄存器按 TM10.5 的 1280×720@65.3MHz 重算。

| # | 命令字节（dts 原文） | 寄存器 | 值 | 含义 |
|---|---|---|---|---|
| 1 | `29 06 3C 01 05 00 03 00` | 0x013C PPI_TX_RX_TA | 0x00030005 | DSI LP 总线周转时序（TA_SURE=3/TA_GET=5） |
| 2 | `29 06 14 01 03 00 00 00` | 0x0114 PPI_LPTXTIMECNT | 3 | LP 发送时基 |
| 3 | `29 06 64 01 04 00 00 00` | 0x0164 PPI_D0S_CLRSIPOCOUNT | 4 | Lane0 HS 建立计数 |
| 4 | `29 06 68 01 04 00 00 00` | 0x0168 PPI_D1S_CLRSIPOCOUNT | 4 | Lane1 同上 |
| 5 | `29 06 6C 01 04 00 00 00` | 0x016C PPI_D2S_CLRSIPOCOUNT | 4 | Lane2 同上 |
| 6 | `29 06 70 01 04 00 00 00` | 0x0170 PPI_D3S_CLRSIPOCOUNT | 4 | Lane3 同上 |
| 7 | `29 06 34 01 1F 00 00 00` | 0x0134 PPI_LANEENABLE | 0x1F | PHY 使能：时钟+4 数据 lane |
| 8 | `29 06 10 02 1F 00 00 00` | 0x0210 DSI_LANEENABLE | 0x1F | DSI 协议层使能：同上 |
| 9 | `29 06 04 01 01 00 00 00` | 0x0104 STARTPPI | 1 | 启动 PPI |
| 10 | `29 06 04 02 01 00 00 00` | 0x0204 STARTDSI | 1 | 启动 DSI 接收 |
| 11 | `29 06 50 04 00 01 F0 03` | 0x0450 VPCTRL | 0x03F00100 | VSDelay=63（bit29:20）+ OPXLFMT=1 RGB888（bit8） |
| 12 | `29 06 54 04 1E 00 3C 00` | 0x0454 HTIM1 | 0x003C001E | HBP=60（高16位）｜HSW=30（低16位） |
| 13 | `29 06 58 04 00 05 61 00` | 0x0458 HTIM2 | 0x00610500 | HFP=97 ｜ HACT=1280 |
| 14 | `29 06 5C 04 02 00 0E 00` | 0x045C VTIM1 | 0x000E0002 | VBP=14 ｜ VSW=2 |
| 15 | `29 06 60 04 D0 02 07 00` | 0x0460 VTIM2 | 0x000702D0 | VFP=7 ｜ VACT=720 |
| 16 | `29 06 64 04 01 00 00 00` | 0x0464 VFUEN | 1 | 时序参数装载生效 |
| 17 | `29 06 A0 04 06 80 44 00` | 0x04A0 LVPHY0 | 0x00448006 | LVDS PLL 配置 + 复位位（bit22）拉起 |
| 18 | `FF 64 00 00 00` | — | 100µs | PLL 复位保持 |
| 19 | `29 06 A0 04 06 80 04 00` | 0x04A0 LVPHY0 | 0x00048006 | 释放 PLL 复位 |
| 20 | `29 06 04 05 04 00 00 00` | 0x0504 SYSRST | 4 | 复位 LCD 控制块 |
| 21 | `29 06 80 04 00 01 02 03` | 0x0480 LVMX0003 | 0x03020100 | 色彩映射（VESA，7 连发之一） |
| 22 | `29 06 84 04 04 07 05 08` | 0x0484 LVMX0407 | 0x08050704 | ↑ |
| 23 | `29 06 88 04 09 0A 0E 0F` | 0x0488 LVMX0811 | 0x0F0E0A09 | ↑ |
| 24 | `29 06 8C 04 0B 0C 0D 10` | 0x048C LVMX1215 | 0x100D0C0B | ↑ |
| 25 | `29 06 90 04 16 17 11 12` | 0x0490 LVMX1619 | 0x12111716 | ↑ |
| 26 | `29 06 94 04 13 14 15 1B` | 0x0494 LVMX2023 | 0x1B151413 | ↑ |
| 27 | `29 06 98 04 18 19 1A 06` | 0x0498 LVMX2427 | 0x061A1918 | ↑（七值与 datasheet p39 VESA 表逐字节一致；面板 NVM 实测也是 VESA） |
| 28 | `29 06 9C 04 41 00 00 00` | 0x049C **LVCFG** | **0x41** | **LVEN=1 + PCLKDIV=4，单链**。⚠️ 首版 0x31（PCLKDIV=3）= 全屏雪花根因 |
| 29 | `FF 10 27 00 00` | — | 10ms | 收尾稳定延时 |

### LVCFG 字段定义（datasheet 6.8.1，地址 0x049C）

| 位 | 字段 | 含义 | 本项目值 |
|---|---|---|---|
| 11:10 | PCLKSEL | 像素钟源：00=DSI_CLK（时钟线频率）01=/2 10=/4 | 00 |
| 7:4 | PCLKDIV | DCLK→PCLK 分频：1=÷1 … 15=÷15（0=÷16） | **4** |
| 1 | LVDLINK | 0=单链 1=双链 | 0（单链） |
| 0 | LVEN | LVDS 发射使能（0=三态） | 1 |

### 时钟公式（本项目最重要的一条知识）

```
LVDS 像素钟 = DSI 时钟线频率 ÷ PCLKDIV = (Byte_clk × 4) ÷ PCLKDIV

首版（雪花）：48975kHz × 4 ÷ 3 = 65.3MHz   带宽余量 0%   ← DSI 协议开销吃穿 → 行缓冲每行欠载
终版（正常）：65300kHz × 4 ÷ 4 = 65.3MHz   带宽余量 +33% ← 像素钟分毫不变，只是 DSI 跑快了
```

带宽账：需求 = 65.3MHz×24bit = 1567.2Mbps；首版链路 = 391.8Mbps×4 = 1567.2（零余量）；终版 = 522.4×4 = 2089.6Mbps。厂商 XLS 示例自带 4.4% 余量（207MHz÷3=69 > 66.06 需求），照抄时序时把余量抄丢了是本次最隐蔽的坑。

## 3. SL1680 侧配套参数（dsi_panel 节点）

| 参数 | 值 | 说明 |
|---|---|---|
| ACTIVE 1280×720，H 97/30/60，V 7/2/14 | HTOTAL=1467 VTOTAL=743 | 与面板 NVM（C0h/B1h 实测）分毫一致 |
| FREQ / PIXEL_CLOCK | 65300 kHz | |
| Lanes | 4 | |
| **Byte_clk** | **65300** | ⚠️ 勿回调 48975（雪花） |
| Vid_mode | 2（burst） | |
| Color_coding | 5（RGB888） | |
| Eotp_tx / dpi_lp_cmd | 1 / 1 | 命令走 LP 模式 |
| power-supply / backlight | 已删除 | 原 RPi 残留指向不存在的 0x45 芯片 |

## 4. 调试全程：具体报错与读数原文（按时间序）

### 4.1 初始黑屏期（软件层三宗罪）

```
/sys/class/drm/card0-DSI-1/status : disconnected disabled     ← SoC 根本没发流
dmesg: [    3.290567] MIPI Rst GPIO not found                  ← panel 探测中段打印（非致命）
i2cdetect -y -r 0: 0x2c 无应答（40/41=UU 是板载电流计）        ← 面板整颗无响应
/sys/kernel/debug/devices_deferred: (空)                       ← 无 probe 挂起
活动设备树: dsi_panel/ACTIVE_WIDTH = 00 00 03 20 (=800)        ← 树莓派残留配置实锤
```

base dts 的 command 是 TC358**762**（树莓派屏）序列：`29 06 10 02 03 00…`（DSI_LANEENABLE=0x3，1-lane）——喂给 775 的 4-lane 场景完全无效。

### 4.2 换新 DTB 后（成功的打印）

```
/sys/class/drm/card0-DSI-1/status : connected enabled, modes: 1280x720
dmesg: [ 6.659009] External Regulator not available            ← 删除 power-supply 后的正常容错路径
crtc[46]: crtc-1  active=1  connector_mask=2
framebuffer[55]: allocated by = weston, 1280x720 XR24
clk_summary: vpll1_clko … 65300000                             ← 显示 PLL 分毫不差
```

### 4.3 屏仍黑：硬件层两宗罪的读数

**RESX 悬空**（面板 U5.9）——三症同源的实测：

```
BIST（SPI 写 B0←00, D6←00, DE←01）无图案；触摸 0x2c 不在；SPI 读全静默
→ 用户把 U5.9 接高 3.3V 后：
i2cdetect: 20: -- … 2c --                                      ← 触摸复活
SPI ID：★ 命中器件码 02 3C 68 0A                                ← 芯片活了
BIST 图案点亮                                                   ← 显示通路无罪
```

**1.8V 倒灌**（LAMTL 侧）——用户万用表实测：

```
VCC1V8_LCM 外供标称 1.8V，实测 2.38V；I2C1_SCL/SDA 同为 2.38V
根因：TC358_RST/TC358_PWR_EN 接了 3.3V → 经 RESX 的 ESD 二极管倒灌抬轨
（2.38 ≈ 3.3 − 两个结压降）。改 1.8V 电平后轨恢复正常。
LVDS PHY 标称 1.8V（datasheet："LVDS PHY: 1.8 V"），2.38V = +32% 超标
```

### 4.4 SPI 调试通道自身的报错链（诊断工具先要自愈）

```
① python3: ModuleNotFoundError: No module named 'fcntl'        ← 板上裁剪版 Python
   → 改用 ctypes.CDLL(None).ioctl 直调 libc
② 位敲候选 GPIO 全部 "export OK 但写 0 读回 1"                  ← 猜的编号没落在 J32 实际脚上
③ spidev 首测：mode0 RX 全 000 / mode3 全 000                   ← 数据脚根本不在 SPI 功能态
④ devmem 0xf7fe2c14 = 0x00095A01
   解码：SM_SPI2_SDO func=2（uart2 RTSn！）SDI func=2（uart2 CTSn！）SS0n/SCLK=0（spi2 ✓）
   ← U-Boot 把两条数据线复用成了 UART2 流控
⑤ devmem 改 0x00005A01 后：mode0 RX 全 1FF                      ← 线活了（上拉可见），芯片仍复位（RESX 期）
⑥ RESX 接高后：RX = 1FF 17E 008 1E3 080 140 …
   ★ 命中器件码 02 3C 68 0A（stride=8, off=1）                  ← 读数与 datasheet BFh 标准值逐字节一致
   （1-bit 偏移是 SDO 下降沿驱动的半拍相位，解析时做位偏移容错即可）
```

pinmux 修复固化进 dts（`spi2_data_pmux`：groups SM_SPI2_SDO/SDI → function spi2），重启后实测 `0x00005A01` 自动就位。

### 4.5 雪花期（第 5 层根因）的症状记录

```
现象：全屏随机彩色噪点（视频抽帧存档 evidence/f1.jpg f3.jpg）
     · 光栅几何稳定（有完整行/帧结构）→ 时钟对锁定、DE 正常
     · 噪点逐帧变化（"活的"）→ 非确定性误码，不是固定错映射
     · 面板 LVDS 故障检测（FAIL_DET）未触发自跑黑屏 → 链路层合法，坏的只是数据位
对照实验：RK3568 原生 LVDS + 同一把杜邦线正常 → 排除线材
面板侧实测（SPI 读 NVM）：
  B1h = 06 00 5A 00 61 00 10 → VESA / 24bit-4lane / DE+SYNC，porch 90/97/16 与我方一致
  C0h = 01 0F 0A 02 D0 00 0A → RTN=271 RCLK，NL=720（原生行数实锤）
结论：配置全对 + 链路锁定 + 数据误码 → DSI→LVDS 行缓冲欠载 → 改 LVCFG 0x31→0x41 + Byte_clk 65300 → 桌面即净
```

### 4.6 顺带记录的工具报错（复用脚本时会再遇到）

```
GStreamer-Wayland-CRITICAL: gst_wl_window_ensure_fullscreen: assertion 'self' failed
  ← waylandsink fullscreen 属性时序告警，无害
WARNING: erroneous pipeline: no element "TD7800"
  ← textoverlay 的 text 属性里放了换行符，被 gst-launch 当元素名；多行文本用多个 textoverlay
busybox 缺项（本轮实撞）：head -c ✗ / head -N ✗（要 -n N）/ timeout ✗ / od -An ✗
  ← head -c 让烧录回读校验假报 MISMATCH（e3b0c44298fc… = 空输入的 sha256，识别这个哈希=管道断了）
PowerShell 内联 wsl 命令的 $变量 会被吃掉 → 一律走脚本文件
```

## 5. 排错决策树（下次再遇 TC358775 链路问题）

```
屏全黑
 ├─ DSI-1 disconnected?          → SoC 侧 dsi_panel 节点/驱动问题（查活动设备树）
 ├─ DSI-1 connected 仍黑
 │   ├─ BIST 能亮?（SPI: B0←00,D6←00,DE←01）
 │   │   ├─ 能 → 面板无罪 → 查桥片：初始化到没到（weston restart 重发）、
 │   │   │        LVPHY/LVCFG、LVDS 线序（OD 组→A 口、P/N）
 │   │   └─ 不能 → 面板侧：RESX(U5.9)? VCC? SPI 通道本身（读 BFh 验证）
 │   └─ 触摸 0x2C 在不在 = 面板整颗死活的免拆快检
 ├─ 全屏活雪花（光栅稳）        → 带宽余量！查 LVCFG PCLKDIV 与 Byte_clk 的比值
 ├─ 颜色错/灰阶异常（几何正常）  → VESA↔JEIDA：删 7 条 LVMX 写回 JEIDA 默认，或对照面板 B1h.LVFMT
 └─ 画面滚动/撕裂               → HTIM/VTIM 与面板 C0h/B1h porch 对不上
```

## 6. 相关资产索引

| 内容 | 位置 |
|---|---|
| 最终 dts（含 command 原文） | `TD7800_bringup_backup_2026-07-28/kernel-dts/dolphin-rdk.dts` |
| 仓库补丁 | `meta-dlsdk/recipes-kernel/linux/files/0001-…patch` |
| SPI 调试脚本（读 ID/BIST/读 NVM） | `TD7800_bringup_backup_2026-07-28/board-scripts/` |
| 厂商寄存器计算表 | `MIPI to LVDS board-1/…/LAMTL-TC358775XBG-DSI-LVDS-timing.xls`（"Code"表） |
| TC358775 功能规格书 | 同目录 `LAMTL-TC358775XBG-datasheet.pdf` |
| 雪花症状帧 | `TD7800_bringup_backup_2026-07-28/evidence/` |
| 六层根因总复盘 | `TM10.5_TD7800屏点亮_总结_2026-07-28.md` |
