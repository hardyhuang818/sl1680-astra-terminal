# TD7800 触摸根因定位与视觉唤醒修复

**日期**：2026-07-28（晚场）　**平台**：SL1680 (Synaptics Astra Dolphin RDK) + TM10.5-TD7800 LVDS 触摸屏
**版本**：meta-dlsdk v2.9　**分支**：[`feat/td7800-reset-level-domain`](https://github.com/hardyhuang818/sl1680-astra-terminal/compare/main...feat/td7800-reset-level-domain)

> 本文只记录本场（晚间）的工作。当天早些时候的九个运行时 bug 见
> `SL1680_故障排查实录_2026-07-28.md`。

---

## 0. 结论先行

| # | 问题 | 根因 | 状态 |
|---|---|---|---|
| 1 | TD7800 触摸长期不报点 | **电平域不匹配**：SL1680 的 1.8V GPIO 物理上驱动不了 TD7800 的 3.3V 域复位输入 | ✅ 定性完成，软件已改回正确形态；永久解需加电平转换 |
| 2 | 视觉唤醒服务 active 但从不触发 | 喂给 NPU 的帧在推理前已被 `multifilesink` 删除，**静默失败** | ✅ 已修，实测端到端跑通 |
| 3 | 触摸驱动 ISR 死锁 | `dev_set_up_app_fw()` 在中断线程上下文同步走 I2C + 拿 mutex | ✅ 已修，补丁入库 |
| 4 | 复位脚交给 VPP 驱动管 | VPP 停在"复位置位"，我们的显示链路走不到它释放复位的代码路径 | ✅ 已回退 |

**最有价值的一句**：软件侧我逐条核对了 pad 映射、pinmux、pinconf、GPIO 方向、驱动死锁，**全部正确** ——
因为问题从来不在那一层。

---

## 1. 触摸：为什么最后确定是硬件

### 1.1 症状

- I2C 地址 `0x2c` 能应答，驱动能读出固件 ID `td7800-12.7.1`
- 但**中断计数恒 0**，ATTN(`gpio586`) 恒 `in hi`（IRQ 配 `TRIGGER_LOW`，等于芯片从不请求上报）
- 每次断电重上表现都不一样：有时半死，有时完全不在 I2C 上
- 显示侧一直正常

### 1.2 软件侧逐条排除

| 检查项 | 手段 | 结论 |
|---|---|---|
| pad↔GPIO 映射 | 读 `drivers/synaptics/pinctrl/berlin/pinctrl-dolphin.c` 的 pad 表 | **正确**（见下表） |
| pinmux | `/sys/kernel/debug/pinctrl/*/pinctrl-handles` | **正确**，`gpio_keys` 已应用 `STS1_* → function gpio` |
| pinconf | `pinconf-groups` | **正确**，`3mA drive + input enabled` |
| GPIO 方向 | `/sys/kernel/debug/gpio` | **out** |
| 驱动死锁 | dmesg | 已修，`helper work(yes)`，0 死锁告警 |

**pad 表标定方法（值得记住）**：找一个*已知能稳定驱动*的脚反推 bank 起始号。
`hdtx5v-gpio = <&portb 23>`，其 pmux 组 `USB2_DRV_VBUS` 在 pad 表里是 **GPIO55**
→ portb 起始 = 55 − 23 = **GPIO32**。

| pad | pad 表 GPIO | portb line | DTS 用法 | 高速采样 |
|---|---|---|---|---|
| USB2_DRV_VBUS | 55 | 23 | `<&portb 23>` | 稳定 |
| STS1_CLK | 39 | 7 | `vol_down` | 稳定 |
| **STS1_SOP** = J32.32 = `TCH_RESET_N` | **38** | **6** | hog | ★飘★ |
| **STS1_VALD** = J32.33 = `RESET_N` | **36** | **4** | hog | ★飘★ |
| I2S2_DI1 = J32.12 = TP_INT | 10 | porta 10 | `<&porta 10>` | 正常 |

> ⚠ 别照抄 mainline 的 `berlin-bg4ct.c` —— 那是另一颗 SoC，注释里的 GPIO 号完全不同
> （它说 `STS1_SOP = GPIO26`）。SL1680 的 compatible 是 `syna,dolphin-soc-pinctrl`。

> ⚠ berlin-pinctrl **注册 0 个 pin、0 条 gpio-range**（`gpio-ranges` 文件为空）→
> gpiolib 无法自动 mux，每个 GPIO 用途都必须在 DT 里显式给 `pinctrl-0`；`pinmux-select` 也用不了。

### 1.3 决定性的对照实验

`gpio-dwapb` 的 `get()` 读的是 **EXT_PORT（真实焊盘电平）**，不是输出影子寄存器。
所以一条 `out` 线读出随机值，就说明**焊盘根本没被驱动**。

高速采样 60 次 @30ms：

```
gpio-516 LCD_RST  (STS1_VALD, hog out)  翻转25  ★飘★   ← 接到屏
gpio-518 TP_RST   (STS1_SOP , hog out)  翻转34  ★飘★   ← 接到屏
gpio-519 vol_down (STS1_CLK , in     )  翻转 0  稳定    ← 同组，没接出去
gpio-524 vmmc_sdhc(对照,     out     )  翻转 0  稳定
gpio-535 hdmitx5v (对照,     out     )  翻转 0  稳定
```

同一个 pmux 组、同一颗 portb 控制器，**飘的正好是接出去到屏的那两条**。指向电气而非配置。

### 1.4 万用表定性

| 脚 | 实测 | 判定 |
|---|---|---|
| J32.32 `TCH_RESET_N` | **1.57V** | 禁区 |
| J32.33 `RESET_N` | **2.28V** | 禁区，离 VIH 只差 30mV |
| J32.1 / J32.2 | 3.3V / 5V | 正常 |

TD7800B datasheet：

- `RESET_N` / `TCH_RESET_N` 属 **IOVCC 域**（Table 44 第 6 项「System interface」）
- **IOVCC = 2.7 ~ 3.6V**（Table 50）
- **`VIH1 = 0.70 × IOVCC`**，`VIL1 = 0.30 × IOVCC`（Table 51 DC characteristics）
  → 3.3V 供电时，**合法高电平最低 2.31V，合法低电平最高 0.99V**

**1.57V 和 2.28V 双双落在 0.99 ~ 2.31V 的禁区里** —— 芯片看到的既不是高也不是低。

而 SL1680 的 `STS1_*` 是 **1.8V 域** pad，输出高最多 1.8V，**物理上给不出 2.31V**。

`2.28V = 1.8V + 0.48V` 正是 pad 上端 ESD 二极管的正向压降 —— 说明**面板侧 3.3V 上拉在往
1.8V pad 里倒灌电流**，被 ESD 结构钳位在 VDDIO + Vf。这是持续的潜在损伤路径。

> 同一块板已有前科：7 月 27 日 `TC358_RST` / `TC358_PWR_EN` 接 3.3V，把 `VCC1V8_LCM`
> 顶到 **2.38V**。**凡是 SL1680 直连面板的控制脚，都要先确认电压域。**

### 1.5 决定性验证

拔掉 J32.32 / J32.33 两根杜邦线后：

```
触摸中断:  0  →  2626
dmesg:  syna_dev_process_unexpected_reset: Device has been reset  ×2
```

拔线产生的复位脉冲让触摸引擎活了，两次意外复位被自愈驱动接住（0 死锁）。**假设成立。**

这一条同时解释了所有此前想不通的现象：

- I2C 从机块能应答固件 ID，但触摸引擎不跑 → 复位处于不确定态
- ATTN 永远不拉低 → 引擎没启动
- 每次断电重上表现都不一样 → 2.28V 离阈值只差 30mV，温度/上拉阻值/驱动强度任何漂移都会翻
- GPIO 读回来是 47% 占空的噪声 → 焊盘被外部拉扯，SoC 没能真正驱动

---

## 2. 视觉唤醒：服务在跑，但从不触发

### 2.1 症状

`systemctl is-active vision-wake` = active，但**开机 30 分钟一条检测日志都没有**，从未触发欢迎语。

### 2.2 根因

手动跑一次就暴露了：

```
$ synap_cli_od -m .../model.synap --score-threshold 0.4 /tmp/vw_270.jpg
E:SyNAP: InputData():102: Input file not found: /tmp/vw_270.jpg
```

把同一帧 `cp` 成稳定副本再推理则完全正常（`Detection time: 52.95 ms`）。

**竞态**：

| 环节 | 时间 |
|---|---|
| `multifilesink max-files=4` @ `framerate=5/1` | 一帧只活 **0.8 秒** |
| `synap_cli_od` 从 fork/exec 到真正打开输入文件 | 要先加载 **30MB** 模型，接近 **1 秒** |

正好卡在边界上 → 时灵时不灵，失败时**静默返回空** → `BH` 为空 → `pct = 0` → 永远达不到 45% 阈值 → 从不触发。

### 2.3 放大这个 bug 的是日志设计

心跳只在 `pct > 0` 时才打，所以"**推理静默失败**"和"**确实没人**"长得一模一样 —— 30 分钟看不出异常。

### 2.4 修法

```sh
# 修前
OUT=$(synap_cli_od -m $MODEL --score-threshold 0.4 "$F" 2>/dev/null)

# 修后
cp "$F" /tmp/vw_cur.jpg 2>/dev/null || { sleep 0.3; continue; }
OUT=$(synap_cli_od -m $MODEL --score-threshold 0.4 /tmp/vw_cur.jpg 2>&1)
case "$OUT" in *"not found"*|*Error*) echo "[vwake] 推理失败(跳过): ..."; continue;; esac
```

外加：没人时也每 ~40 tick 打一条"在跑，当前无人"，让"在跑"和"卡死"可区分。

**修完 5 秒内即报** `[vwake] 框高=75% (阈值45%, armed=1)`，随后实测到 94%、28%、33%。
端到端也跑通了：

```
[vwake] 框高=94%  →  astra_voice: [唤醒] 检测到有人靠近，播欢迎语
[vwake] 人已离开，重新武装
[vwake] 在跑，当前无人 (armed=1)          ← 新加的心跳
```

> **教训**：任何"只在成功时才打日志"的循环，都会把静默失败伪装成正常运行。

---

## 3. 触摸驱动：ISR 上下文死锁

内核报：

```
INFO: task irq/49-synaptic blocked for more than 983 seconds
```

`syna_dev_process_unexpected_reset()` 在**中断线程上下文**里同步调用 `dev_set_up_app_fw()`，
而后者要走 I2C 并拿 mutex → IRQ 线程被永久卡住。

补丁（60 行，`meta-tcm2-touch/.../0001-enable-helper-and-fix-isr-deadlock.patch`）：

1. `syna_tcm2.h`：`/* #define ENABLE_HELPER */` → `#define ENABLE_HELPER`
2. `syna_tcm2.c`：

```c
#if defined(ENABLE_HELPER)
    if (!tcm->helper.workqueue)
        tcm->dev_set_up_app_fw(tcm);
#else
    tcm->dev_set_up_app_fw(tcm);
#endif
```

有 workqueue 就交给 helper，没有才保持原行为。

**实测有效**：拔线产生的那两次意外复位就是它接住的，0 死锁告警。

---

## 4. dts 复位脚的三次变更（走过的弯路）

| 版本 | 做法 | 结果 |
|---|---|---|
| A | `TP_RST` 用 `gpio-hog` 常拉高（`vol_up` 键占了这个 pad，改成 hog 夺回） | 触摸一度恢复 |
| B | `LCD_RESET` 交给 `&avio/vpp` 的 `mipirst-gpios` | ❌ 驱动确实接管了，但停在 `out lo + ACTIVE_LOW`（复位一直按着）。我们的链路是 DSI→TC358775→LVDS，**走不到 VPP 调 `avio_module_mipirst_set_gpio_val(0)` 释放复位的那条路径** |
| C | 两个都改成 `gpio-hog` 常拉高 | ❌ 焊盘读数仍是噪声 —— 此时才发现是电平域问题 |
| **D（最终）** | **两个 hog 全部移除**，复位交给面板侧上拉 | ✅ 拔线后触摸 2626 中断 |

最终 dts 在原位留下了完整注释：电平域数据、失败过的方案、以及恢复 SoC 控制所需的条件。

---

## 5. 改动清单

### 仓库（分支 `feat/td7800-reset-level-domain`，commit `c2e529b`）

| 文件 | 改动 |
|---|---|
| `meta-dlsdk/recipes-kernel/linux/files/0001-dolphin-rdk-td7800-tm10p5-lvds-panel.patch` | 186 → **248 行**：移除两个复位 `gpio-hog` + 完整注释 |
| `meta-dlsdk/recipes-ai/astra-voice/files/vision_wake.sh` | 帧竞态修复 + 显式错误日志 + 无人心跳 |
| `meta-dlsdk/VERSION` | v2.9 条目 |
| `meta-tcm2-touch/`（新） | 触摸驱动层**首次入库**，含 60 行 ISR 死锁补丁 |
| `.gitignore` | 加 `pptx_build/`、`synaptics_blog_assets_original/`、`tmp/`、`*.ko`、`~$*` |
| `tools/_wslout/*`（21 个） | 本次定位用的脚本：pad 表标定、pinmux dump、GPIO 高速采样对照、补丁重生成等 |
| `tools/make_robotics_pptx.py` | Synaptics 博客 deck 生成器 |

**补丁重生成方法（重要）**：不能用 `git diff` —— Yocto 的 `do_patch` 已经把旧补丁提交进
`kernel-source` 的 git HEAD，`git diff` 只会输出**增量**（实测 70 行 vs 应有 248 行）。
正确做法是拿 TD7800 工作开始前的基线 dts 作为 `a/` 侧做完整 `diff -u`，并做**干跑重放验证**：

```
基线 + 补丁  ==  当前 dts     ← patch --dry-run 后 cmp，字节级一致 ✓
```

### 板端

| 项 | sha256 / 说明 |
|---|---|
| 最终 `dolphin-rdk.dts` | `41412f2e17fe438743a765971de04b111ee935e6b083c2f45735d7ef25ad5585` |
| `boot_nohog.subimg`（已烧 p8/p9，回读校验通过） | `dc24f81e6364e151a293f468101c172f475d0f6daa8974b5e906b6ad90666026` |
| `synaptics_tcm2.ko`（自愈驱动） | `7bcbe2c807e47b2beb9f0e9580d4479e4e9248fa701356efc177d15b10d6bf31` |
| `/usr/bin/vision_wake.sh` | 已修，备份 `.bak_race` |

---

## 6. 当前状态（demo 可用）

| 项 | 状态 |
|---|---|
| **显示** | DSI-1 `connected + enabled`，1280×720，面板 PowerMode `0x1F`（sleep_out + display_on），weston `transform=rotate-180` |
| **触摸** | probe 完整成功（`td7800-12.7.1`、`helper work(yes)`、`Device connected`），累计 357 中断，`LIBINPUT_CALIBRATION_MATRIX=-1 0 1 0 1 0` 已生效，weston 已绑 DSI-1 |
| **语音** | astra-voice 在跑，ASR 收音正常 |
| **视觉唤醒** | 完整状态机跑通：靠近→欢迎语→离开→重新武装 |
| **记忆/定时** | astra-timer 在跑 |
| **多屏** | dl-face 在跑 |

---

## 7. 待办：硬件

### 7.1 现状的风险

复位脚现在硬接 3.3V，**能跑但上电不保证稳**。

datasheet Table 57 要求上电复位低电平宽度 **`tRW1 ≥ 3000µs`**（Figure 32：`RESET_N` /
`TCH_RESET_N` 在 IOVCC/VCI/LVDSVCI 爬升期间必须保持低）。直连 VCC 时复位与电源轨同时上升，
低电平宽度 ≈ 0 → NVM 加载可能不完成 → 上电黑屏 / 触摸不上 I2C。

**现场应急**（不用改焊接）：

1. 软件路线：weston 重启（触发 modeset → VPP 重发 TC358775 初始化）+ SPI 发 `29h` Display-On、`11h` Sleep-Out
2. 硬件路线：拔掉两根复位线 → 等 2~3 秒 → 先插回 J32.32（TP_RST）→ 停 1 秒 → 再插 J32.33（LCD_RESET）

> ⚠ **复位线直连 3.3V 时绝不能碰 GND** —— 那是直接短路电源。

### 7.2 短期正解：RC 上电延时

```
3.3V ──[ 10k ]──┬── 复位脚 (J32.32 / J32.33)
                │
              [ 1µF ]
                │
               GND
```

- τ = 10k × 1µF = **10ms**，爬到 VIH(2.31V) 约需 **12ms**，对 `tRW1 ≥ 3ms` 有 **4 倍余量**
- 要让 LCD_RESET 比 TP_RST 晚 10ms，把 J32.33 那颗电容换成 **2.2µF**（约 26ms）
- 加了 RC 之后这两根线才可以安全地碰地做手动复位（电流被 10k 限住）

### 7.3 长期正解：电平转换

要把复位交回 SoC 用软件精确控时序，必须先加 **1.8V → 3.3V 电平转换**：

- **推荐 `74LVC2T45`**（`VCCA` = 1.8V、`VCCB` = 3.3V、`DIR` 固定 A→B），双通道正好两条线，推挽输出顶得住面板上拉
- 或每条线一颗 **2N7002** + 10k 上拉到 3.3V
- ❌ **不要用 `TXB0102` / `TXS0102`** —— 自动方向器件驱动能力弱，会被面板侧上拉误判方向

装好后按 Table 57 做时序：上电复位低电平宽度 `tRW1 ≥ 3ms`，`LCD_RST` 比 `TP_RST` 延后 10ms 释放。

### 7.4 顺带要查的

TD7800 的 `TCH_IO_SCL` / `TCH_IO_SDA` **也在 IOVCC(3.3V) 域**（Table 44 第 6 项）。
目前 I2C 能通，说明总线上拉在 3.3V —— 也就是说 SL1680 的 1.8V I2C pad 一直在承受 3.3V。
虽然一直没出问题，但属于同一类隐患，做板时应一并处理。

---

## 8. 本次的操作失误（记录以免重犯）

| 失误 | 后果 | 教训 |
|---|---|---|
| 裸读 `/dev/mem` 的 pinctrl 寄存器区（`0xf7ea8000`） | **内核挂死，只能断电重上** | 这类寄存器有访问保护或需先使能时钟；看 pinmux 用 `/sys/kernel/debug/pinctrl/*/` 就够，零风险 |
| 明知 unbind/rebind 路径不可靠，仍跑了第二次 | 触摸刚恢复（2626 中断）又被打断，probe 走不完 | 这个驱动 rebind 时 probe 不完整；要重置驱动用模块重载，别用 sysfs rebind |
| `rmmod` 在已损坏状态下调用 | segfault，模块卡在半加载态，只能重启 | 驱动已处于异常状态时不要再叠加操作 |
| 用 `git diff` 生成 Yocto 补丁 | 只出 70 行（应为 248），差点把完整补丁覆盖成增量 | `do_patch` 会把补丁提交进 kernel-source 的 git HEAD；必须对**原始基线**做 diff 并重放验证 |

---

## 9. 可复用的方法（金手法）

1. **pad↔GPIO 标定**：找一个已知能稳定驱动的脚反推 bank 起始号，别靠注释推。
2. **判断焊盘有没有被真正驱动**：`gpio-dwapb` 的 `get()` 读 EXT_PORT（真实焊盘电平）。
   一条 `out` 线读出随机值 = 焊盘没被驱动。配合"同组没接出去的线作对照"，能一眼分开配置问题和电气问题。
3. **电平域快速判据**：读数 = `VDDIO + 0.5V` 左右 → 高压侧上拉在往低压 pad 倒灌，被 ESD 二极管钳位。
4. **补丁重放验证**：`patch --dry-run` 后 `cmp`，确认"基线 + 补丁 == 当前源码"，避免补丁悄悄失真。
5. **静默失败的识别**：服务 active + 零日志 ≠ 正常。任何只在成功路径打日志的循环都要补一条"我还活着"的心跳。

---

## 10. 相关文档

- `SL1680_故障排查实录_2026-07-28.md` —— 当天早些时候的九个运行时 bug
- `TM10.5_TD7800屏点亮_总结_2026-07-28.md` —— 屏点亮全过程（五层根因）
- `SL1680_TD7800_LAMTL_硬件接线总表_2026-07-28.md` —— 三板逐脚接线基准
- `TD7800_SPI_读ID与BIST_源代码_2026-07-28.md` —— SPI 调试通道
- `SL1680项目总览_2026-07-28.md` —— 项目顶层文档

---

*本文引用的 TD7800B datasheet（PN: 505-001033-01 Rev J）内容受 Synaptics NDA 约束，仅供内部工程参考，请勿外发。*

*整理：2026-07-28*
