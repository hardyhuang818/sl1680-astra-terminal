# meta-dlsdk 版本记录
#
# 这份 layer 的"真相"是板子，不是仓库 —— 历史上仓库多次落后于板端实际运行版本。
# 每次同步到 WSL 构建树时在这里追加一条，并跑 tools/export_live.sh 核对 sha256。

v2.9  2026-07-28  ★ 触摸失效定性为电平域不匹配（硬件），视觉唤醒静默失败修复
  【结论先行】TD7800 触摸一直不报点，根因**不在软件**：
    SL1680 的 STS1_* 是 1.8V 域 pad，而 TD7800 的 RESET_N / TCH_RESET_N 属 IOVCC 域
    （datasheet Table 44 第6项；Table 50: IOVCC=2.7~3.6V；Table 51: VIH1 = 0.70 x IOVCC）。
    3.3V 供电时合法高电平最低 **2.31V**，1.8V GPIO 物理上给不出来。
    用户实测：J32.32 TP_RST = **1.57V**，J32.33 LCD_RESET = **2.28V**，
    双双落在 VIL(0.99V) 与 VIH(2.31V) 之间的禁区。
    2.28V = 1.8 + 0.48 正是 pad 上端 ESD 二极管压降 —— 面板侧 3.3V 上拉在倒灌。
    决定性验证：拔掉两根杜邦线 → 触摸中断从 0 跳到 **2626**，
    dmesg 同时出现两次 syna_dev_process_unexpected_reset（拔线产生的复位脉冲）。

  【dts】移除两个复位 gpio-hog，改由面板侧上拉负责（补丁 186→248 行）
    删 td7800_lcd_rst_hog(portb 4 = GPIO36 = STS1_VALD = J32.33 = RESET_N)
    删 td7800_tp_rst_hog (portb 6 = GPIO38 = STS1_SOP  = J32.32 = TCH_RESET_N)
    原位留下完整注释：电平域数据、恢复 SoC 控制所需的电平转换方案与时序要求。
    ⚠ 曾试过 &avio/vpp 的 mipirst-gpios 接管 LCD_RESET —— 驱动确实接管了，
      但停在 out lo + ACTIVE_LOW（复位一直按着）。我们的链路是 DSI→TC358775→LVDS，
      走不到 VPP 调 avio_module_mipirst_set_gpio_val(0) 释放的那条路径。已回退。

  【meta-tcm2-touch】首次入库（此前只有厂商 tarball，补丁不可复现）
    recipes-kernel/linux-drivers/synaptics-tcm2/files/0001-enable-helper-and-fix-isr-deadlock.patch
      1) syna_tcm2.h  打开 ENABLE_HELPER
      2) syna_tcm2.c  syna_dev_process_unexpected_reset() 里 dev_set_up_app_fw()
         原本在 ISR 线程上下文同步调用，而它要走 I2C 并拿 mutex →
         内核报 "INFO: task irq/49-synaptic blocked for more than 983 seconds"。
         改为有 workqueue 时交给 helper，无则保持原行为。
    实测有效：拔线那两次意外复位就是它接住的（0 死锁告警）。

  【vision_wake.sh】修掉"喂给 NPU 的帧已经被删掉"的竞态
    症状：服务 active，但开机 30 分钟一条检测日志都没有，从不触发。
    根因：multifilesink max-files=4 @5fps → 一帧只活 0.8 秒；
          synap_cli_od 从 fork/exec 到读输入文件之间要先加载 30MB 模型，
          正好卡在边界 → "Input file not found" → 静默返回空 → pct 恒 0。
    放大它的是日志设计：心跳只在 pct>0 时打，"静默失败"和"确实没人"长得一模一样。
    修法：先 cp 成 /tmp/vw_cur.jpg 再推理；推理报错明确打日志；无人时也定期报活。
    修完 5 秒内即报 [vwake] 框高=75%。
    → 教训：任何"只在成功时才打日志"的循环，都会把静默失败伪装成正常运行。

  【已知未解决 / 下一步硬件】
    复位脚硬接 3.3V 会导致上电不亮：datasheet Table 57 要求 tRW1 ≥ 3ms 的复位低电平宽度，
    直连 VCC 时复位与电源轨同时上升，宽度≈0，NVM 加载不完成。
    建议接法：3.3V ─[10k]─┬─ 复位脚，脚对地并 1µF（τ=10ms，爬到 VIH 约 12ms，4 倍余量）；
    LCD_RESET 想晚 10ms 就把电容换 2.2µF。
    要把复位交回 SoC 必须先加 1.8V→3.3V 电平转换（74LVC2T45 固定方向；
    或每线 2N7002 + 10k 上拉。**不要用 TXB0102/TXS0102**，自动方向器件驱动弱，
    会被面板侧上拉误判方向）。
  【文档】新增 Synaptics 机器人高速接口博客中文版 + 代理培训包（各 html + pptx）

v2.8  2026-07-28  ★ 交付级镜像 + 一天修掉 9 个真 bug
  【Yocto 集成】rootfs 三件套进镜像，两个遗留项关闭：
    recipes-extended/timezone/tzdata.bbappend       DEFAULT_TIMEZONE=Asia/Shanghai
      ⚠ 文件名不能带版本通配(tzdata_%.bbappend 会 "No recipes available")
    recipes-graphics/wayland/weston-init.bbappend   DSI-1 transform=rotate-180 (追加不覆盖)
      ⚠ 真身是 /etc/xdg/weston/weston.ini.weston-init (update-alternatives),
        debugfs 不跟符号链接 -> 验收要直接查真身文件, 否则误报"缺失"
    recipes-bsp/panel-config/                       触摸 X 轴镜像校准 udev 规则
    recipes-core/systemd-conf/systemd-conf.bbappend 固定公共 DNS (见下 DNS 坑)
    packagegroup 加 tzdata + panel-config
    ⚠ linux-syna.bbappend 必须写成 linux-syna_%.bbappend (配方带版本号 6.12.62)
    cleanall 干净构建验证通过：186 行补丁自动应用, DTB/subimg 字节级校验全绿
  【astra_voice.c】删除 factual_reply 里的天气拦截规则(见下"最贵的教训")
  【astra_llm.py】五处：中文数字音量 / 摄像头同义词 / 城市定位 / 提示词去"根据资料" /
    **Open-Meteo 天气直答**(免key、1-2秒、结构化、不经LLM) / 调用日志 _trace
  【service】麦增益 80%→50%(削顶) / 音量默认 30%→90% / LD_LIBRARY_PATH=/usr/lib/sherpa
  【astra_setvol.sh】诚实化：回读核对, 失败非零退出(旧版无条件 exit 0)
  板端 sha256:
    5ecc0727dc06ed9ae39de54f27f3a51a5bb2b836e202f1050e1cd6d4ddb230d2  astra_llm.py
    f109b79c8adca2920cd0562363f91c7c838c712dcf953b834beb13a82089c350  astra-voice.service
    86f77f7d5eea7e44821860db13061de24ece8409c79e7271f8c9cac088a2fca2  astra_setvol.sh
    375ba4b55e7d8889444d81b77e861fa3297e3f3a46646554d72bae82f342992e  astra_voice(新构建)
  ⚠ 板上原 astra_voice 是 34MB 静态链接老产物, recipe 产出 67KB 动态版 ->
    板子 rootfs 里没有 sherpa .so, 需装 /usr/lib/sherpa + ld.so.conf.d + service 环境变量。
    新镜像自带 sherpa-onnx 包, 无此问题。

  ═══ 最贵的教训：能力补齐后必须删掉"没有能力时的诚实兜底" ═══
  astra_voice.c 的 factual_reply() 里有一条 "我现在没有联网，查不到实时天气" ——
  写它时确实没有数据源，是为了防幻觉，当时**完全正确**；但它排在 cloud_reply 之前，
  等于把后来做好的联网能力永久遮蔽：astra_llm.py 连调用都不会被调到。
  排查耗时六轮，因为每层都在说谎：DNS 假IP(真问题但非主因) / 我的测试没带环境变量(测试本身有缺陷) /
  超时猜测(错) / **strings 默认不认中文, 搜不到内置规则(必须 grep -a)**。
  最终靠给 astra_llm.py 加调用日志才见底 —— 日志文件为空 = 根本没被调用。
  配套教训：astra_voice 调 LLM 时写死 2>/dev/null, 把诊断信息全扔了。

v2.7  2026-07-28  ★ TM10.5-TD7800 LVDS 屏点亮（经 LAMTL/TC358775 桥）
  新增:
    recipes-kernel/linux/linux-syna.bbappend
    recipes-kernel/linux/files/0001-dolphin-rdk-td7800-tm10p5-lvds-panel.patch
      dsi_panel: RPi 配置→TM10.5 1280x720@65.3MHz + TC358775 初始化 command
      Byte_clk=65300 (DSI 带宽 +33% 余量, 回调 48975 会全屏雪花!)
      LVCFG=0x41 (PCLKDIV=4, LVDS pclk=261.2/4=65.3MHz)
      &spi0 + spidev (TD7800 SPI 调试通道) + spi2_data_pmux (夺回 SDO/SDI)
  五层根因复盘见 SL1680_td7800_boot/README_TD7800屏点亮.md
  硬件前提: 面板 U5.9 RESX 拉高 3.3V; LAMTL 1.8V 外供, RST/PWR_EN 用 1.8V 电平
  板端 boot sha256 (boot_a/b 双槽, 分区前 19110288 字节):
    9a86143dac2f0c7650a6ec8d9e113951898498229445ee5a21ba2528c78a8a9f  boot_td7800_margin.subimg
  ⚠ 补丁尚未跑过 cleanall→do_patch 全流程验证 (板子当前配置 = 补丁内容,
    由 work-shared 直接编译产生; 下次干净构建时验证补丁可应用)
  2026-07-28 追加: 触摸也通了 (补丁重生成为 186 行, 含 tcm2 节点)
    synaptics_tcm@2c on i2c0, INT=&porta 10 (=J32.12, pad I2S2_DI1 func0,
    实验法确认: ATTN 锁低, I2C 排空报文后释放), 无 reset-gpio (TDDI!)
    实测: evtest 报点 0-1279×0-719, 双指多点, ~120Hz, IRQ48 porta10
    最终板端 boot sha256 (双槽):
    0aa901725795467ba867e5a11982fadc6a606b7003f89393d327a0fd263dac75  boot_td7800_touch.subimg
  另: 板端 rootfs 手工配置三件套 (镜像重烧会丢, 待进 Yocto recipe):
    /usr/share/zoneinfo/Asia/Shanghai + /etc/localtime (镜像无 tzdata)
    /etc/xdg/weston/weston.ini 追加 [output] DSI-1 transform=rotate-180 (面板倒装)
    /etc/udev/rules.d/99-td7800-touch-cal.rules (触摸 X 镜像校准矩阵)
  触摸最终验收: 用户满屏实画轨迹 OK (方向/多点/跟踪全正常)

v2.6  2026-07-24  ★ 出了第一个"烧完即用"的完整镜像
  bitbake astra-media 成功，产出 SYNAIMG 烧录包 2.5GB，
  已复制到 D:\Claude code\Case6_Astralash_image_20260723163055
  新增:
    recipes-core/packagegroups/packagegroup-astra-terminal.bb
      只拉 astra-voice + dl-face，具体依赖交给各自 RDEPENDS，避免两处不同步
    recipes-bsp/alsa-state/alsa-state.bbappend
      删掉它自带的 32 字节占位 /etc/asound.conf，解除与 astra-voice 的文件冲突

  改:
    recipes-core/images/astra-media.bbappend
      加 ASTRA_TERMINAL_PACKAGES(可在 local.conf 置空退回原厂镜像)
      ⚠️ 合并了重复的 bbappend —— 一度存在两个(recipes-core/ 和 recipes-bsp/)，
         bitbake 会全部应用，谁加了什么根本看不出来
    recipes-ai/astra-voice/astra-voice_1.0.bb
      SYSTEMD_SERVICE 加 vision-wake.service + astra-cleanup.timer(对齐板子实测状态)
      RDEPENDS 加 synasdk-synap-runtime + synasdk-synap-models
        (vision_wake.sh 调 synap_cli_od，这两个包碰巧 astra-media 已装，
         但依赖没声明，换个 image 就静默失效)
    recipes-graphics/dl-clock/dl-clock_1.0.bb
      SYSTEMD_AUTO_ENABLE 改 disable —— 会和 dl-face 抢 DL7400(DLSDK 单进程独占)

  镜像实测(debugfs 直接读 ext4，不是看构建清单):
    自启: astra-voice / astra-translate / dl-face / vision-wake / astra-cleanup.timer
    未自启(正确): dl-clock / astra-mode / astra-xiaozhi
    文件: astra_voice, dl_face, libsherpa-onnx-c-api.so, libonnxruntime.so,
          libdlsdk.so, astra_llm.py, asound.conf, synap_cli_od, wqy-zenhei.ttc

  ⚠️ 仍未闭环: 模型约 2GB 不在镜像里(需单独灌)，且**没有在真板子上实烧验证过**
     (板子 SSH 自 2026-07-23 晚起不可达)。

v2.5  2026-07-23  ★ sherpa-onnx 首次在 Yocto 里完整构建成功
  这个 recipe 此前从没编译过 —— do_configure 就挂了，work 目录只有源码没有 image/。
  修了五个问题后 bitbake sherpa-onnx / astra-voice / dl-face 三个全绿，产出 arm64 deb。

  改 recipes-ai/sherpa-onnx/sherpa-onnx_1.13.4.bb:
    1. SRC_URI 增加 14 个第三方包(带 sha256/downloadfilename/unpack=0)
       + do_configure:prepend 拷进 ${S} 命中 possible_file_locations
       + EXTRA_OECMAKE 加 -DFETCHCONTENT_FULLY_DISCONNECTED=OFF
       根因: cmake.bbclass 硬关了 FetchContent，症状像断网但根本没联网
    2. openfst / eigen 各备两个版本(子项目要的版本和 sherpa 要的不同)
    3. SECURITY_STRINGFORMAT 摘掉 -Werror (espeak-ng 丢了 -Wformat)
    4. do_install:append 把 sherpa-onnx.pc / cargs.h 挪到标准位置
    5. RPROVIDES 声明 libonnxruntime.so(VERS_1.27.0)(64bit)
    另: LICENSE 改为 Apache-2.0 & GPL-3.0-or-later & MIT & BSD-3-Clause
        (开 TTS 会把 espeak-ng GPLv3 编进库，原来写 Apache-2.0 不准确)

  实测产出:
    sherpa-onnx_1.13.4-r0_arm64.deb   运行时包含 4 个 .so, -dev 包 0 个(SOLIBS 修复端到端确认)
      libsherpa-onnx-c-api.so 3.2MB / libonnxruntime.so 17.4MB (strip 后)
    astra-voice_1.0-r0_arm64.deb      Depends: sherpa-onnx (>= 1.13.4) ✓
    dl-face_1.0-r0_arm64.deb          Depends: dlsdk, ttf-wqy-zenhei ✓
    astra_voice / dl_face 均为 ELF 64-bit ARM aarch64

  ⚠️ 仍未闭环: 没有任何 image 把这三个包拉进 rootfs(见 IMAGE_INSTALL)，
     模型约 2GB 不在 recipe 里，未做整板实烧验证。

v2.4  2026-07-23  麦克风等待守护
  新增 recipes-ai/astra-voice/files/astra_wait_mic.sh
  改   recipes-ai/astra-voice/files/astra-voice.service  (ExecStartPre 排第一 + 放宽启动限流)
  改   recipes-ai/astra-voice/astra-voice_1.0.bb          (SRC_URI + do_install)
  板端 sha256:
    4600a0eecc374e74ecb66ded4dcd629b276134cf2960d0dfa6504b58750ff517  /usr/bin/astra_wait_mic.sh
    17145e68fbc3ddb48e2d0e0bdaa6bc1d0e1b9ec6a73bacb11ffcc82902d7d423  /etc/systemd/system/astra-voice.service

v2.3  2026-07-23  Codex 审阅整改
  P0  dl_face.c 移除会让所有输出黑屏的 AUX 轮询，危险版本改名留档
  P1  板端配置全量回灌仓库(11 个文件，其中 5 个仓库里原本不存在)
  P1  新增 recipes-ai/astra-voice/astra-voice_1.0.bb        (此前只有 files/ 没有 .bb)
  P1  新增 recipes-graphics/dl-face/dl-face_1.0.bb          (同上)
  修  recipes-ai/sherpa-onnx/sherpa-onnx_1.13.4.bb          (SOLIBS 修无 SOVERSION 的 .so 被打进 -dev 包)
  板端 sha256:
    538511420bda5a9214060f1a0302ba5e8bfd3a8cc9a06ca8df231b249831d747  /usr/bin/dl_face
