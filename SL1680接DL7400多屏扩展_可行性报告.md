# SL1680 + DL7400 多屏扩展 · 方案总结

> 更新：2026-07-14（含当天真机实测结果）
> 依据：DL-7xxx Datasheet RevE、DisplayLink SDK 1.5.0(build156)、DisplayLink Ubuntu 驱动 v6.2.0 发布说明、Astra SL1680 培训资料、`libdlsdk.so` 二进制实测、**SL1680 真机 USB 实测**

---

## 0. ✅ 已实测跑通（2026-07-17，DL7400 EVK 真机）

**SL1680 接 DL7400 EVK，4K 显示器已点亮。全部验证完成。**

```
发现 DisplayLink 设备: 1 个
设备 #0  id=USB_7000-0003001RW02-7400-08G0014
  固件版本: 12.3.26                    ← 与 SDK 要求完全一致
  ── 屏 #0  首选模式: 3840x2160 @60Hz
     ✅ 已点亮: 3840x2160
     ★ 实测: 30 帧 / 1.29s = 23.3 fps
```

### ⚠️ 实测性能（用**真实视频**重测，已更正）

> **重要更正**：早先用合成测试图测出的「1080p 60fps / 4K 23fps」是**错的，不可用于规划**。
> 那个测试图只有一根白竖条在动，其余帧间不变；DisplayLink 是**按变化区域(delta)压缩**的，
> 几乎不用传数据 → 假的高帧率。**真实视频每帧全变，实测只有 1/8。**

| 内容 | 分辨率 | 实测帧率 | 说明 |
|---|---|---|---|
| **真实视频**（4K VP9 → 缩放到 1080p） | 1920×1080 | **7.4 fps** ❌ | 每帧全变，压缩率低 |
| 合成测试图（仅一根竖条在动） | 1920×1080 | 60.3 fps | ⚠️ **误导性数据**，勿用 |
| 静态图保持 | 1920×1080 | ~10 fps | 故意 10Hz 节流，省 CPU |

**瓶颈定位（实测）**：
```
纯硬解 4K VP9                     : 111 fps   ← 解码毫无压力
硬解 + 软件 videoconvert/videoscale:  8.5 fps  ← CPU 软缩放，浪费 94% 时间(已改掉)
硬解 + 硬件 synavideoconvertscale : 31.9 fps  ← 管线上限
   同上 + sync=true(按时间戳)      : 17.6 fps
   同上 + 推给 DL7400              :  7.4 fps  ← ★ 真正的瓶颈在 DL 推流
CPU 占用: 30% / 65% idle                      ← 不是 CPU 瓶颈
```

### 结论：定位是「多屏内容展示」，不是「视频墙」

| 用途 | 可行性 |
|---|---|
| **数字标牌/看板**：静态图文、局部刷新 | ✅ **很好**（变化少→压缩率高） |
| **多屏 UI/仪表盘**：偶尔刷新 | ✅ 可行 |
| **全屏流畅视频播放** | ❌ **7.4 fps，不可用** |

> 这**印证了最初的悲观判断**。中间一度被合成测试图误导而错误上修，现已用真实内容更正。

### 关于「如何更新固件」——**SDK 自动做了，不用手动刷**

设备现在报告固件 **12.3.26**，正是 SDK 要求的版本。这是 SDK **自动升级**的结果：
```c
dlsdk_config cfg = {0};
cfg.size = sizeof(cfg);
cfg.firmwarePath = "/usr/share/displaylink/DL-firmware";  // ★ 指向 spkg 目录即可
cfg.embeddedMode = 0;
dlsdk_initialise_with_config(&cfg);   // SDK 检测到固件不匹配 -> 自动升级 -> 设备重启
```
早期几次运行报「0 个设备」，就是 SDK 在**后台完成固件升级**（升级约 30s，设备会重启）。升级完成后即正常枚举。
**所以固件更新的正确做法 = 初始化时把 `firmwarePath` 指向 spkg 目录，SDK 全自动处理。**

### 排查过程中的两个教训（避免误导）

1. **`snd-usb-audio` 不是问题** —— DL7400 带 USB 音频，内核会自动绑 `snd-usb-audio` 到音频接口。曾误判为真凶，但**实测证明：它绑着 SDK 照样正常工作**，无需 unbind。udev 里只需要权限规则。
2. **USB 3.0 是硬前提** —— EVK 实测跑在 `speed=5000` SuperSpeed。之前用零售 Anker 坞经 USB-C→USB-A 线只到 USB 2.0，SDK 直接不驱动。

### ✅ 三屏实测跑通（2026-07-17）

**2×4K + 1×1080p 三台屏同时点亮，全部稳定：**
```
屏0  Redmi 27 NU (XMI 0xB005)  4K@60    865 帧   7.2 fps  ✅
屏1  S24D390 三星 (SAM 0x0B65)  1080p   1801 帧  15.0 fps  ✅
屏2  Mi Monitor  (XMI 0x27B4)  4K@60    859 帧   7.2 fps  ✅
```

### 🔑 根因：**绝不能盲信 `dlsdk_display_preferred_mode()`**

Mi Monitor 的 EDID 把 **3840x2160@160Hz** 报成"首选模式"。**DL7400 + SL1680 的 USB3(5Gbps) 根本喂不动这个模式** → `dlsdk_display_show`/`wait_on_show` **永久阻塞**（确定性死卡：7 帧 / 18 帧，每次一样）。

**限到 60Hz 后立刻正常** —— 同一台屏、同一根线、同一个口。

**修复**：`pick_mode()` —— 从 `dlsdk_display_modes()` 里挑**「与 preferred 同分辨率、刷新率 ≤ max_hz(默认60) 的最高刷新率」**，找不到才退回 preferred。
```bash
dl_clock --max-hz 60      # 默认 60，已写进 dl-clock.service
```

### ⚠️ 排查过程中我下过的错误结论（已被数据推翻，勿重蹈）

| 错误结论 | 为什么错 |
|---|---|
| ❌「Mi Monitor 的 HDMI 坏了」 | **错**。HDMI 大概率没问题，是 160Hz 首选模式把链路搞死。故障之所以「跟着屏走」，是因为 **EDID 本来就是屏的属性** —— 这恰恰是它看起来像"屏/线故障"的原因。换 4K HDMI 线"没用"也是同理。 |
| ❌「USB3 带宽不够」 | **错**。降到 5fps（总量 ~207 MB/s，远低于 USB3）照样卡死。 |
| ❌「DRAM 不够」 | **错**。Datasheet Table 3：8Gb LPDDR4x 的 DL-7400 支持三屏（5K2K + 4K + 4K）。 |
| ⏸「DL3590 的 KVM 限制只能 2 屏」 | 已不成立 —— 三屏现在都能跑。 |

### 排查方法论（下次省时间）

- **看每屏的帧数计数器，别靠肉眼**。`dl_clock` 每 60s 打印各屏帧数：
  - **帧数冻结** = 链路卡死
  - **帧数在涨但屏黑** = 显示器输入源没切对
  - 两者原因和解法完全不同，别混为一谈
- **注意日志里的 PID** —— 卡死的旧进程会留下误导性的统计（我就被坑过一次）
- **屏数和模式号会变**（实测 2→3→4）。动线之后必须重新 `dl_edid` 读一次，否则模式号对不上（有一轮测试因此作废）

### 🚫 部署铁律：一台屏只接一根线

**绝不要把同一台屏同时用 DP + HDMI 接到 DL7400** —— 它会被枚举成**两个独立 display（EDID 完全相同）**，同时驱动会冲突。
实测：Mi Monitor 同时接 DP+HDMI 时，在 `^0` 和 `^3` 各出现一次，其中一个还报出**虚假的 3840x2160@160Hz**。

### 排查经验

- **屏数会变**（实测 2→3→4），**模式号也会跟着变** —— 动线之后必须重新 `dl_edid` 读一次，否则模式号对不上（我有一轮测试就因此作废）。
- **判断"真的在显示"看帧数计数器**（`dl_clock` 每 60s 打印），卡死的那一路帧数会冻结。别靠肉眼。

### 理论上限

- **多颗 DL7400 可同时挂**：`dlsdk_get_devices()` 支持枚举多颗（C++ 封装自动扩容），无设备数硬上限（唯一上限是 255 个 surface）。
- → 很多屏 = **N 颗 DL7400 × 每颗 4 屏**，天花板是 USB3 带宽 + A73 CPU + 上述 KVM 限制。

---

## 1. 结论速览

| 检查项 | 结果 | 依据 |
|---|---|---|
| DL7400 能否接 SL1680 | ✅ **能** | DL7400 是标准 USB 设备(UFP)，SL1680 有 4× USB3.0 主机口 |
| 需不需要写内核驱动 | ✅ **不需要** | DLSDK 是**纯用户态 libusb** |
| SDK 有没有 aarch64 | ✅ **有** | `libs/linux/aarch64/libdlsdk.so` 实测存在 |
| SDK 支不支持 DL-7xxx | ✅ **支持** | Release Note 明列 |
| glibc 够不够 | ✅ 够 | 要求 2.30，板子是 2.39 |
| Yocto 集成 | ✅ **已完成并编译通过** | `meta-dlsdk` 层 + demo，产出 arm64 deb |
| **真机点亮 4K 屏** | ❌ **暂时卡住** | **USB 链路只跑到 USB 2.0**，见 §3 |

---

## 2. 🔑 关键发现：DLSDK 是纯用户态，完全不碰内核

实测 `libdlsdk.so`(aarch64) 的动态依赖：

```
NEEDED: libusb-1.0.so.0     ← 唯一的外部依赖
        librt / libpthread / libstdc++ / libm / libgcc_s / libc
内部符号: core/sys/libusbbus/libusb/BusImpl_libusb10.cpp
          /dev/bus/usb/
```

**没有 evdi、没有 DRM、没有任何 .ko。**

DisplayLink 在 PC Linux 上的常规方案要装 **evdi 内核模块 + DisplayLinkManager**，移植到嵌入式很痛苦。
**这个 SDK 完全绕开了它** —— 直接用 libusb 从用户态跟芯片讲话。

> 实际意义：SL1680 上**不用改内核、不用重编 kernel、不用重刷 boot 分区**。
> 甚至**不用重刷镜像** —— 直接把 `.so` + demo `scp` 到运行中的板子就能跑。

---

## 3. 🔴 当前唯一阻塞：DL7400 没跑上 USB 3.0

### 实测到的现象

板子上 `lsusb` **能看到** DisplayLink：
```
Bus 001 Device 005: ID 17e9:7000 DisplayLink A83B3_A1
```

但 USB 拓扑暴露了问题：
```
Bus 001 (USB2.0 root hub, 480M)  ← 所有东西都挤在这条
  └─ 05e3:0610  SL1680 板载 Hub (USB2 面, 4口)
      └─ 2109:2822  Anker 坞的 USB2 Hub
          ├─ 17e9:7000  DisplayLink   ← 480M, ver 2.10  ❌ USB2!
          └─ 291a:83b3  USB Billboard Device  ← Alt-Mode 协商失败的标志

Bus 002 (USB3.0 root hub, 5000M)  ← USB3 这条路上空空如也
  └─ 05e3:0620  SL1680 板载 Hub (USB3 面, 4口)  speed=5000
     ⚠️ 下面什么都没挂
```

`dmesg` 里**全是 `new high-speed`(=USB2)，一条 `SuperSpeed` 都没有**。

### 这直接解释了「SDK 枚举到 0 个设备」

DL-7xxx 是 **USB 3.x SuperSpeed 器件**。在 USB2 下它只能**枚举出来**（所以 `lsusb` 看得到），但 **SDK 拒绝驱动它** → `dlsdk_get_devices()` 返回 0。

**不是驱动问题，是物理链路没跑起来。**

### 板子这一侧是好的（已证明）

```
1-1 : 05e3:0610  USB2.1 Hub  480M   端口数=4
2-1 : 05e3:0620  USB3.2 Hub  5000M  端口数=4    ← 同一颗板载 Hub 的两张脸
```
同一颗 Genesys 板载 Hub 同时提供 USB2/USB3 通道，喂那 4 个 Type-A 口。
👉 **4 个 Type-A 口每个都有 USB3 通道，不存在「哪个口才是 USB3」的问题。换口无用。**

### 原因分析

那个 **`USB Billboard Device`** 是 **USB-C Alt-Mode** 才会出现的东西 → 说明 **Anker A83B3 本质是 USB-C 坞**。
用 **USB-C → USB-A 线**接到板子 Type-A 口时：

> **市面上绝大多数 USB-C 转 USB-A 的线只有 USB 2.0**（哪怕线很粗）。真正 USB3 的 C-to-A 线是少数。

已换过一根线，**仍是 USB2** → 判断为线的问题（目前手上没有支持 3.0 的 USB-A 转 C 线）。

### ⚠️ 退一万步：USB2 下 4K 本来就不可能

| 分辨率 | 未压缩码率(RGB32) | USB2.0 实际可用 ~40 MB/s |
|---|---|---|
| 1080p60 | 497 MB/s | 差 12 倍 |
| 4K60 | 1990 MB/s | 差 50 倍 |

**所以 USB3 不是「优化项」，是「先决条件」。**

---

## 4. ⚠️ 零售 Anker 坞 vs DL7400 EVK —— 固件不是一套

屏幕上出现的 **「驱动版本过低，去 www.anker.com/83B3 下载」** 是 **DisplayLink 固件内置的「没装主机驱动」提示画面**。

**好消息**：它能显示出来，说明坞有电 ✅、HDMI→4K 屏是通的 ✅、坞固件活着并在输出画面 ✅。

**但它也暴露了**：

| | 固件 | 配套驱动 |
|---|---|---|
| **零售 Anker 坞**(手上这台) | Anker 量产固件 | DisplayLink Manager（消费级，Win/Mac） |
| **DL7400 EVK**(明天到) | **SDK 固件 12.3.26** | **DLSDK** ✅ 我们集成的这套 |

SDK changelog 明写：**"Do not list devices with incompatible firmware"**。
→ 所以 `get_devices()` 返回 0 **可能有两个叠加原因**：USB2 **＋** 固件不兼容。

> ⛔ **别去 anker.com 下那个驱动** —— 那是 Windows/macOS 的消费级驱动，SL1680 的 Linux 上完全用不了。
> ⛔ **不要给零售坞刷我们手上的 spkg 固件** —— 那 4 个（ella-dock / navarro-dock / ridge-dock / firefly-monitor）都是 **DisplayLink 参考设计**，不是 Anker A83B3。板型/DDR 配置不同，**有刷砖风险**。

---

## 5. 两条驱动路线对比（有个反直觉的点）

| | **DLSDK**（已集成 ✅） | **官方 evdi 驱动** |
|---|---|---|
| 内核工作量 | ✅ **零**（纯用户态 libusb） | ❌ 要为 6.12.62 编 `evdi` 模块 |
| **屏数上限** | **4**（DL7400 全部输出） | **2**（消费级驱动限制） |
| 零售 Anker 坞 | ⚠️ 固件可能不兼容 | ✅ **本来就是配它的** |
| DL7400 EVK | ✅ | ✅ |
| **能否扩展桌面** | ❌ 只能「推像素」，应用自己渲染 | ✅ **能**（evdi 提供虚拟 DRM 设备） |
| Yocto 适配 | ✅ **已完成** | ❌ 安装包是 Ubuntu 的，要手工移植 |
| **USB3** | **必需** | **必需** |

**官方 Linux 驱动 v6.2.0（2025-09）实测查证：**
- ✅ **DL-7xxx 已列入支持**（v5.7 还没有，新版加了）
- ✅ **aarch64 有二进制**（i686 / amd64 / armv7 / aarch64）
- ✅ evdi 已验证到内核 6.15 → **6.12 没问题**
- ⚠️ **最多 2 个屏**
- ⚠️ 明确要求 **"DisplayLink USB 3.0 devices"**

> **反直觉的点**：官方驱动**反而能「扩展桌面」**（SDK 做不到），但**只能带 2 个屏**（SDK 能带 4 个）。
> 两条路**都要 USB3**，所以 USB3 没通之前，装哪个都点不亮。

### 怎么选

- **要多屏各显示各的内容**（标牌 / 看板 / 内容墙）→ **DLSDK**，4 屏，零内核工作
- **要把 Linux 桌面扩展到外接屏** → **官方 evdi 驱动**，但只有 2 屏，且要编内核模块

---

## 6. 性能天花板（别按 Datasheet 规划）

DL-7xxx Datasheet 写的是 **4× 4K@120Hz** —— 那是**芯片本身**的能力，**不是 SL1680 能喂饱的量**。

**瓶颈① USB 3.0 只有 5 Gbps**（DL7400 支持 10 Gbps）→ 带宽砍半。

**瓶颈② DL3+ 压缩跑在 SL1680 的 CPU 上** ← **真正的天花板**
SL1680 = **4× Cortex-A73 @1.8GHz**。SDK changelog 有 "Enable multi-threaded encoder on **arm64** platform" → 编码是**主机 CPU 软件做的**。

但 DisplayLink 是**按变化区域(delta)压缩**的，画面不动的部分不重编码。
**所以能带几个屏，取决于「内容动不动」，而不只是分辨率：**

| 场景 | 可行性 |
|---|---|
| 标牌/看板：多屏静态图文、局部刷新 | ✅ **很轻松，4 屏 1080p 是甜点区** |
| 多屏 UI / 仪表盘：偶尔刷新 | ✅ 可行 |
| 2 屏 1080p 视频播放 | ⚠️ 需实测 |
| **4 屏 4K60 全动态视频** | ❌ **不现实**（带宽 + CPU 双杀） |

---

## 7. 已交付的东西

| 产出 | 位置 | 状态 |
|---|---|---|
| `meta-dlsdk` Yocto 层 | `Case6_Astra/meta-dlsdk/` | ✅ 编译通过 |
| ├ dlsdk recipe | `recipes-graphics/dlsdk/dlsdk_1.5.0.bb` | 打包 aarch64 `.so` + 头文件 + 4 个固件 spkg + udev 规则(VID 17e9) |
| ├ 多屏 demo | `recipes-graphics/dl-multiscreen-demo/` | 枚举设备/所有屏 → 逐屏点亮 → **实测 fps / MB/s** |
| └ 镜像集成 | `recipes-core/images/astra-media.bbappend` | 依赖 `libusb1`，产出 arm64 deb |
| 快速部署脚本 | `D:\Astra\deploy_and_run_dl.sh` | **不用重刷镜像**，直接 scp 到运行中的板子 |
| USB 诊断脚本 | `D:\Astra\usb_topo.sh` / `usb3_selftest.sh` | 一键查 USB3 链路 |

**踩过的坑（已修）**：Yocto 里 `/var/log` 是符号链接，不能直接打包进去 → 改用 systemd `tmpfiles.d`。

---

## 8. 明天的测试计划（EVK 到了之后）

### Step 1 — 先验板子 USB3（可选，5 分钟）
USB 3.0 U 盘**直插板子** Type-A 口（别过坞）：
```bash
wsl -d Ubuntu-22.04 -- bash /mnt/d/Astra/usb3_selftest.sh
```
出现在 **Bus 002 / 5000M** = 板子 USB3 正常，责任在坞/线。

### Step 2 — 接 DL7400 EVK
EVK 是**原生 USB3 上行**，不走 USB-C→USB-A 转接，**大概率直接解决 USB2 问题**。

确认链路：
```bash
wsl -d Ubuntu-22.04 -- bash /mnt/d/Astra/usb_topo.sh
```
要看到：DisplayLink 出现在 **Bus 002 / 5000M**，`dmesg` 有 **`new SuperSpeed USB device`**。

### Step 3 — 跑 SDK 点屏
```bash
wsl -d Ubuntu-22.04 -- bash /mnt/d/Astra/deploy_and_run_dl.sh
```
它会自动：传 `libdlsdk.so` + libusb + demo + 固件 → 检查 `lsusb` 有没有 `17e9` → 跑 demo。

**期望输出**：
```
发现 DisplayLink 设备: 1 个
  设备 #0  id=...   固件版本: 12.3.26.4df748dd
  ★ 该设备下的 display 数: N
  ── 屏 #0   首选模式: 3840x2160 @60Hz
     ✅ 已点亮
     ★ 实测: 60 帧 / X.XXs = XX.X fps  (未压缩等效 XXXX MB/s)
```

**那个 fps 数字就是「SL1680 到底能带几个屏」的最终答案。** 配合 `top` 看 CPU 占用。

### Step 4 — 按需选路线
- SDK 跑通 → 多屏内容墙方案成立，继续做应用
- 要扩展桌面 → 再上官方 evdi 驱动（我可以编）

---

## 9. 待决策 / 待办

- [ ] **USB3 线**：需要一根支持 3.0 的 USB-A ↔ USB-C 线（当前没有）
- [ ] **EVK 实测**（明天）：这是最关键的一步
- [ ] **选路线**：DLSDK(4屏/推像素) vs 官方 evdi(2屏/扩展桌面)
- [ ] ~~给零售 Anker 坞刷 SDK 固件~~ —— **有刷砖风险，不建议，未经你明确同意不会做**

---

## 附：核心 API（`dlsdk.h` 实测）

```c
dlsdk_initialise_with_config(cfg)                 // 可指定固件目录 + embedded 模式
dlsdk_get_devices(devices, &n)                    // 枚举 DL 设备
dlsdk_device_get_displays(dev, displays, &n)      // 拿到该设备下的 N 个屏
dlsdk_display_preferred_mode(disp, &mode)         // 读屏的首选分辨率
dlsdk_display_power_on_with_mode(disp, &mode)     // 点亮 + 设模式
dlsdk_display_show(disp, fmt, pixels, stride)     // 推一帧  ← 核心
dlsdk_display_wait_on_show(disp)                  // 等这帧送完
dlsdk_display_edid(disp, buf, &len)               // 读 EDID
// 还有热插拔回调、DP AUX 读写、固件升级
```

另有 **Python 绑定**（`...cp312-manylinux_2_31_aarch64.whl`）—— 可先用 Python 在板子上快速验证。
