# SL1680 调试总结 · Codex 审阅与修改建议

**日期：2026-07-23**  
**重点审阅对象：** `SL1680调试总结_2026-07-23_完整版.md`  
**关联对象：** `DisplayLink_Issue_Report_2026-07-23.md`、当前路径中的 `meta-dlsdk` 源码与 systemd 配置

---

## 一、总体结论

Claude Code 的技术排查方向整体正确，完整版中的多数经验具有实际价值，特别是：

- DisplayLink preferred mode 不能盲信，需要限制刷新率；
- DLSDK 是 userspace 推帧接口，不是 DRM/KMS 桌面扩展；
- C920 应使用 `/dev/v4l/by-id`，不能固定 `/dev/videoN`；
- 22050 Hz 音频应由 ALSA plug 重采样到 48 kHz，避免直接触发底层驱动问题；
- DisplayLink 的 `display_show()` 成功不代表物理显示器实际在线；
- 底噪和人声 RMS 重叠时，单纯调整能量门槛无法根治误触发，KWS 才是正确方向；
- 目前经过验证的 DisplayLink 恢复方式仍是完整 teardown、重新枚举和重新点亮。

但是，当前完整版不能直接作为“可重建、可恢复的最终交付状态”。最重要的问题不是主要技术路线错误，而是：

> 文档描述的板端最终运行状态，与当前路径中的源码和 systemd 配置明显不一致。

板子上可能运行着临时修改后的新文件，但这些修改没有完整回灌到仓库。下次重新构建镜像或部署当前仓库时，已经解决的问题可能全部复现。

---

## 二、最高优先级问题：文档与当前仓库不一致

完整版声称已经完成：

- `dl_face --fps 15 → 5`；
- `vision_wake` 采集帧率 `15 → 5`；
- `vision_wake` 检测循环增加 `sleep 0.5`；
- `dl-face.service` 增加 `ExecStartPre=/bin/sleep 20`；
- `astra-voice` 使用 `--min-rms 1150 --min-dur 0.6`；
- 存在并启用 `astra-translate` 服务；
- `astra_llm.py` 已实现音量、设备、实时搜索、普通对话四层处理；
- DisplayLink AUX 自动恢复因会导致双屏黑屏而撤回，改成人工触发进程重启。

但当前路径实际情况如下：

| 项目 | 完整版描述 | 当前路径中的文件 |
|---|---|---|
| `dl_face` 帧率 | 5 fps | `meta-dlsdk/recipes-graphics/dl-face/files/dl-face.service` 仍为 `--fps 15` |
| DLSDK 启动等待 | 启动前等待 20 秒 | `dl-face.service` 中没有 `ExecStartPre=/bin/sleep 20` |
| vision-wake 采集帧率 | 5 fps | `vision_wake.sh` 仍为 `framerate=15/1` |
| vision-wake 检测节流 | 每轮 `sleep 0.5` | 主循环中没有该节流 |
| 麦克风门槛 | `min-rms 1150 / min-dur 0.6` | 仓库 `astra-voice.service` 没有传入这两个参数 |
| 麦克风设备 | 应使用稳定设备名/by-id | 仓库 service 仍写死 `plughw:2,0` |
| 四层 LLM | 已实现 | 仓库 `astra_llm.py` 仍只有基础 DeepSeek 调用 |
| 翻译服务 | 已部署并启用 | 当前路径找不到 `astra_translate.py` 和相应 service |
| DisplayLink 恢复 | 已退回人工重启 | 当前 `dl_face.c` 仍包含每 10 秒 AUX 在线探测和原地重亮 |

### 最危险的不一致

当前文件：

`meta-dlsdk/recipes-graphics/dl-face/files/dl_face.c`

仍在主循环中执行：

```c
enum AuxStatus a = dlsdk_dpaux_read(s->aux, 0x0000, &rev, 1);
```

而 `DisplayLink_Issue_Report_2026-07-23.md` 已经记录：

- 对已断开 sink 调用 `dlsdk_dpaux_read()`；
- 同时存在多屏 `display_show()` 推帧；
- 可能导致该 DL7400 device 上所有输出一起变黑；
- `power_on_with_mode()` 不能恢复；
- 最后只能重启进程、重新枚举。

因此当前仓库仍保留了已经被实测证明不安全的自动恢复代码。这是必须最先处理的问题。

---

## 三、请 Claude Code 优先完成的修改

### P0：消除危险代码与仓库漂移

1. 从生产版 `dl_face.c` 中撤掉 AUX 周期轮询和原地重亮逻辑。
2. 不要在正常推帧期间对掉线 sink 调用 `dlsdk_dpaux_read()`。
3. 保留人工语音命令或明确的运维命令，通过重启 `dl-face` 完成完整重新枚举。
4. 检查 `dl_face.c.pre_selfheal` 是否确实是安全回退版本；不要只按文件名判断，必须 diff。
5. 从板上导出真实运行版本，与仓库逐项比较。

建议从板上导出：

```text
/usr/bin/vision_wake.sh
/home/voice/astra_llm.py
/home/voice/astra_translate.py
/etc/asound.conf
/etc/systemd/system/astra-voice.service
/etc/systemd/system/astra-translate.service
/etc/systemd/system/dl-face.service
/etc/systemd/system/vision-wake.service
```

`/usr/bin/dl_face` 是二进制，只能用于核对哈希和部署状态，不能替代 C 源码。必须确认该二进制对应的准确源码版本和构建命令。

### P1：把板端最终配置正式回灌到 Yocto 层

至少需要同步：

- `dl-face.service`：帧率、启动等待、启动参数；
- `vision_wake.sh`：5 fps、循环节流、摄像头 by-id；
- `astra-voice.service`：RMS、最短语音时长、稳定声卡标识；
- 最终版 `astra_llm.py`；
- `astra_translate.py` 及其 service；
- service 的安装、enable、依赖和清理逻辑；
- `/tmp/astra_status_en.txt` 的生命周期管理。

### P1：建立可核验的部署清单

建议生成一个 manifest，至少记录：

```text
板端目标路径
仓库源文件
文件 SHA-256
systemd enable 状态
构建版本
部署日期
回滚文件
```

以后总结中的“已完成”必须同时满足：

1. 板端实测通过；
2. 源码已回灌；
3. 新镜像或干净部署可重现；
4. 文档中的命令和当前文件一致。

---

## 四、完整版中需要降低确定性的结论

### 1. C920 麦克风崩溃根因

当前文档写得过于确定：

> C920 与 DisplayLink dock 同总线，dock 重枚举打断 USB 等时传输，因此导致 Protocol error。

“换 USB 口后恢复”支持这个方向，但还不能单独证明完整因果。也可能涉及：

- 原 USB 口供电或信号完整性；
- hub 调度或 USB 带宽分配；
- xHCI root-port 差异；
- C920 与 dock 的端点/等时资源冲突。

建议改为：

> 高概率为同一 USB 拓扑下 dock 重枚举或总线资源冲突导致；换到另一 root-port 后问题消失。若要完全坐实，需要结合前后 `lsusb -t`、xHCI 拓扑和同步 dmesg 时间线。

### 2. “热插拔注册失败一定在 libdlsdk.so 内部”

当前证据已经排除了基础 libusb capability 缺失，但还不能完全排除：

- DLSDK 未公开的初始化顺序；
- event loop 要求；
- SDK 构建选项；
- embedded mode 或固件组合限制；
- libusb backend/ABI 行为；
- 运行时前置条件。

建议将：

> Conclusion: the failure is inside libdlsdk.so, not a platform or libusb limitation.

改为：

> The failure occurs within the DLSDK registration path. Basic libusb hotplug capability is present; whether this is an SDK defect or an undocumented prerequisite requires confirmation from DisplayLink.

中文建议：

> 失败发生在 DLSDK 的注册路径内；系统具备基础 libusb hotplug capability，但究竟是 SDK 缺陷还是未公开的前置条件，仍需 DisplayLink 确认。

### 3. 用 `prompt_tokens=21` 证明没有联网

这是强诊断线索，但不应作为普适的唯一判据。供应商可能使用 server-side retrieval，或者 usage 字段不统计工具结果。

建议改成：

> 在本次智谱接口实测中，返回没有搜索结果、没有工具调用证据，回答与真实数据明显冲突，且 prompt token 数与原始问题规模相当。这些证据共同表明该请求没有发生有效联网检索。

更可靠的验证还应包括：

- 返回中是否有 tool/search 调用记录；
- 是否返回来源或引用；
- 使用当日新事件或随机 nonce 做不可猜测查询；
- 多次重复结果是否一致；
- 服务端请求日志或计费记录。

### 4. CPU 从 52.5% 降至 21.7%

需要补充实验条件，否则数字不能稳定复现：

- 是整机 CPU busy%，还是某个进程占用；
- 四核总量按 100% 还是 400%；
- 采样窗口长度；
- 两次测试的显示分辨率、实际 fps 和画面变化程度；
- 是否正在 ASR、TTS、摄像头检测；
- 各进程 CPU 分布。

建议至少做三组 60 秒稳定采样，并同时记录：

```text
整机 busy%
load average
dl_face CPU%
synap_cli_od CPU%
astra_voice CPU%
各显示器实际 fps
测试时的业务状态
```

文档中的像素带宽：

```text
2880 × 1800 × 4 bytes × 15 fps ≈ 311 MB/s
```

只能表示未压缩源 buffer 的理论读取/搬运量，不能直接等同于 USB 实际传输量或 CPU 内存带宽消耗。

---

## 五、DisplayLink 问题报告审阅

### 总体评价

`DisplayLink_Issue_Report_2026-07-23.md` 的结构清楚，Issue 2 和 Issue 4 的 API 对比很有价值，可以提交给 DisplayLink。

尤其值得保留的证据：

- `get_displays()`、EDID、preferred mode 对掉线屏仍返回缓存状态；
- `dpaux_read()` 能区分正常屏和掉线屏；
- `display_show()` 和 `wait_on_show_for()` 对正常屏、黑屏都返回 SUCCESS；
- `power_on_with_mode()` 返回 SUCCESS 但不能重新建立链路；
- 完整 teardown/re-enumeration 能恢复。

### Issue 3 仍需加强因果隔离

当前证据说明：

> 屏 1 断电后，在多屏推帧期间调用 AUX read，随后两块屏一起黑。

但还不足以完全区分以下原因：

- AUX read 与 show 的线程安全或 SDK 锁问题；
- DL7400 固件 DP AUX 状态机问题；
- 显示器断电事件本身导致 dock 级异常；
- 应用调用方式不符合 SDK 的隐含并发要求。

建议补做以下 A/B 实验：

1. 推帧期间关闭屏 1，但完全不调用 AUX read。
2. 两屏都在线时持续调用 AUX read。
3. 屏 1 断电后，暂停所有 `display_show()`，再调用 AUX read。
4. 只向屏 0 推帧，对掉线的屏 1 调 AUX read。
5. 将 AUX read 与全部 show 调用串行化到同一线程。
6. 测试 `dlsdk_dpaux_begin_exclusive_access()` / `end_exclusive_access()`。
7. 分别测试拔线、关显示器电源、切换输入源。
8. 测量每次 AUX read 的实际耗时，而不只记录返回状态。
9. 如条件允许，抓 USB trace，判断 bulk endpoint 是否停止或固件是否不再消费帧。

在完成这些实验前，Issue 3 建议使用：

> Under the tested concurrent-streaming conditions, AUX access to a detached sink consistently preceded a device-wide display stall.

不要直接写成已经完全证明 SDK 内部某把 device-level lock 一定被持有；锁只是合理假设。

---

## 六、建议的安全恢复架构

在 DisplayLink 官方确认正确的 hotplug/presence API 之前：

1. 正常渲染进程不要轮询掉线 sink 的 AUX。
2. 语音或运维命令触发 `systemctl restart dl-face`。
3. 重启前确保进程能尽量正常执行 DLSDK teardown。
4. 如果普通重启无法重新枚举，再按 VID `17e9` 动态寻找 USB device 做 unbind/bind。
5. 不要把 USB sysfs 路径写死成 `2-1.4.1`。
6. systemd 增加启动等待和合理的 RestartSec，避免 DL7400 尚未稳定时反复初始化。
7. 自动恢复若必须实现，可以在低业务时段做完整的受控重枚举，而不是在实时推帧线程旁边做 AUX polling。

需要注意：

- 帧计数增长只能说明应用仍在提交/处理帧；
- `display_show()` SUCCESS 也不能证明显示器在线；
- 因此软件无法仅靠现有帧路径形成可信的物理显示健康检查。

---

## 七、语音部分后续建议

### KWS 优先级应高于继续调整 RMS

已测数据：

- 底噪 RMS：约 575–713；
- 人声 RMS：约 470–1170。

两者明显重叠，因此不存在一个能同时做到低误触发、低漏检的单一能量阈值。

建议：

1. 引入“小智”等 KWS 唤醒词；
2. RMS 只作为 KWS 前后的辅助 VAD；
3. 记录唤醒前后音频环形缓冲，避免吞掉唤醒词后的第一句话；
4. 后续再评估 AEC，避免扬声器 TTS 被麦克风重新识别；
5. 将误触发率、漏唤醒率和距离作为正式验收指标。

### 搜索失败不应默认回落普通 LLM

完整版写的是“搜不到就回落普通问答”。对天气、股价、汇率、新闻等实时问题，这会重新引入幻觉风险。

建议改成：

- 实时问题搜索成功：使用检索结果回答；
- 搜索失败或证据不足：明确说暂时无法获取实时数据；
- 不允许自动回落到无实时资料的普通模型生成具体数字。

---

## 八、安全问题

文档提到 DeepSeek key 和智谱 key 曾经出现在聊天中。

建议不要等“产品化前”再处理，应立即：

1. 作废旧 key；
2. 生成新 key；
3. 检查仓库、脚本、日志、备份和 shell history；
4. key 仅通过权限受限的 EnvironmentFile 或密钥服务注入；
5. 报告、备份和诊断输出中只显示掩码；
6. 确认备份 tar 包中没有旧 key。

已经在聊天或日志中明文出现的 key，应按已经泄露处理。

---

## 九、建议 Claude Code 更新完整版的状态定义

建议把结论分成三类，不要统一写成“所有结论均来自实测”：

| 标记 | 含义 |
|---|---|
| ✅ 已验证 | 有明确命令、日志、重复测试或重启验证 |
| ⚠️ 高概率推断 | 现象符合，但尚未完成因果隔离 |
| ❓ 待厂商确认 | SDK 行为、硬件设计或未公开条件无法从现有资料确认 |

例如：

- “换 USB 口后 C920 恢复”是 ✅；
- “根因一定是 dock 重枚举打断等时传输”是 ⚠️；
- “hotplug 注册失败是 DLSDK bug”是 ❓/⚠️；
- “show 返回 SUCCESS 但屏幕黑”是 ✅；
- “内部 device-level lock 导致全屏黑”是 ⚠️。

---

## 十、建议的最终验收清单

Claude Code 完成修改后，应至少提供以下结果：

- [ ] 仓库生产版 `dl_face.c` 已无危险 AUX 自动轮询；
- [ ] `dl-face.service` 与板端运行参数一致；
- [ ] `vision_wake.sh` 已同步 5 fps 和循环节流；
- [ ] `astra-voice.service` 已同步最终 RMS/min-dur 和稳定设备标识；
- [ ] 最终版 `astra_llm.py` 已回灌；
- [ ] `astra_translate.py` 和 service 已加入仓库/Yocto recipe；
- [ ] 搜索失败不会回落为无依据的实时数字；
- [ ] 所有 service 均能由干净镜像安装并自动 enable；
- [ ] 冷启动后两屏、语音、翻译、视觉唤醒全部恢复；
- [ ] 人工显示恢复命令经过断屏测试；
- [ ] 旧 API key 已撤销；
- [ ] 生成板端部署文件 SHA-256 manifest；
- [ ] 更新完整版中写得过于确定的根因结论；
- [ ] DisplayLink Issue 3 补充 A/B 实验或明确标记为工作假设。

---

## 十一、给 Claude Code 的直接任务

请以当前文件为审阅依据，完成以下工作：

1. 检查板端实际运行文件和当前仓库的差异。
2. 将板端已经验证的最终修改完整回灌到 `meta-dlsdk`。
3. 删除或禁用 `dl_face.c` 中已被证明不安全的 AUX 自动恢复。
4. 确保当前仓库能从干净镜像重建完整版描述的功能。
5. 修订 `SL1680调试总结_2026-07-23_完整版.md` 中过于绝对或缺少证据边界的措辞。
6. 修订 `DisplayLink_Issue_Report_2026-07-23.md`，区分观测事实和工作假设。
7. 输出修改文件列表、关键 diff、构建结果、板端部署结果和验收日志。

不要只修改 Markdown；本次工作的核心是让文档、源码、Yocto recipe、systemd 配置和板端实际状态一致。
