# SL1680 驱动机器人手指 —— 方案调研（硬件 + 软件）

**日期**：2026-07-24
**依据**：J32 排针接线为本项目原理图实读（RevE 第 4 页实表）；外部方案为 GitHub/官网公开资料，文末附来源。

---

## 零 · 结论速览

**SL1680 完全可以驱动机器人手指，且不需要写任何内核代码。** 三条硬件路径按推荐排序：

| 路径 | 怎么接 | 成本量级 | 适合 |
|---|---|---|---|
| **A. 串行总线舵机**（推荐） | USB 转接板 → `/dev/ttyUSB0`，舵机菊花链 | 舵机 ~¥60-100/只 | 正经做手，带位置/扭矩反馈 |
| **B. PCA9685 + 普通舵机**（最便宜） | J32 的 I2C（pin3/5）→ 16 路 PWM 板 | 整手几百元（3D 打印） | 快速 PoC、预算最低 |
| **C. 成品灵巧手** | USB/串口直连 | ¥千级~万级 | 研究/演示，免机械工作 |

软件三条路都在**用户态**：纯 Python 标准库（板上无 pip 也能跑）、交叉编译 C（走我们现成的 Yocto 工具链）、或 vendoring 纯 Python SDK（与我们部署模型同法）。

**独有优势**：本项目已有的语音+视觉栈直接复用——语音说「握个手」→ `astra_llm.py` 的
`try_device()` 加一个分支；NPU 看到人走近 → 自动挥手（`vision-wake` 的 SIGUSR1 模式照搬）。
Case41 官方资料也明确写了 Smart Assistant "可通过 **GPIO 做 tool calling**"——语音→动作是官方路线图内的用法。

---

## 一 · 板子这一侧：有什么接口可用（实读原理图）

**J32 40-pin 排针**（1.8V SoC 经 TXB0108 转 3.3V 电平）：

| 资源 | 引脚 | 驱动手指的用途 |
|---|---|---|
| **I2C（TW0）** | 3=SDA、5=SCL | → PCA9685 舵机板（方案 B 主通道） |
| **UART0** | 8=Tx、10=Rx | → 总线舵机 TTL 电平直连（备选） |
| PWM ×2 | 7=PWM[1]、16=PWM[2] | 仅 2 路，**不够 5 指**，只做单指验证 |
| SPI2 | 19/21/23/24/26 | 已有先例：官方方案用它驱 WS2812B 灯带 |
| ADC ×2 | 16/18 | 可接压力/弯曲传感器做力反馈 |
| 5V | 2/4（USB-C PD 总预算 25.5W） | ⚠️ 只够小舵机，见风险节 |
| **USB-A ×4** | GL3523 hub | → 总线舵机 USB 适配器（方案 A 主通道，**最省事**） |

已验证的用户态外设先例（说明这条路是通的）：
- 官方 Edge AI 方案：**SPI 驱 LED 灯带、I2C 驱触摸板**（Case41 PoC 清单）
- 本板 I2C 上挂着两颗 INA220 电流计（0x40/0x41），i2c 总线用户态可访问
- 官方文档确认 TWSI=I2C、40-pin 排针及 GPIO expander 用法（[SL1680 User Guide](https://synaptics-astra.github.io/doc/v/latest/hw/sl1680.html)）

> ⚠️ **别用片上 PWM 做多指**：只有 2 路，且这块板的 pinmux/设备树我们踩过坑
> （板载麦 i2s-mic1 时钟问题至今需 FAE）。PCA9685 和 USB 舵机适配器都绕开了这个雷区。

---

## 二 · 硬件方案详解

### 方案 A：串行总线舵机（推荐）

一根总线菊花链挂多个舵机，每只有 ID，可**回读角度/温度/负载**——这是所有正经开源机器手的共同选择。

| 舵机体系 | 代表项目 | 接入 SL1680 的方式 |
|---|---|---|
| **Feetech STS3215**（~$15/只） | **[SO-ARM100/101](https://github.com/TheRobotStudio/SO-ARM100)**（HuggingFace [LeRobot](https://github.com/huggingface/lerobot) 官方推荐硬件，6 只舵机成臂） | Feetech USB 总线适配器 → `/dev/ttyUSB0`，115200~1M baud |
| **Dynamixel XC330** | **[LEAP Hand](https://github.com/leap-hand/LEAP_Hand_API)**（CMU，16-DOF，$2000 自组，MIT 许可 API，Python/C++/ROS） | U2D2 USB 适配器 → `/dev/ttyUSB0` |
| Hiwonder LX 系列 | [uHandPi](https://www.hiwonder.com/products/uhandpi)（见方案 C） | 自带串口控制板 → USB serial，[第三方 Python 模块](https://github.com/aakmsk/serial_bus_servo_controller_python_module) |

**为什么推荐**：只占一个 USB 口（板上有 4 个）、电平/时序问题全在适配器里解决、
LeRobot 生态（模仿学习/遥操作）未来可直接嫁接。

### 方案 B：I2C + PCA9685 + 普通舵机（最便宜）

经典配方，树莓派世界的标准做法，SL1680 同样适用：

```
J32 pin3(SDA)/pin5(SCL)/pin9(GND) ──► PCA9685 16路PWM板 (~¥15)
                                          │ 独立 5-6V 舵机电源(共地!)
                                          ├─► MG90S/MG996R ×5 (~¥10-30/只)
                                          └─► 腱绳(鱼线)牵动 3D 打印手指
```

- **手指机械**：[InMoov Hand](https://inmoov.fr/hand-and-forarm/) —— 最成熟的开源 3D 打印手，
  每指一舵机 + 200 lb 鱼线腱绳，社区大量 [树莓派+PCA9685 复刻案例](https://github.com/mpi3d/robotic-arm)
- **驱动芯片**：PCA9685 是 I2C 器件，Linux 下**纯用户态**可驱（见软件节）

### 方案 C：成品手（免机械工作）

| 产品 | 特点 | 接入方式 |
|---|---|---|
| **[Hiwonder uHandPi](https://www.hiwonder.com/products/uhandpi)** | 树莓派生态成品五指手，串行总线舵机，Python SDK。**把 RPi 换成 SL1680 即可**（同为 Linux + USB serial） | USB 串口 |
| **[LEAP Hand](https://github.com/leap-hand/LEAP_Hand_API)** | 16-DOF，4 小时可组装，性能超 Allegro 而价格 1/8；V2 Adv 17-DOF 刚柔混合 | U2D2 USB |
| **[ORCA Hand](https://github.com/orcahand)**（ETH Zurich） | 17-DOF 腱驱拟人手，<2000 CHF，8 小时组装；`orca_core`(Python) / `orca_sim` / URDF 全开源；关节过载自脱位设计，2250 次连续抓取零故障 | USB |
| 因时 Inspire RH56 等国产工业手 | 商用级，串口/CAN 协议 | 串口 |

---

## 三 · 软件方案（贴着本板现实写）

本板现实：**无 pip、无 gcc**，一切要么纯 Python 标准库、要么 WSL 交叉编译、要么 vendoring
纯 Python 包拷上板（我们部署模型就是这么干的）。三条路全部走得通：

### 路线 1：纯 Python 标准库（最快 PoC）

**PCA9685 只需 i2c-dev + ioctl，50 行标准库代码**，无任何依赖：

```python
# /home/voice/servo.py —— 标准库驱 PCA9685（骨架）
import os, fcntl, time
I2C_SLAVE = 0x0703
f = os.open("/dev/i2c-0", os.O_RDWR)      # 总线号以板上 ls /dev/i2c-* 为准
fcntl.ioctl(f, I2C_SLAVE, 0x40)           # PCA9685 默认地址
def wr(reg, val): os.write(f, bytes([reg, val]))
wr(0x00, 0x10); wr(0xFE, 121); wr(0x00, 0x00); time.sleep(0.005)  # sleep→prescale(50Hz)→wake
def servo(ch, us):                        # us: 500~2500 脉宽
    ticks = int(us * 4096 / 20000)
    base = 0x06 + 4*ch
    for i,b in enumerate([0,0, ticks&0xFF, ticks>>8]): wr(base+i, b)
servo(0, 1500)   # 通道0 舵机回中
```

**串口总线舵机**同理：`termios` 标准库直接开 `/dev/ttyUSB0`；或把
[pyserial](https://github.com/pyserial/pyserial)、Feetech `scservo_sdk`、
[DynamixelSDK](https://github.com/ROBOTIS-GIT/DynamixelSDK)（均为**纯 Python**）
整目录拷到 `/home/voice/lib/` 即用——不需要 pip。

### 路线 2：交叉编译 C（性能/产品化）

现成用户态库直接编：[libPCA9685](https://github.com/edlins/libPCA9685)（专为 Linux i2c-dev 写，
无 wiringPi 依赖）、[pi-pca-9685](https://github.com/andrewwgordon/pi-pca-9685)。
走我们已打通的 WSL 交叉编译 → `meta-dlsdk` recipe 路线，和 `astra_voice` 同法。

### 路线 3：ROS 2（研究向，不建议 PoC 用）

LEAP/ORCA/LeRobot 都有 ROS 接口；Yocto 有 meta-ros（scarthgap 可用）。
但对"手指跟着语音动"这个场景是杀鸡用牛刀，建议后期需要遥操作/模仿学习时再上。

### 与现有语音/视觉栈的集成（一天量级的活）

```
「握个手 / 挥挥手 / 比个耶」
   → astra_llm.py try_device() 新增手势分支（照抄"打开摄像头"的短指令精确匹配模式）
   → echo "wave" > /tmp/astra_gesture.txt（或直接调 servo.py）
   → servo daemon 播放预置手势序列

人走近（NPU 检测，已有）
   → vision_wake.sh 的 SIGUSR1 模式照搬 → 自动挥手欢迎
```

这正是 Case41 官方 Agent 架构里 "tool calling via GPIO" 的实例化——
**语音多模态大脑 + 机械输出**，方案上是官方背书的方向。

---

## 四 · 推荐落地三步

| 阶段 | 内容 | 采购 | 周期 |
|---|---|---|---|
| **1. 单指验证** | PCA9685 + 1×MG90S + 上面 50 行 Python，语音「动一下手指」 | ~¥50 | 1-2 天 |
| **2. 五指手** | InMoov 手 STL 打印 + 5 舵机腱绳；或直接买 uHandPi 手部接 SL1680 | ¥300~1500 | 1-2 周 |
| **3. 灵巧手** | Feetech 生态（对接 LeRobot）或 LEAP/ORCA（对接研究生态） | ¥7k~15k | 按需 |

---

## 五 · 风险与注意

1. **舵机必须独立供电、与板共地**。MG996R 堵转电流 2A+/只，J32 的 5V 出自 USB-C PD
   （整板预算 25.5W），5 指同时动作必炸电源——独立 5-6V/10A 电源是标配做法
2. **避开片上 PWM**：仅 2 路 + pinmux/DT 是本板已知雷区（板载麦的教训）
3. **I2C 总线号要实测**：板上 `ls /dev/i2c-*` 确认 TW0 对应哪个编号，别猜
4. **电平**：J32 是 3.3V（TXB0108 转过），PCA9685/总线舵机适配器都兼容；
   直连 UART0 到舵机时确认舵机侧是 TTL 3.3V 容限
5. 手指抓握不需要硬实时，Linux 用户态完全够；总线舵机自带位置环，主控只发目标角

---

## 六 · 已购硬件核对：PCA9685A + MG90S（可以，注意三点）

**结论：这就是第四节"单指验证"的标准组合，直接用。**
PCA9685A 与 PCA9685 是同一颗 NXP 芯片（后缀差异，寄存器/地址/协议完全一致）；
MG90S 是金属齿轮 9g 舵机（~1.8 kg·cm @4.8V，堵转电流几百 mA 级），单指/轻负载腱绳合适。

### 接线（对照 J32 实表）

```
PCA9685A 板              SL1680 J32 排针
  VCC  (逻辑电源)  ←───  pin1  (3.3V)
  SDA            ←───  pin3  (TW0_SDA)
  SCL            ←───  pin5  (TW0_SCL)
  GND            ←───  pin9  (GND)
  V+   (舵机电源)  ←───  独立 5V 电源（与板共地！）
  通道0 三针        →    MG90S（棕=GND 红=V+ 橙=信号）
```

单个 MG90S 短时测试可以从 pin2 (5V) 取电，但**转起来就换独立电源**——
板子整机 USB-C PD 预算才 25.5 W，舵机瞬态电流会拖累全板。

### ⚠️ 三个注意

1. **I2C 地址可能撞车（本板特有，先查再上电跑）**：PCA9685 默认地址 **0x40**，
   而载板上有两颗 INA220 电流计占着 **0x40/0x41**（原理图实读）。它们在不在 TW0
   这条总线上待实测——接好后先跑 `i2cdetect -y <总线号>`（镜像里有 i2c-tools）：
   - 若 0x40 已被占：焊 PCA9685A 板上的 **A1 跳点 → 地址变 0x42**，代码里改地址即可
   - 总线号也要实测：`ls /dev/i2c-*` 逐个 i2cdetect，认准出现 0x40/0x42 的那条
2. **脉宽范围保守起步**：MG90S 先用 **1000~2000 µs**（对应约 ±45°），确认不顶死
   再逐步放宽到 500~2500 µs。顶死 = 堵转 = 电流暴涨 + 舵机发热
3. **共地**：独立舵机电源的 GND 必须和 J32 的 GND 连在一起，否则信号电平没有参考

### 到手后的最短验证路径

```bash
# 1. 板上确认总线和器件
ls /dev/i2c-*
i2cdetect -y 0     # 逐条总线查，找到 0x40（或改跳线后的 0x42）

# 2. 跑第三节的 50 行标准库 servo.py（把总线号和地址改成实测值）
python3 /home/voice/servo.py

# 3. 接语音：astra_llm.py try_device() 加「动一下手指」分支
```

---

**Sources**:
- [LEAP_Hand_API (GitHub)](https://github.com/leap-hand/LEAP_Hand_API) · [LEAP Hand 论文](https://www.roboticsproceedings.org/rss19/p089.pdf) · [leap-hand org](https://github.com/leap-hand/)
- [SO-ARM100 (GitHub)](https://github.com/TheRobotStudio/SO-ARM100) · [huggingface/lerobot](https://github.com/huggingface/lerobot) · [LeRobotDepot 硬件目录](https://github.com/maximilienroberti/lerobotdepot) · [Seeed SO-ARM 教程](https://wiki.seeedstudio.com/lerobot_so100m/)
- [ORCA Hand (ETH SRL)](https://srl.ethz.ch/orcahand.html) · [ORCA 论文 arXiv:2504.04259](https://arxiv.org/abs/2504.04259) · [github.com/orcahand](https://github.com/orcahand)
- [InMoov Hand and Forearm](https://inmoov.fr/hand-and-forarm/) · [RPi+PCA9685 复刻例](https://github.com/mpi3d/robotic-arm) · [RPi 官方杂志远程手案例](https://magazine.raspberrypi.com/articles/telepresence-hand-robot-hazardous-areas)
- [Hiwonder uHandPi](https://www.hiwonder.com/products/uhandpi) · [串行总线舵机 Python 模块](https://github.com/aakmsk/serial_bus_servo_controller_python_module)
- [libPCA9685](https://github.com/edlins/libPCA9685) · [pi-pca-9685](https://github.com/andrewwgordon/pi-pca-9685) · [PiPCA9685](https://github.com/barulicm/PiPCA9685)
- [Synaptics Astra SL1680 User Guide](https://synaptics-astra.github.io/doc/v/latest/hw/sl1680.html) · [Machina 开发套件 PDF](https://cp.synaptics.com/cognidox/download/NR-156755-MS-APPROVED.pdf)
