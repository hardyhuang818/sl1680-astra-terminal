# Astra SL1680 建置環境與指令手冊

**日期**：2026-08-01　**適用**：Synaptics Astra SL1680（Dolphin RDK）
**SDK 版本**：`scarthgap_6.12_v2.3.0`（Yocto 5.0 scarthgap / kernel 6.12.62）
**對象**：需要搭建同款建置環境的同事。本文所有資料取自實際運作中的建置主機，並非抄錄官方文件。

---

## 0. 一張圖看懂整體結構

```
┌─ Windows 11 ────────────────────────────────────────────────┐
│  D:\...\sl1680-astra-terminal\        ← git 儲存庫(文件+自有層原始碼) │
│                                                              │
│  ┌─ WSL2: Ubuntu 22.04 ────────────────────────────────┐    │
│  │  /home/astra/sdk/                ← 官方 SDK(鎖定版本)  │    │
│  │    ├─ poky/                        Yocto 5.0.9        │    │
│  │    ├─ meta-synaptics/              官方 BSP 層         │    │
│  │    ├─ meta-openembedded/ 等        官方自帶            │    │
│  │    ├─ meta-dlsdk/            ★ 我們的層(由儲存庫接入)   │    │
│  │    ├─ meta-tcm2-touch/       ★ 我們的層(觸控驅動程式)   │    │
│  │    └─ build-sl1680/                建置目錄             │    │
│  │         └─ tmp/deploy/images/sl1680/   產出物           │    │
│  └──────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────┘
                    │ scp / dd
                    ▼
            SL1680 板子 (192.168.5.126)
```

**核心原則：不 fork 官方 SDK。** 上游鎖定在 tag，我們的全部修改都放在兩個自有 Yocto 層裡
（核心改動以 `bbappend` + patch 形式套在官方 `linux-syna` 上）。官方升級時只需換 tag 重新驗證。

---

## 1. 主機需求（實測配置）

| 項目 | 本機實際 | 最低建議 |
|---|---|---|
| 系統 | **Ubuntu 22.04.5 LTS**（WSL2） | Ubuntu 20.04/22.04，實體機或 WSL2 皆可 |
| CPU | 24 核 | ≥8 核（核心越多全量建置越快） |
| 記憶體 | 47 GB | ≥16 GB（連結 chromium/qt 時吃記憶體） |
| 磁碟 | 1 TB（已用 357 GB，其中 `tmp/` 佔 **159 GB**） | **≥500 GB 可用空間**，SSD |
| Python | 3.10.12 | ≥3.8 |
| git | 2.34.1 | ≥2.28 |

> WSL2 使用者注意：SDK 務必放在 **WSL 原生檔案系統**（`/home/...`），不要放 `/mnt/c` ——
> 跨檔案系統 IO 會讓建置慢一個數量級。本機的 WSL 虛擬磁碟另外掛了 1TB（`/dev/sdd`）。

### 1.1 相依套件（Yocto scarthgap 標準 + 少量補充）

```bash
sudo apt update
sudo apt install -y gawk wget git diffstat unzip texinfo gcc build-essential \
  chrpath socat cpio python3 python3-pip python3-pexpect xz-utils debianutils \
  iputils-ping python3-git python3-jinja2 python3-subunit zstd liblz4-tool \
  file locales libacl1 bmap-tools
sudo locale-gen en_US.UTF-8
```

---

## 2. 取得原始碼（四步，可重複執行）

```bash
# 1) 官方 SDK —— 版本必須鎖死，勿用 master
git clone --branch scarthgap_6.12_v2.3.0 https://github.com/synaptics-astra/sdk ~/sdk
cd ~/sdk
# SDK 自帶 poky/meta-synaptics 等子層；若為 submodule 形式:
git submodule update --init --recursive 2>/dev/null || true

# 2) 我們的儲存庫（含 meta-dlsdk / meta-tcm2-touch 兩個層 + 全部文件）
git clone git@github.com:hardyhuang818/sl1680-astra-terminal.git ~/sl1680-astra-terminal

# 3) 接層（腳本可重複執行無副作用；做兩件事：symlink 層目錄 + 寫 bblayers.conf）
~/sl1680-astra-terminal/tools/setup_sdk.sh ~/sdk

# 4) 驗證層都接上了
cd ~/sdk && source poky/oe-init-build-env build-sl1680
bitbake-layers show-layers | grep -E "dlsdk|tcm2"
```

### 2.1 版本錨點（對帳用）

| 元件 | 版本 |
|---|---|
| Astra SDK | tag `scarthgap_6.12_v2.3.0` |
| poky | yocto-5.0.9（SDK 自帶） |
| meta-synaptics | `d552818`（SDK 自帶，勿單獨升級） |
| kernel（linux-syna） | 6.12.62 |
| 我們的層 | `meta-dlsdk`（版本流水見其中 `VERSION` 檔，目前 v2.9）+ `meta-tcm2-touch` |

### 2.2 bblayers.conf 最終形態（供核對）

官方自帶層之外，結尾追加兩行即可：

```
  /home/astra/sdk/meta-tcm2-touch \
  /home/astra/sdk/meta-dlsdk \
```

### 2.3 local.conf 關鍵項（SDK 範本預設已帶，列出便於核對）

```
MACHINE ??= "sl1680"
PACKAGE_CLASSES ?= "package_deb"
INIT_MANAGER = "systemd"
LICENSE_FLAGS_ACCEPTED = "commercial Synaptics-EULA ..."
IMAGE_FSTYPES += " synaimg ext4 ext4.gz"
INITRAMFS_IMAGE_BUNDLE = "1"
```

我們唯一會動的開關：`ASTRA_TERMINAL_PACKAGES = ""`（設空 = 略過語音/多螢幕應用，退回原廠映像檔）。

---

## 3. 建置指令全集

**每個新終端機都要先進環境**（bitbake 只在 source 之後可用）：

```bash
cd ~/sdk && source poky/oe-init-build-env build-sl1680
```

### 3.1 全量映像檔（首次約 3~6 小時，視機器而定）

```bash
bitbake astra-media
```

產出物在 `tmp/deploy/images/sl1680/`：

| 檔案 | 用途 |
|---|---|
| `SYNAIMG/` | **完整燒錄包**（約 2.5GB），搭配官方 USB 燒錄工具 |
| `astra-media-sl1680.rootfs.ext4` | rootfs 單獨映像檔 |
| `linux_bootimgs.subimg` | **boot 包**（kernel + DTB + initramfs），可單獨 dd |
| `Image-sl1680.bin` / `*.dtb` | 裸 kernel / 裝置樹 |

### 3.2 核心快速疊代（日常最常用，全程**約 75 秒**）

改了 `dolphin-rdk.dts` 或核心原始碼之後：

```bash
bitbake linux-syna -C compile        # 大寫 C：強制從 compile 重跑（含 dts 重編）
bitbake linux-syna -c deploy -f      # 強制重新部署產出物
```

> ⚠️ 直接改的是 `tmp/work-shared/sl1680/kernel-source/` 下的原始碼 ——
> **這個目錄是易失的**，`cleanall` 就沒了。改完必須回灌成 patch 放進
> `meta-dlsdk/recipes-kernel/linux/files/`（如何正確產生 patch 見 §5.6）。

改完後**必須做位元組級 DTB 驗證**（建置成功 ≠ 改動生效）：

```bash
python3 - <<'EOF'
b = open("/home/astra/sdk/build-sl1680/tmp/deploy/images/sl1680/dolphin-rdk.dtb","rb").read()
print("我的節點在嗎:", b"synaptics,tcm-i2c" in b)
EOF
```

### 3.3 單一套件建置（改了某個 recipe 之後）

```bash
bitbake astra-voice                  # 語音主程式
bitbake sherpa-onnx                  # 本地 ASR/TTS 執行環境
bitbake dl-face                      # DL7400 對話介面
bitbake synaptics-tcm2               # 觸控驅動核心模組(meta-tcm2-touch)
```

### 3.4 清理（由輕到重）

```bash
bitbake <recipe> -c clean            # 清 work 目錄，保留 sstate
bitbake <recipe> -c cleansstate      # 連 sstate 一起清（強制真重編）
bitbake <recipe> -c cleanall         # 再連 downloads 一起清（會重新下載！慎用）
```

> 驗證「patch 能否從零套用」用 `cleansstate` + 重編即可，**不要**動不動 `cleanall`。

### 3.5 排查與檢查

```bash
bitbake <recipe> -e | grep ^SRC_URI=          # 看變數展開後的實際值
bitbake <recipe> -c devshell                  # 進 recipe 的建置環境 shell
bitbake-layers show-recipes "*tcm*"           # recipe 在哪個層
bitbake-layers show-appends linux-syna        # 誰 append 了它
oe-pkgdata-util find-path "*/astra_voice"     # 檔案屬於哪個套件
```

建置日誌位置：`tmp/work/<arch>/<recipe>/<ver>/temp/log.do_<task>`

---

## 4. 燒錄

### 4.1 全量燒錄（USB 工具，第一次/大版本用）

Windows 側用官方 `usb-tool-astra-update`（板子進 USB 燒錄模式），
把 `SYNAIMG/` 整個目錄餵給它。約 2.5GB，10 分鐘內。

### 4.2 只刷 boot（核心/DTS 疊代用，30 秒）

板子運作中直接 dd（A/B 雙槽都刷）+ **sha256 回讀校驗**：

```bash
scp tmp/deploy/images/sl1680/linux_bootimgs.subimg root@192.168.5.126:/tmp/
ssh root@192.168.5.126 'sh -s' <<'EOF'
IMG=/tmp/linux_bootimgs.subimg
SZ=$(wc -c < $IMG)
dd if=$IMG of=/dev/mmcblk0p8 bs=1M 2>/dev/null
dd if=$IMG of=/dev/mmcblk0p9 bs=1M 2>/dev/null
sync
python3 -c "
import hashlib
exp = hashlib.sha256(open('$IMG','rb').read()).hexdigest()
for d in ('/dev/mmcblk0p8','/dev/mmcblk0p9'):
    got = hashlib.sha256(open(d,'rb').read($SZ)).hexdigest()
    print(d, 'OK' if got==exp else 'MISMATCH!')
"
EOF
```

然後**斷電重上**（見 §5.1，這塊板軟重啟不可靠）。

### 4.3 燒完映像檔後還缺兩樣

| 缺什麼 | 怎麼補 |
|---|---|
| 約 2GB 語音模型（不進映像檔） | 另外灌到板子 `/home/voice/`，清單見 `meta-dlsdk/recipes-ai/astra-voice/files/README_models.md` |
| DLSDK 二進位檔（DisplayLink NDA） | 建置前放入 `meta-dlsdk/recipes-graphics/dlsdk/files/`，清單+sha256 見同目錄 `BINARIES_MANIFEST.txt`；不用 DL7400 就在 local.conf 設 `ASTRA_TERMINAL_PACKAGES = ""` |

---

## 5. 踩過的坑（每條都真踩過，按血淚值排序）

### 5.1 這塊板軟重啟不可靠
`reboot -f`、`systemctl reboot -ff`、sysrq 全部無效。**每次刷完 boot 都按「需要人工斷電重上」來安排**，
改動盡量攢成一次重啟。

### 5.2 bbappend 檔名的版本萬用字元規則（兩邊坑正好相反）
- `linux-syna` recipe **帶版本號**（6.12.62）→ append 必須寫 **`linux-syna_%.bbappend`**（帶 `_%`）
- `tzdata` recipe **不帶版本號** → append 必須寫 **`tzdata.bbappend`**（不帶 `_%`）
寫反的症狀都是 `No recipes available for ...`，bitbake 不會告訴你是檔名的問題。

### 5.3 cmake FetchContent 被 Yocto 硬關（症狀像斷網）
`cmake.bbclass` 預設 `FETCHCONTENT_FULLY_DISCONNECTED=ON`，第三方套件在 do_configure 階段
下載失敗，錯誤看起來像網路問題——其實根本沒連網。解法：全部第三方套件寫進 `SRC_URI`
（帶 sha256）+ `do_configure:prepend` 拷到原始碼樹命中 `possible_file_locations`。
參照 `meta-dlsdk/recipes-ai/sherpa-onnx/sherpa-onnx_1.13.4.bb`（14 個套件的完整做法）。

### 5.4 無 SOVERSION 的 .so 會被打進 -dev 套件
建置全綠、套件也出了，燒到板子上就是沒有那個 .so ——它被分進了 `-dev` 套件。
症狀離建置極遠，極難回溯。解法見 sherpa-onnx recipe 裡的 `SOLIBS`/`FILES` 處理。

### 5.5 映像檔整合四坑（建置成功 ≠ 功能完整）
① 兩個套件裝同一個檔案 → do_rootfs 衝突；② 兩個服務搶同一裝置（DLSDK 單行程獨佔）；
③ 套件裝進去了但服務沒 enable（`SYSTEMD_AUTO_ENABLE` 只在 recipe 裡生效，
`SYSTEMD_AUTO_ENABLE:pn-X` 寫在 conf 裡**不生效**）；④ 驗收必須用 debugfs 直讀產出的 ext4，
別信建置清單。

### 5.6 核心 patch 不能用 `git diff` 產生
Yocto 的 `do_patch` 會把舊 patch commit 進 kernel-source 的 git HEAD，
`git diff` 只會輸出**增量**（實測 70 行 vs 應有 248 行）。正確做法：
對**原始基線 dts** 做完整 `diff -u`，然後乾跑重放驗證：

```bash
# 基線 + patch == 目前原始碼，位元組級一致才算數
patch -p1 --dry-run < 新patch && patch -p1 -s < 新patch && cmp 打完的檔案 目前檔案
```

### 5.7 debugfs 不跟隨符號連結
weston.ini 真身是 `/etc/xdg/weston/weston.ini.weston-init`（update-alternatives 管理）。
用 debugfs 驗收映像檔時要查真身檔案，否則誤報「設定缺失」。

### 5.8 WSL 特有
- 從 Windows 呼叫 WSL 跑複雜指令，**用腳本檔**別用行內指令（引號會被 PowerShell/bash 兩層轉義打碎）
- Git Bash 會把 `/home/...` 路徑重寫成 Windows 路徑，呼叫 wsl 要從 PowerShell 走

---

## 6. 與板端的關係（團隊約定）

**板子是真相。** 在板上直接調通的東西，必須回灌儲存庫並跑 sha256 對帳：

```bash
tools/export_live.sh        # 拉板端檔案與儲存庫比對
```

- 版本流水：`meta-dlsdk/VERSION`（每個里程碑一條，含板端 sha256）
- 每里程碑：git commit + tag（v2.7、v2.8、v2.9…）
- 逐日改動：`backups/CHANGELOG.md`

## 7. 參考文件索引

| 文件 | 內容 |
|---|---|
| `SETUP_SDK.md` | 四步復現的最簡版 |
| `SL1680项目总览_2026-07-28.md` | 專案頂層文件（三大成果域 + 全部專題索引） |
| `meta-dlsdk/VERSION` | 版本流水與每版改動明細 |
| `SL1680_故障排查实录_2026-07-28.md` | 執行期問題排查實錄 |
| `TD7800触摸根因与视觉唤醒修复_2026-07-28.md` | 硬體電平域問題的完整方法論 |

---

*本文資料取自實際建置主機（Ubuntu 22.04.5 / 24 核 / 47GB / tmp 佔 159GB），2026-08-01 核對。*
*DLSDK 二進位檔與面板廠商資料受 NDA 約束，請勿隨文件外發。*
