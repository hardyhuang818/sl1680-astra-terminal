# SL1680 智能语音 AI — 系统现状与操作手册

> 更新：2026-07-18　板子：Synaptics Astra SL1680 @ 192.168.5.126（root / 1234）
> 定性：**内部 POC / demo**，可完整演示；产品化缺口见文末。

---

## 一、这套系统是什么

一台**能看见人、听得懂话、答得上来、说得出声、屏上有字**的本地语音 AI，跑在 SL1680 上：

```
 [C920摄像头] ──人走近──▶ NPU人体检测 ──▶ 唤醒
 [C920麦克风] ──说话──▶ VAD切句 ──▶ SenseVoice识别 ──▶ 回复分级 ──▶ matcha合成 ──▶ [Razer喇叭]
                                                    │                              │
                                          事实接地/Kimi云端/本地兜底         [DL7400屏] 显示可爱脸+字幕
```

**四层回复(逐层失败自动降级)**：① 事实类(时间/日期/天气)查真实数据 → ② Kimi 云端 LLM(联网,流式) → ③ 本地 Qwen0.5B(断网兜底) → ④ 规则。

---

## 二、当前硬件连接

| 设备 | 接口 | 作用 | 系统里的名字 |
|---|---|---|---|
| **Logitech C920** | USB | 摄像头(视觉唤醒) + 麦克风(采音) | 摄像头 `by-id`；麦 `CARD=C920` |
| **Razer Kraken** | USB | 喇叭(放回复) | `CARD=Edition` |
| **DL7400 EVK** | USB | 接显示器,显示对话界面 | DisplayLink `17e9` |
| 显示器 | DL7400 的 HDMI/DP 口 | 显示可爱脸 + 实时字幕 | — |
| 有线网 | eth0 | 连 Kimi 云端 | 网关 192.168.5.242 |

> ⚠️ **USB 声卡号/摄像头号会随重启拔插重排** —— 系统已全部改用**名字/稳定路径**寻址，不受影响。

---

## 三、当前能力清单（实测状态）

| 功能 | 状态 | 说明 |
|---|---|---|
| 👀 NPU 人体检测 | ✅ | 25ms/帧，置信 0.94 |
| 🔔 人走近自动唤醒 | ✅ | 框高≥45%触发，说欢迎语（阈值可调） |
| 👂 语音识别 | ✅ | SenseVoice，中英，正常说话准确 |
| 🧠 云端对话 | ✅ | Kimi(moonshot-v1-8k)，答得准 |
| 🧠 本地兜底 | ✅ | 断网走 Qwen0.5B |
| 📅 事实接地 | ✅ | 时间/日期答真实，天气诚实说"没联网" |
| ⚡ 流式提速 | ✅ | 边生成边按句播，首句更快 |
| 🗣️ 语音合成 | ✅ | matcha 中英双语，RTF~0.4 |
| 📺 DL7400 界面 | ✅ | 可爱脸 `(・ω・)` + 中文字幕 |
| 🧹 每天自动清理 | ✅ | 凌晨 4 点清语音段，保留模型 |
| 🔌 断网降级 | ✅ | 云端失败自动回落本地 |

**四个服务全部 active + 开机自启**：`astra-voice` / `dl-face` / `vision-wake` / `astra-cleanup.timer`。

---

## 四、操作手册

### 4.1 开机即用
**四个服务都设了开机自启，通电约 15 秒后自动就绪。** 不用手动启动。

### 4.2 日常使用
- **走近摄像头** → Astra 主动说"你好，欢迎，有什么可以帮你的吗？"
- **对着 C920 正常说话**（正常音量、面向摄像头）→ 它识别 → 回答 → Razer 出声 → DL7400 屏显字幕
- 时间/日期问它会查真实数据；其它问题走 Kimi

### 4.3 看实时日志（排查/演示用）
```bash
# 看语音对话
journalctl -u astra-voice -f -o cat

# 看视觉唤醒（框高%、触发）
journalctl -u vision-wake -f -o cat

# 一起看
journalctl -u astra-voice -u vision-wake -f -o cat
```

### 4.4 启停服务
```bash
systemctl stop astra-voice      # 停语音
systemctl start astra-voice     # 起语音
systemctl restart astra-voice   # 重启（改配置后用）
systemctl stop dl-face          # 停屏幕界面
systemctl stop vision-wake      # 停视觉唤醒
# 全停：
systemctl stop astra-voice dl-face vision-wake
```

### 4.5 配置：换云端 key / 模型
配置文件 `/etc/astra/llm.conf`（600 权限）：
```bash
vi /etc/astra/llm.conf
# DEEPSEEK_API_KEY=<你的key>          ← 换 key（这个变量名是通用容器，装任何家的key）
# LLM_MODEL=moonshot-v1-8k            ← 换模型（如 moonshot-v1-32k 更长上下文）
# LLM_URL=https://api.moonshot.ai/v1/chat/completions   ← 换服务商
systemctl restart astra-voice        # 改完必须重启
```
**验证 key 通不通**（不进语音，直接测）：
```bash
set -a; . /etc/astra/llm.conf; set +a
python3 /home/voice/astra_llm.py "你好"
# 出中文回复=通；HTTP 401=key无效；HTTP 429=额度/频率
```

### 4.6 调视觉唤醒灵敏度
唤醒阈值 = 人在画面里占的高度%。改 `vision-wake.service` 的参数：
```bash
vi /etc/systemd/system/vision-wake.service
# ExecStart=/bin/sh /usr/bin/vision_wake.sh 45 2
#                                            ↑  ↑
#                                       框高阈值%  持续帧数
# 太难触发→调小(如35)；太灵敏→调大(如55)
systemctl daemon-reload; systemctl restart vision-wake
```
> 想知道你站着时框高多少：`journalctl -u vision-wake -f` 看"框高=XX%"。

### 4.7 手动清理存储 / 看下次自动清理
```bash
systemctl start astra-cleanup.service            # 立刻清一次
systemctl list-timers astra-cleanup.timer        # 看下次自动清理时间
```

---

## 五、常见故障排查

| 现象 | 原因 | 解法 |
|---|---|---|
| **说话没反应** | 麦克风电平低/没对准 | 面向 C920、正常音量；`journalctl -u astra-voice -f` 看有没有"[跳过]太轻" |
| **回复没声音** | Razer 掉线或音量 | `cat /proc/asound/cards` 看 Edition 在不在；戴上耳机 |
| **答非所问/乱码** | 识别糊了(音量太低) | 靠近点说清楚；RMS≥0.04 才准 |
| **回复"没联网查不到天气"** | 正常 | 事实接地，不让 AI 编。要真天气得接天气 API |
| **云端不通/答得慢** | 网络或 key | `python3 /home/voice/astra_llm.py "你好"` 测；断网会自动走本地 |
| **屏幕没画面/方框** | DL7400 掉线/字体 | `lsusb|grep 17e9` 看 DL7400；中文字体是 wqy-zenhei |
| **走近不唤醒** | 阈值太高/没检到 | 看 4.6 调阈值 |
| **重启后设备错乱** | USB 重新编号 | 已用名字寻址，一般无影响；`systemctl restart astra-voice` |

**万能重启**（出问题先试这个）：
```bash
systemctl restart astra-voice dl-face vision-wake
```

---

## 六、关键文件位置

| 路径 | 是什么 |
|---|---|
| `/usr/bin/astra_voice` | 语音助手主程序（VAD+ASR+LLM+TTS 一进程常驻） |
| `/usr/bin/dl_face` | DL7400 对话界面 |
| `/usr/bin/vision_wake.sh` | 视觉唤醒检测 |
| `/usr/bin/astra-cleanup.sh` | 定时清理脚本 |
| `/home/voice/astra_llm.py` | 云端 LLM 助手(python) |
| `/home/voice/{sv,matcha,llm,kws}` | 模型：ASR / TTS / 本地LLM / 唤醒词 |
| `/etc/astra/llm.conf` | 云端 key/模型配置 |
| `/etc/systemd/system/*.service` | 四个服务定义 |
| `/tmp/astra_status.txt` | 语音↔屏幕通信（状态+字幕） |
| `/home/voice/segs/` | 语音段调试录音（每天自动清） |

源码在 PC：`D:\Claude code\Case6_Astra\meta-dlsdk\recipes-ai\astra-voice\files\`

---

## 七、产品化前要补（POC 可先放）

| 缺口 | 说明 |
|---|---|
| **TTS 商用授权** | matcha 模型无 license、espeak-ng 是 GPLv3 —— 产品化必解，走商业 TTS 最干净 |
| **AEC 回声消除** | 现在靠"放音时丢采集"的土办法，没有真 AEC |
| **本地 LLM 常驻** | 断网走本地时每轮冷启动 3~4s，应上常驻 llama-server |
| **联网天气** | 现在诚实说"查不到"，可接天气 API 真答 |
| **真唤醒词** | 现在是"一说话就应答"(VAD) + 视觉唤醒；没有"叫名字才醒" |
| **API key 轮换** | 测试用的 Kimi key 在聊天里贴过，建议重新生成 |

---

## 八、一句话现状

> **四项目标(联网/提速/DL7400界面/定时清理)全部落地，另加视觉唤醒。**
> **对着摄像头走近会打招呼，说话能对话(联网更聪明、断网也能用)，屏上有可爱脸和字幕，每天自动清理。**
> **通电即用，全部开机自启。**

*本手册每条状态均为 2026-07-18 板上实时快照。*
