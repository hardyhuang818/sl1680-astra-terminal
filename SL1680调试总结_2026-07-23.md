# SL1680 语音终端调试总结 — 2026-07-23

> 今日目标：**完全甩掉 PC**，实现"一台板子有网就跑"的本地语音助手（板载喇叭 + C920 麦 + 云端 DeepSeek 大脑），并解决喇叭出声、麦克风应答、音量控制。

---

## 一、问题与解决清单

### 1. 切换到完全本地模式（不用 PC）✅
- 停掉 PC 的 Docker 服务端 → 看门狗 `astra-mode` **自动切到本地模式** `astra-voice`。
- 本地链路：**C920 麦 → SenseVoice ASR（板上 CPU）→ matcha TTS（板上 CPU）→ DeepSeek（云端大脑）→ HT517 喇叭**。
- 板子直连 DeepSeek 验证通过（api.deepseek.com）。
- **结果：完全不用 PC，一台板子有网即可运行。**

### 2.【今日主线】HT517 喇叭"没声 / 破音"大排查 ✅（真凶＝接线）
**现象**：喇叭时而没声、时而破音，**结果飘忽不定**（下午刚接好是干净的，后来越来越差）。

**排查历程（走了弯路，记录以儆效尤）**：
- 一度**误判为软件**：以为 `I2S PRI SWITCH` 被重置是 bug、加了 `i2s-keepon` 强行摁 ON（**错误，已铲除**）；又怀疑 asound.conf / alsactl store。
- **决定性判据**：从板子端证明 **数字通路 3/3 完全一致**（DAPM 开关每次都自动 engage、PCM 每次 RUNNING、零 XRUN），而听感却飘忽 → **数字侧稳定 + 模拟侧飘忽 ＝ 铁定物理/接线故障**。
- **真凶**：
  1. **功放 GAIN→GND 那根杜邦线断了** —— GAIN 悬空导致放大器增益乱套 + 时断时续，**这就是"结果飘忽"的根本原因**。
  2. **Vin 只接了 3.3V**（应接 5V，电压余量与电流不足）。
- **解决**：重接 GAIN→GND（换新线）+ Vin 挪到 5V → **声音正常**。

> **教训**：用户报硬件问题时，**先让用户查接线/供电**，别一头扎进软件。**数字侧稳定 + 模拟侧飘忽 = 物理故障**，这是最可靠的判据。

**顺带解决的驱动坑**：`22050` 采样率经 `plughw` 会触发内核 **Oops**（`berlin_outdai_hw_params` 空指针），卡死播放设备只能重启 → 用 ALSA plug 定义 `astraout` **强制 48000** 绕开（**必需，非可选**；matcha TTS 是 22050）。

### 3. 麦克风不应答 ✅
- **现象**：对着 C920 说话，助手没反应。
- **根因**：`astra-voice` 的 `--min-rms 1000` 门槛太高 —— C920 正常说话的 RMS 只有 **351~875**，被判定"太轻/太短"直接丢弃（日志可见 `[跳过] 太轻/太短`）。
- **解决**：`--min-rms 1000 → 300`，麦增益 `60% → 80%`。

### 4. 音量太大 + 语音调音量 ✅
- `dolphinasoc` 是 **dummy codec，无硬件音量控件** → 用 ALSA **softvol** 加了个 `AstraVolume` 软件音量控件到 `astraout` 链。
- 开机默认 **10%**（`ExecStartPre` 调 `/usr/bin/astra_setvol.sh`）。
- **语音调音量**：patch 了 `astra_llm.py`（改 Python，不重编译，有 `.bak` 备份）加 `try_volume()` 拦截 —— 听到音量指令就直接操作真实 `AstraVolume`，其它问题照常走 DeepSeek。
- **可用语音指令**：「现在音量多少」/「音量调到30」/「大声一点」(+15) /「小声一点」(−15) /「静音」。

### 5. 天气"没有联网" + 联网方案讨论 🔜（明天做）
- **现象**：问天气，助手答"没有联网，查不到实时天气"。
- **真相**：DeepSeek API **是通的**。但它是语言模型、**没有实时数据**，且系统提示词要求"需要实时数据的问题直接说不知道、绝不编造" → 它如实说查不到（**正确安全行为**）。"联网"指**实时数据能力 ≠ API 连接**。音量之前答"50%"是它**幻觉**（不知真值），已被 `try_volume` 拦截解决。
- **2026 国内联网方案**（见下节建议）。

---

## 二、NPU 使用情况（回答"NPU 有没有用模型"）

| 用途 | 是否用 NPU | 说明 |
|---|---|---|
| **视觉唤醒** vision-wake | ✅ **用 NPU** | SyNAP NPU 跑 people 人体检测模型 `mobilenet224_full1/model.synap`（`synap_cli_od`）；有人走近→检测→触发欢迎语 |
| **图像预处理** | ✅ 用 NPU | 一批 SyNAP 模型做 NV12→RGB 转换，供视觉管线 |
| **ASR** SenseVoice | ❌ 跑 CPU | SyNAP NPU 是 **vision-only**，跑不了语音模型 |
| **TTS** matcha | ❌ 跑 CPU | 同上 |
| **LLM** | ❌ 云端 | DeepSeek API |

**结论：NPU 只用于视觉（人体检测/图像预处理），整条语音链一点不碰 NPU。**

---

## 三、联网模型建议（2026 国内可直连）

| 模型 | 联网能力 | 免费 | 备注 |
|---|---|---|---|
| **智谱 GLM-4-Flash** | ✅ 内置联网搜索 | **永久免费无限量**（限并发） | OpenAI 兼容，需实名。**推荐** |
| 通义千问 Qwen | ✅ `enable_search` | 有免费额度 | 阿里百炼 |
| **DeepSeek**（现用） | ❌ 无内置搜索，需自挂工具 | 便宜非免费 | 通用/推理强 |
| wttr.in（天气专项） | ✅ 天气 | 免费免 key | 不用换模型 |

**推荐方案**：保留 **DeepSeek 当主脑**，加一层 **GLM-4-Flash 联网层**（需要实时数据的问题走 GLM 联网，其它走 DeepSeek）；或只加 **wttr.in** 解决天气。

---

## 四、当前系统状态

```
[视觉] C920 → NPU(people检测) → 有人走近 → 欢迎语
[语音] C920麦(min-rms 300/增益80%) → SenseVoice ASR(CPU)
        → matcha TTS(CPU) → DeepSeek(云端) → HT517喇叭(softvol 10%)
[显示] dl-face 对话脸/字幕(DL7400 屏，如接)
[大脑] DeepSeek API (deepseek-chat)
```
- 服务全部 **active**：`astra-voice` / `astra-mode`(看门狗) / `dl-face` / `vision-wake`
- **完全不用 PC**，一台板子有网即可
- 音量语音可调（`AstraVolume` softvol），开机默认 10%

---

## 五、关键技术备忘（继续时必读）

- **astraout 必需**：`/etc/asound.conf` 定义 `plug → softvol(AstraVolume) → plug强制48000 → hw:CARD=dolphinasoc,DEV=0`。**不能让 22050 直接到硬件**，否则 Oops 卡死设备。
- **I2S PRI SWITCH 是 DAPM 正常电源管理**（空闲 OFF、播放自动 ON），**不是 bug，别去强摁**（`i2s-keepon` 已 disable）。
- **ALSA 声卡号重启会交换**（dolphinasoc 时而 card0 时而 card1）→ 一律用 `CARD=名字`，别用索引。
- **softvol 值重启不持久** → 每次启动由 ExecStartPre `astra_setvol.sh 10%` 设（脚本先播 0.3s 静音创建控件再 sset）。
- `astra_llm.py` 已加音量拦截；备份 `astra_llm.py.bak`。
- 板端 SSH：`192.168.5.126` root/1234；传文件用 PowerShell 调 `board.py`（Git Bash 会改写 `/tmp` 路径）。

---

## 六、明天继续（待办）

- [ ] **联网层**：加 wttr.in 天气 和/或 GLM-4-Flash 联网搜索层（三选一：①只天气 ②GLM联网层 ③都要）
- [ ] 板载麦 ZTS6672 驱动层问题仍未解（i2s-mic1 时钟未使能），目前用 C920 USB 麦替代 → 需 Synaptics FAE
- [ ] **安全**：DeepSeek key 已在聊天贴过，产品化前重新生成作废旧的
- [ ] 断电自启全链路最终验证（视觉唤醒 + 语音 + 音量默认）
- [ ] （可选）Phase 4：AEC 回声消除、KWS 唤醒词/能量门限（治环境音误触发）

---

*生成时间：2026-07-23 · SL1680 (Synaptics Astra) 小智兼容语音终端项目*
