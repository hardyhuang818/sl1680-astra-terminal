# SL1680 智能语音 AI + 视觉唤醒 — 实现进展与实测数据

> 日期：2026-07-18　板子：Synaptics Astra SL1680 (dolphin RDK) @ 192.168.5.126
>
> 本文记录从"能否参考 xiaozhi"到跑通完整本地语音 AI、再到摄像头视觉唤醒的实现过程。
> **证据分级**：🟢**实测**＝板上跑出的数字，可复现｜🔵**已核实**＝一手源码/文档｜🟡**推断**｜🔴**待实测**
>
> 定性：**内部 POC / demo，不出货**。产品化前的授权与工程缺口见文末。

---

## 〇、一句话总结

> **SL1680 上跑通了完整的本地语音 AI：麦克风 → 语音识别 → 大模型对话 → 语音合成，全部在板子上，一轮对话约 5~6 秒。**
> 并验证了 NPU 做人体检测 24ms/帧 —— 正在把它接成"人走近自动唤醒"。

---

## 一、完整链路与实测性能

```
C920麦克风 → Silero VAD → SenseVoice ASR → 事实接地/Qwen LLM → matcha TTS → Razer喇叭
              (切句)      (转文字)         (生成回复)          (合成语音)
```

全部本地运行，一个常驻 C 程序 `astra_voice` 把四个模型装在一个进程里，**只在启动时加载一次(~10s)，之后零加载**。

| 环节 | 方案 | 实测 | 证据 |
|---|---|---|---|
| **VAD** | Silero ONNX | 实时切句 | 🟢 |
| **ASR** | SenseVoice **int8** | **RTF 0.197**（4线程，5× 实时余量） | 🟢 |
| **LLM** | Qwen2.5-0.5B **Q8** | **19.6 tok/s** 生成，冷启动 3~4s | 🟢 |
| **TTS** | matcha-zh-en + vocos-16khz | 热合成 **RTF 0.37** | 🟢 |
| **一轮对话** | 端到端 | **~5~6 秒**（ASR 0.5s + LLM 3~4s + TTS 1s） | 🟢 |

### 关键工程决策

- **进程常驻是第一优化**：之前 CLI 每次合成都重载模型，白扔 11 秒。改成常驻后每轮零加载。
- **量化收益因模型而异，逐个实测**（见 §五）：SenseVoice 用 int8（快且省内存），vits-melo 用 fp32，llama.cpp 用 Q8_0。
- **交叉编译链路**（可复用）：Yocto 的 gcc-cross-aarch64 + binutils-cross-aarch64 + `~/xshim` 符号链接层；cmake 用 `pip install`（Yocto 的 cmake-native 缺 libbz2）；llama.cpp 必须 `-DGGML_OPENMP=OFF`（板子无 libgomp）。

---

## 二、识别错误的排查与修复 —— 全程用 CER 数字

用户反馈"识别错误"。我建了 6 句标准测试集，用**字错率(CER)** 量化每一步改动，不靠眼睛判断。

### 结果（越低越好）

| 配置 | CER 🟢 |
|---|---|
| baseline（lang=auto，Razer麦） | 48.6% |
| lang=zh | 51.4% |
| lang=zh + 前后补静音 0.2/0.2 | 35.1% |
| lang=zh + pad + gain5x | **27.0%** |
| **换 C920 麦 + 正常说话** | **≈0%（句句全对）** |

### 两个根因（都是拿录音实测定位的，不是猜）

**根因① VAD 削掉句首清辅音** 🟢
Silero VAD 把"今"的 j 这类清辅音当静音切掉，SenseVoice 拿到残缺开头就糊字。
> PC 对照实验：同一段"看这怎么样" → **前面补 0.3s 静音** → "今天天气怎么样"。
> 只前补有效，后补没用。补零(静音)即可，不必真实音频。

**根因② Razer 耳麦采得太轻** 🟢
Razer 的 Mic 已 100%/35 满档(+5dB)，用户"靠近清晰念"采到的仍只有 RMS **0.011~0.037**（识别需 ~0.04）。
> 换 **Logitech C920**（增益 0~60/+42dB）→ 采到 RMS **0.095~0.159**，响一个数量级。
> 实时数据铁律：**RMS ≥ 0.04 → 句句全对；< 0.033 → 糊**。

### 顺带修的一个真 bug 🟢
C920 是摄像头**只有麦没有喇叭**，而程序原来用同一个 `--dev` 管采集和播放 → 直接切 C920 会让回复往没喇叭的设备播，什么都听不到。**已拆成 `--dev-in`（C920采音）/ `--dev-out`（Razer出声）。**

---

## 三、LLM 幻觉 → 接地(grounding) 🟢

用户发现回答是错的：问"现在几点"答"12点"（没读时钟瞎编）、问"天气"答"晴朗气温适宜"（没联网瞎编）。

**根因是回复逻辑顺序反了**：LLM 总能吐出文字，导致报时规则永远轮不到。

**修法**（正是官方方案调研 md 说的 RAG/接地）：**事实类问题先用真实数据回答，只有开放闲聊才交给 LLM。**

| 问 | 修复前（幻觉） | 修复后（接地） |
|---|---|---|
| 现在几点了 | "现在是12点"（编） | "现在是11点13分"（读系统时钟 TZ=CST-8）✅ |
| 今天星期几 | — | "今天是星期六"（真实日期）✅ |
| 今天天气怎么样 | "晴朗，气温适宜"（编） | "我现在没有联网，查不到实时天气"（诚实）✅ |
| 你是谁 / 讲笑话 | — | 走 LLM 闲聊 ✅ |

> 加了 `--test-reply "文本"` 模式，可离线自测回复逻辑不启动音频 —— 上面结果就是这么验证的。

---

## 四、视觉唤醒（进行中）

**目标**：人走近 → 摄像头检测 → 自动唤醒语音助手。比喊唤醒词体验好，也真正用上 NPU。

| 步骤 | 结果 | 证据 |
|---|---|---|
| 板上视觉栈 | `people` 人体检测模型 + `synap_cli_od` + NPU + GStreamer synapinfer 插件 | 🔵 |
| C920 出图 | 抓到清晰画面（YUYV/MJPG, /dev/video8） | 🟢 |
| **NPU 人体检测** | **23.96ms/帧，person 置信 0.94** | 🟢 |
| 连续检测节奏 | 常驻管道 **~0.4s/帧(~2fps)**，够"人走近"用 | 🟢 |
| 阈值标定 | 用框高%↔距离 定"够近该唤醒"，**待用户走位标定** | 🔴 |
| 触发集成 | 检测到有人靠近且持续 → 触发 Astra 欢迎语 | 🔴 |

> **这印证了整个 case 的核心判断**：NPU 对视觉真强（人体检测 24ms），对语音帮不上忙（int8 量化在 A73 上还慢）。现在把 NPU 用在了它真正擅长的地方。

**一个性能坑**：spawn-per-frame（每帧重启 gst-launch）要 2.3s/帧（摄像头冷启动）。改成**常驻管道持续出帧到文件、检测循环复用 synap_cli_od**，降到 0.4s/帧，且不用写 C。

---

## 五、方法论：这一段我犯的错与纠正

这一段的价值不只在结果，也在**排查纪律**。我犯了不少错，但每个都用实测纠正了 —— 记下来避免重蹈。

| 我的错误假设 | 实测纠正 |
|---|---|
| "A73 无 dotprod ⇒ int8 量化都是负优化" | **过度泛化**。VITS int8 慢 1.9×，但 **SenseVoice int8 快 1.45×**、llama Q4 比 Q8 还慢。**逐模型实测** |
| "RMS 归一化能救糊字" | A/B 证伪：SenseVoice 前端带 **CMVN 对电平不敏感**，三档归一化结果完全一样 |
| "开 Razer 的 Auto Gain Control 能提麦克风" | 它是**播放控件**，对麦克风无用 |
| "xiaozhi 有神奇的音频前端要抄" | 源码级核实：**py-xiaozhi 的 WebRTC APM 是死代码，0 次调用**。能抄的只有 lang=zh + 别削波 |
| 构建脚本打印"✅编译成功" | 它靠"文件是否存在"判断，**旧文件冒充成功**。改成看编译退出码 |
| 采集播放用同一 `--dev` | C920 无喇叭，回复播不出 → 拆 `--dev-in/--dev-out` |

> **核心纪律：先拿地面真相，再建理论。** 之前排查麦克风时，我为一个不存在的故障连续编了六个假设（低通/削波/混叠/端点/声道抵消/数字伪影），全错 —— 根因只是用户说的是生造词"astra"。教训贯穿这一段：任何"只有一个来源能证明"的结论，都要用独立方法交叉验证；改 ASR 参数必须用**内容已知的固定录音 + CER 数字**比较。

---

## 六、产品化前必须解决的（POC 阶段可先放）

| 缺口 | 说明 | 证据 |
|---|---|---|
| **TTS 授权** | 所有开源 TTS 都有硬伤：**espeak-ng 是 GPLv3 且无条件静态链进二进制**；matcha 模型**查不到 license**（= 版权保留）；melo 上游 Bert-VITS2 是 AGPL。产品化前必解，走商业 TTS 授权最干净 | 🔵 |
| **真唤醒词 / 视觉唤醒** | 现在是 VAD 触发（一说话就响应）。视觉唤醒进行中 | — |
| **AEC 回声消除** | 现在靠"放音时丢弃采集"的权宜之计，没有真正的 AEC | 🟡 |
| **LLM 常驻** | 现在每轮冷启动 3~4s。产品化应上常驻 llama-server，降到 1~2s | 🔴 |
| **输出设备** | 现在走 Razer 耳机（还偶尔 USB 掉线）。kiosk 应该外放喇叭/HDMI 音频 | — |

---

## 七、当前状态一句话

> **麦克风(C920) → VAD → SenseVoice → 事实接地/Qwen LLM → matcha TTS → Razer 喇叭：全链路本地跑通。**
> **正常说话识别准确、时间/日期答真实数据、天气诚实说不知道、闲聊走 LLM。**
> **NPU 人体检测 24ms 已验证，视觉唤醒集成进行中。**

---

## 附录：关键文件

| 文件 | 作用 |
|---|---|
| `meta-dlsdk/recipes-ai/astra-voice/files/astra_voice.c` | 常驻语音助手主程序（VAD+ASR+LLM+TTS 一个进程） |
| `astra-voice.service` | systemd 开机自启 |
| `/home/voice/` (板上) | sv/(SenseVoice) matcha/(TTS) llm/(Qwen) kws/ bin/ |
| `D:\Astra\cer_harness.py` | CER 度量工具（改 ASR 参数用它比，别靠眼睛） |
| `SL1680语音AI_xiaozhi参考与本地AI可行性报告.md` | 更早的 xiaozhi 参考 + 可行性分析 |

*本报告每个 🟢 标记的数字都可在板子上复现。*
