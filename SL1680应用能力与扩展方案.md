# SL1680 应用能力与扩展方案

> 日期：2026-07-18　板子：Synaptics Astra SL1680 @ 192.168.5.126
>
> **证据分级**：🟢**实测**＝板上跑出来的｜🔵**已核实**＝一手源码/官方文档｜🟡**推断**｜🔴**待验证**
>
> 回答两个问题：① 原生 HDMI 能否常驻图形界面？② SL1680 能做什么应用 + GitHub 现成方案。

---

## 一、原生 HDMI 图形界面：能，而且实测点亮了

### 实测结果 🟢

我在板子上**实际点亮了原生 HDMI**：

```
card0-HDMI-A-1: connected  1920x1080
weston 运行中: DRM head 'HDMI-A-1' connector 47, EDID 'YCT Sculptor'
Output 'HDMI-A-1' ... sRGB, backlight 已初始化   ← 硬件合成桌面点亮
gst-ai IC(摄像头→NPU分类→原生HDMI): ✅ 跑通(加载模型+1000标签+appsink)
```

### 显示栈（板上全部现成）🟢

| 组件 | 状态 |
|---|---|
| DRM/KMS 硬件显示控制器 | ✅ /dev/dri/card0 + renderD128(GPU) |
| Weston(Wayland 合成器) | ✅ 实测点亮 HDMI 1080p |
| Qt5/QML 全套 | ✅ Quick/Quick3D/Controls2/Particles |
| 官方 AI demo | ✅ gst-ai(IC实测通)、syna-ai-player、6+个QML |
| HDMI 规格 | 🔵 Type-A V2.1 + HDCP2.2，4K60p |

### 踩到的坑（诚实标注）🟢

| 现象 | 说明 |
|---|---|
| gst-ai **OD 模式** "Unknown app mode" | gst-ai 自身 quirk，IC 分类能跑、OD 检测不认(与参数文件无关) |
| syna-ai-player **core dump** | Qt "wayland" 平台插件加载失败，镜像缺 Qt-wayland 集成依赖 |
| **不支持 X11** | Yocto scarthgap 只有 Wayland，GUI 要设 `QT_QPA_PLATFORM=wayland` |
| 不支持镜像(mirror) | HDMI + DSI 可各跑独立画面，不能镜像 |

> **结论**：原生 HDMI 常驻硬件合成 GUI **能力确凿**(weston+gst-ai IC 实测)。官方两个高级 demo(OD检测/AI-player)在当前镜像有 bug，要自研 GStreamer+SyNAP 管线(我们已熟)或等官方修。

### 原生 HDMI vs DL7400 🟢🔵

| | 原生 HDMI | DL7400(USB DisplayLink) |
|---|---|---|
| 原理 | **硬件合成**(GPU/显示控制器) | USB 推像素(CPU 压缩) |
| 帧率 | **4K60 满帧** | 全屏视频 ~7fps(实测) |
| 屏数 | 1 HDMI + 1 DSI | **4 口多屏**(已跑通) |
| 定位 | **主交互屏**：对话脸/视频/仪表盘/AI叠加 | 多屏信息墙/静态低刷看板 |

> **推荐**：主界面(对话脸/AI画面)放**原生 HDMI**(丝滑)，DL7400 留作辅助多屏。

**一键启动**（插屏到主板 HDMI 口后）：`sh /usr/bin/hdmi_demo.sh gst`

---

## 二、SL1680 高价值应用清单

标注：**NPU**=SyNAP视觉(真强)｜**CPU**=A73×4(无dotprod)｜**云**=需联网。

| # | 应用 | 算力 | 和已有能力叠加 | 契合度 |
|---|---|---|---|---|
| 1 | **智能看护/安防**(跌倒/滞留/离岗/遗留物) | NPU | 已做demo！扩展人体检测+规则+语音告警 | ⭐⭐⭐⭐⭐ |
| 2 | **多模态对话终端**(视觉唤醒+语音+脸+字幕) | NPU+CPU+云 | 四项已完成，差产品化封装 | ⭐⭐⭐⭐⭐ |
| 3 | **客流/行为分析**(计数/热区/姿态) | NPU | 预装YOLOv8+posenet | ⭐⭐⭐⭐ |
| 4 | **工业质检异常检测** | NPU | Anomalib类，算子需验 | ⭐⭐⭐⭐ |
| 5 | **边缘隐私家居中枢** | CPU+云 | astra_voice接HomeAssistant | ⭐⭐⭐⭐ |
| 6 | 手势交互终端 | NPU/CPU | MediaPipe手势 | ⭐⭐⭐ |
| 7 | 实时字幕/翻译屏 | CPU+云 | 字幕栈复用 | ⭐⭐⭐ |
| 8 | 门禁/人脸考勤 | NPU | ⚠️人脸线授权全非商用+无SyNAP后端 | ⭐⭐ |

---

## 三、GitHub 项目分档（2026-07-18 API 实测 star/license）🔵

### 🟢 能用（aarch64 支持 + 不绑特定NPU / 或原生吃 SyNAP）

| Repo | Star | License | 一句话 |
|---|---|---|---|
| **blakeblackshear/frigate** | 34k | MIT | **唯一原生支持 SL1680 SyNAP 的成熟视觉方案**(`type:synaptics`)。⚠️但 Docker 应用，**板子无容器运行时**，要加容器或原生移植 |
| **synaptics-synap/examples** | 10 | Apache-2.0 | 官方示例(YOLOv8/pose/Moonshine/Piper/Gemma) |
| **ggml-org/llama.cpp** | 120k | MIT | 本地LLM，已在用(Qwen0.5B 19.6t/s) |
| **roboflow/supervision** | 48k | MIT | 纯Python检测计数/热区，架构无关，客流分析直接用 |
| **home-assistant/core** | — | Apache-2.0 | 家居中枢，但走 HA Container(需Docker) |

### 🟡 需适配

| Repo | Star | License | 说明 |
|---|---|---|---|
| ultralytics(YOLO) | 60k | **AGPL-3.0** | 导出→.synap上NPU；**AGPL商用硬约束** |
| anomalib | 6k | Apache-2.0 | 工业质检，算子需验NPU |
| RapidOCR | 7k | Apache-2.0 | OCR，aarch64需自测 |
| whisper.cpp | — | MIT | ASR，translate仅→英文 |

### 🔴 不现实

人脸识别整条线(**模型非商用+无SyNAP后端**)、RAGflow(要16GB+x86)、数字人LivePortrait/SadTalker(全CUDA)、Duix-Mobile(是手机SDK非Linux)、多数数字标牌(Anthias/Xibo绑Debian或已归档)、opendatacam(绑Jetson)。

### ★关键约束（本次板上实测发现）🟢

1. **板子无 docker/podman/containerd** → 所有 Docker 应用(Frigate/HA/RAGflow)要么加容器支持要么原生移植。**最大落地门槛。**
2. **NPU 只做视觉**，GUI 框架只管渲染 → NPU 那半段自己接 SyNAP(已熟)。
3. **人脸识别授权全是雷**，产品化避开。

---

## 四、本次新做的两个 demo 🟢

### ① 智能看护 demo（已跑通）
`vision_care.sh` —— C920 + NPU 人体检测 + 时间/形状规则：
- **滞留过久**(连续在画面>N秒) → 语音"有人长时间停留"
- **区域离开**(消失>M秒) → 语音"监护区域已无人"
- **疑似跌倒**(检测框宽>高，人躺倒) → 语音"疑似跌倒，请查看"

机制：给 astra_voice 发 SIGUSR2 + 写 `/tmp/astra_say.txt` → 播报任意告警。**告警播报已实测通过。**
运行：`systemctl stop vision-wake; sh /usr/bin/vision_care.sh 15 6`（和视觉唤醒二选一，都独占摄像头）

### ② 原生 HDMI demo 启动器（已跑通核心）
`hdmi_demo.sh` —— 设好 Wayland 环境 + 启 weston + gst-ai。
运行：插屏到主板 HDMI 口 → `systemctl stop vision-wake; sh /usr/bin/hdmi_demo.sh gst`

---

## 五、最推荐的下一步

你的积木(语音AI+视觉+多屏+对话界面+看护)都就位，**最高杠杆是合成产品：**

**🥇 智能看护/安防终端** —— demo 已跑通，产品化只差：多路摄像头(CSI×2)、事件录像、告警推送(短信/APP)、看护规则细化。自研 GStreamer+SyNAP 栈比 Frigate 更轻、无容器摩擦。

**🥈 对话设备产品化** —— 把对话脸迁到原生 HDMI(满帧)，封装成开机即用一台设备。

**🥉 边缘家居中枢** —— astra_voice 接 Home Assistant，但**先确认能不能上容器**(板子现在没 docker)。

---

## 附录：本次新增文件

| 文件 | 作用 |
|---|---|
| `/usr/bin/vision_care.sh` | 智能看护(滞留/离开/跌倒告警) |
| `/usr/bin/hdmi_demo.sh` | 原生 HDMI 图形demo 启动器 |
| `/usr/share/gst-ai/od.json` | gst-ai 目标检测参数(OD模式待官方修) |
| astra_voice 新增 SIGUSR2 | 播报任意告警(`/tmp/astra_say.txt`) |

*每个 🟢 数字/结论均为 2026-07-18 板上实时验证。*
