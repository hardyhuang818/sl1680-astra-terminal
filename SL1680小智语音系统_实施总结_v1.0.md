# SL1680 小智语音系统 · 实施总结

> **版本 v1.0**　日期：2026-07-19　板子：SL1680 @ 192.168.5.126　PC：RTX 4090 @ 192.168.5.166
>
> **证据分级**：🟢**实测**＝板上/PC上跑出来的｜🔵**已核实**＝一手源码/图纸｜🔴**受阻/待办**
>
> 一天内从"分析 xiaozhi-esp32 为什么强"走到"SL1680 全双工语音 + MCP 设备控制 + 双模自动切换全部真机跑通"。

---

## 〇、最终架构（今日全部实测 🟢）

```
人走近 ──NPU视觉──→ SIGUSR1 ─┐（顺带解除暂停）
说话 ──C920麦──→ Opus/60ms ──→ astra_xiaozhi ⇄ WebSocket ⇄ xiaozhi-server(Docker)
                                   │                          ├ SenseVoice ASR(容器CPU)
听到回复 ←─Razer喇叭─← Opus ←──────┘                          ├ Qwen3-30B-A3B(4090,无思考)
     │                                                        └ EdgeTTS
     ├ MCP 设备控制：查状态/调音量/切屏(face↔clock)/看护开关/暂停聊天
     ├ dl_face 表情+字幕（DL7400 屏，多屏驱动+热插拔自愈）
     └ PC 关机/断网 → 看门狗 8 秒内自动降级 astra_voice 本地全栈 → 恢复自动升回
```

---

## 一、六个里程碑（按当日时间线）

### M1 硬件与能力核查 🔵
读完 RDK 载板原理图全 14 页 + DL7400 全 21 页 + MIPI/LVDS 图纸，产出《SL1680硬件与能力核查报告》。两个新硬结论：**载板无音频 codec/无 SD 卡**（USB 声卡是唯一免焊路径）；**DL7400 每口都是 DP+HDMI 二选一 combo、纯输出无采集**。

### M2 xiaozhi 分析与方案 🔵
回答"ESP32 为什么能做这么多"：**瘦客户端 + 云大脑 + MCP 让 LLM 长手 + 生态飞轮**——跟算力无关。SL1680 的正确姿势是"胖客户端"：联网用云端大脑，断网自己扛（ESP32 做不到）。

### M3 Phase 0 · 服务端落地 🟢
Docker(NJU 镜像 10.6GB) + SenseVoice model.pt(936MB, ModelScope) + LM Studio。挖出两大雷（见坑清单 #1#2）后闭环三连测通过。

### M4 模型换正 🟢
`lms get` 官方 **qwen/qwen3-30b-a3b-2507**（18.56GB Q4，36MB/s 下完）：无思考、89.7 tok/s 满 GPU、**首帧 11~21s → 1.25~1.43s**、正规授权。

### M5 Phase 1-3 · 板端客户端三级跳 🟢
- **astra_xiaozhi.c**（~700 行自包含 C：手写 RFC6455 + libopus + ALSA + 迷你 JSON(\uXXXX 解码) + 防回声 + 断线重连），WSL Yocto 交叉编译，HTTP 拉取部署
- **MCP 设备控制**：get_device_status(真实 CPU 66°C)/set_volume/set_screen_mode(真切 dl-face↔dl-clock)/set_vision_wake/pause_chat——**5 工具全过，LLM 选择零失误**
- **全双工**：麦克风推流→云→TTS 回放实测活了（助手自己跟房间视频声聊起来，还识别出粤语）
- **视觉唤醒接云**：人走近 → "欢迎光临呀"
- **双模看门狗** astra_mode：断服务端→本地接管；恢复→云端回归，双向实弹通过

### M6 四问排障 🟢
- 屏不亮：dl_face 曾锁单屏(已改全屏驱动+热插拔自愈)；**第二块屏是物理层不在**（dl_edid 实测只枚举到 Redmi 27 NU 一台）
- 字幕不准：状态词缩写 vs 全词比对（listen≠listening）→ Astra 回复从没显示过，已修
- 断电自启：板端全绿；PC 端 LM Studio 已自启，**Docker Desktop AutoStart=False 是唯一断点**
- 语音停止：新增 pause_chat（"闭嘴"→闭麦；到时自动/人走近恢复）

---

## 二、当前组件版本与状态

| 组件 | 版本 | 位置 | 状态 |
|---|---|---|---|
| astra_xiaozhi | **v1.0**（协议+MCP+唤醒+暂停+重试）| 板 /usr/bin + 源码 astra-xiaozhi/ | 🟢 服务运行中 |
| dl_face | **v2.0**（多屏+热插拔+paused/offline 态）| 板 /usr/bin + meta-dlsdk | 🟢 4K@60 单屏（等第二块线）|
| astra_mode | **v1.0**（curl 探测 8s 周期）| 板 /usr/bin + astra-xiaozhi/ | 🟢 enabled |
| vision_wake | **v1.1**（双目标：xiaozhi 优先）| 板 /usr/bin + meta-dlsdk | 🟢 enabled |
| xiaozhi-server | 0.9.5 + data/.config.yaml **v1.0** | PC Docker | 🟢 restart=unless-stopped |
| LLM | qwen/qwen3-30b-a3b-2507 Q4 | LM Studio(自启+JIT) | 🟢 8K ctx 满 GPU |
| astra_voice（本地兜底）| 不变 | 板 /usr/bin | 🟢 enabled（看门狗调度）|

**速查**：改服务端配置 `xiaozhi-server/data/.config.yaml` → `docker restart xiaozhi-esp32-server`｜重编板端 `wsl -d Ubuntu-22.04 bash build-wsl.sh`｜看板端日志 `journalctl -u astra-xiaozhi -f`

---

## 三、实测数据汇总 🟢

| 指标 | 数值 |
|---|---|
| 语音首帧延迟（云端模式）| **1.25~1.43s**（思考模型时代 11~21s）|
| 30B 生成速度 | 89.7 tok/s（21.1GB 显存满 GPU）|
| MCP 工具调用正确率 | 5/5 工具注册、4/4 场景 LLM 选择正确 |
| 双模切换 | 探测 8s 周期，断→本地/恢复→云端均 ≤16s 完成 |
| 双屏 | DL7400 枚举 1 台（Redmi 4K@60 已亮）；第二台物理层缺席 |
| 板端二进制 | astra_xiaozhi 77KB，零新增运行时依赖 |

---

## 四、今日坑清单（16 连，复用价值最高的一节）🟢

1. **假 35B**：`...@q8_0` 实为 Qwen3-4B NSFW 微调+不匹配 mmproj（必崩）；`@q3_k_m` 才是真 35B-A3B。model_name 必须带量化后缀
2. **服务端 0.9.5 双 system bug**：增强提示词按 `<context>` 拆两条 system → LM Studio 严格模板 400（"第一轮必成之后必挂"）。修：`prompt_template` 指向**不存在的文件**（空串会回落默认）
3. lms load 大模型必须限 `--context-length`，`--gpu max` 对超显存文件必 OOM
4. **Sec-WebSocket-Key 解码必须恰 16 字节**，否则 aiohttp 400
5. **下行采样率跟服务端 hello 走**（本次 16000，别硬编码 24k）
6. 板子 SFTP 子系统坏 → **PC 起 http.server 板上 wget 拉**是最稳部署路径
7. busybox 四连：无 `timeout`、`head -c` 不支持、nc 是无 `-w` 极简版、**wget -T 管不住 connect 挂起**（探活必须 curl -m）
8. **glibc signal() 默认 SA_RESTART**：recv 永远不醒，信号处理必须 sigaction 无 RESTART；EINTR 分支要查退出标志否则 SIGTERM 拖 90s 还把对端服务搞成 failed
9. SSH 单行里 `$(pgrep -f xxx)` 匹配到外层 shell 自己 → 用 `systemctl show -p MainPID --value`
10. **MCP 时序竞态**：服务端 initialize 后 1s 才要工具表，提问太早 LLM 无工具可用会瞎编 → tools/list 应答后再发问
11. 工具输出**单位写死**（"446/3959MB"会被 LLM 复述成"446GB"）
12. 工具名只用 `[a-zA-Z0-9_]`（服务端 sanitize 换点号，LLM 会困惑）
13. **dl_face 全词比对状态**（listening/thinking/speaking/paused），缩写不认
14. dl_face 交叉编译：gstappsink.h/libgstapp 在 gst-plugins-base **自己的 image/** 目录；libdlsdk→libusb→libudev 传递依赖用 `-Wl,--allow-shlib-undefined`
15. Git Bash 会改写 `/tmp` 参数、拆坏 wsl 单行传参 → 这类调用一律走 PowerShell + 脚本文件
16. **环境音误触发**：房间有视频声时助手会连续对话（还接了粤语）——KWS/能量门限是产品化必做

---

## 五、遗留与下一步

### 需要人工的两件小事 🔴
1. **第二块屏**：查线/显示器输入源/换口——插好后 dl_face 热插拔自动点亮，无需任何操作
2. **Docker Desktop** 勾选 "Start when you sign in"（全链路断电自愈的最后一块）

### Phase 4 技术清单 🔴
- **KWS 唤醒词/能量门限**（治环境音误触发，产品化第一优先）
- **AEC 回声消除**（现为播报时闭麦的土办法，上 AEC 才能"插话打断"）
- 服务端 `close_connection_no_voice_time` 从 120s 调大（减少空闲重连）
- 对话脸迁原生 HDMI 满帧 / 服务端接 Home Assistant 等云侧 MCP

---

## 版本历史

| 版本 | 日期 | 内容 |
|---|---|---|
| **v1.0** | 2026-07-19 | 首版：Phase 0-3 全部完成——服务端+官方30B、板端客户端、MCP 设备控制(5工具)、全双工语音、视觉唤醒、双模切换、双屏/字幕/自启/暂停四问排障 |

*所有 🟢 数据均为 2026-07-19 当日真机实测；完整坑背景见记忆库 xiaozhi-server-pc-deploy。*
